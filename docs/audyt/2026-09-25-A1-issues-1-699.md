# Audyt A1 — otwarte issues #1–#699 (stan na 2026-09-25)

Źródło: otwarte issues z API GitHuba (47 pozycji o numerach < 700, bez PR-ów) i 170 otwartych PR-ów,
porównane z `origin/main` @ `a1d9943c`. Audyt tylko do odczytu: bez zmian w kodzie, bez komentarzy
i bez zmian w issues. Testów nie uruchamiano, dowody pochodzą z odczytu kodu, testów i `git log`.

Kategorie: **a** — zrobione · **b** — częściowo · **c** — otwarte · **d** — wymaga właściciela lub produkcji ·
**e** — duplikat, nieaktualne albo źle opisane. Zapis „d (+e)” oznacza kategorię główną i uwagę drugorzędną.

## Podsumowanie

| Kategoria główna | Liczba | Issues |
|---|---|---|
| a — zrobione | 2 | #666, #681 |
| b — częściowo | 9 | #89, #372, #492, #598, #599, #605, #611, #617, #684 |
| c — otwarte | 10 | #18, #22, #23, #27, #28, #35, #600, #612, #613, #615 |
| d — właściciel/produkcja | 24 | #5, #8, #15, #29, #30, #119, #120, #193, #204, #278, #369, #370, #568, #581, #594, #595, #597, #601, #602, #603, #604, #610, #619, #647 |
| e — nieaktualne/źle opisane | 2 | #342, #614 |
| **Razem** | **47** | |

Najważniejsze ustalenia:

- **Do zamknięcia od razu (a):** #666 i #681. Oba są odebrane na produkcji. Jako nieaktualne można też zamknąć #342.
- **Ryzyko prywatności przy #602:** opis issue twierdzi, że nieoczyszczony oryginał nie trafia do trwałego storage. To nieprawda. Kreator Livewire zapisuje surowy plik, razem z GPS, w `livewire-tmp/` na dysku `r2` (`.railway/railway.ts:325` `LIVEWIRE_TEMPORARY_FILE_UPLOAD_DISK: "r2"`, `config/livewire.php:135`). Dane GPS są usuwane dopiero przy zapisie oryginału (`StoreUploadedImage.php:188`). Proponuję osobne issue z wyższym priorytetem albo co najmniej regułę lifecycle na prefiks `livewire-tmp/`.
- **Tekst prawny przy #619:** `resources/legal/polityka-prywatnosci.md:83` nadal twierdzi, że zdjęcia są w UE („dziś tak jest w przypadku hostingu, bazy, zdjęć i poczty”). Test `DokumentyPrawneNieKlamiaTest` tego zdania nie sprawdza.
- **Kolejność pracy:** #22, #23, #27 i #35 mają P1, choć według `docs/ROADMAP.md:118` i `docs/FEATURES.md` należą do V1. Przy regule P0→P1→P2 agent mógłby je wziąć przed MVP.
- Otwarte PR-y, które zamykają lub przesuwają issues z tej puli: #1523 (#89), #1646 (#372), #1658 i #1659 (#598, #599), #1660 (#601), #1661 (#602), #1497 (#617), #1645 (#647), #1653 (#684). Wszystkie checki #1523 mają status `cancelled`, więc trzeba ponowić CI.

## Tabela

| Issue | Kat. | Dowód | Proponowany komentarz zamykający (a) albo co dalej |
|---|---|---|---|
| #5 Maskotka po #15: stany, due diligence | d (+c) | Znak jest w kodzie: `public/icons/kuking-mark.svg`, `kuking-mark-mono.svg`, `favicon.ico`, ikony PWA 192/512, `kuking-udostepnianie.png`; testy `tests/Feature/LogotypIMarkaTest.php:209` (favicon), `:237` (mono), `:252` (OG), `:267` (wszystkie ikony pokazują ten sam znak). Lista due diligence: `docs/brand/MASCOT_CONCEPT.md:404` (§8, UPRP/EUIPO/TMview), ale nigdzie nie ma zapisanego wyniku z datą. Nie ma otwartego PR-a. | Czeka na #15 (bramka) i na właściciela lub prawnika (due diligence). Po #15 wybrać wariant A („znak zostaje”, zamknąć jako not planned albo completed) albo B. Nie ma tu pracy dla agenta. |
| #8 Regulamin i polityka: weryfikacja prawnika | d (+e) | Brak placeholderów: `git grep` po `resources/legal` jest pusty; pilnuje tego test `DokumentyPrawneNieKlamiaTest.php:92`. Zdanie „Umów powierzenia… jeszcze nie mamy” nadal stoi w `polityka-prywatnosci.md:81`, pilnuje go test `:320`. W `regulamin.md:3` nadal nie ma daty wejścia w życie. Ścieżki CSAM nie ma w `app/` ani `resources/views` (trafienie jest tylko w `resources/legal/regulamin.md`). **NOWE, czego opis nie zna:** `docs/legal/REJESTR_CZYNNOSCI_PRZETWARZANIA.md` i `docs/legal/REJESTR_UMOW_POWIERZENIA.md` (commit 387482a1, 22.09) oraz `COMPLIANCE.md:328` („rejestr istnieje… pola DO UZUPEŁNIENIA PRZEZ WŁAŚCICIELA”). Przełącznik `KUKING_REGISTRATION_OPEN` jest w `config/kuking.php:461`, `RegisterController.php:41`, a test w `EkranZaproszeniaTest.php:194`. Nie ma otwartego PR-a. | Zostają rzeczy człowieka: przegląd prawnika, podpisanie DPA (według `REJESTR_UMOW_POWIERZENIA.md`), uzupełnienie pól rejestru, decyzja o dacie wejścia w życie, decyzja CSAM. Po decyzji prawnika zostaje jedna rzecz kodowa: obsługa ścieżki CSAM w panelu moderacji (warto na nią założyć osobne issue). Opis zaktualizować (patrz uwagi). |
| #15 Testy z użytkownikami 50+ | d (+e) | Zestaw jest gotowy: `docs/product/TESTY_Z_UZYTKOWNIKAMI.md` (§1–§7, „Osiem zadań” §4:133). Trasy istnieją: `/szukaj` `routes/web.php:100`, `/dodaj/zdjecie` `:631`, `/dodaj/przepis` i `/jedna-strona` `:692/:694`, `/zeszyt` `:739`, `/ustawienia/czytelnosc` `:897`, `/ustawienia/twoje-dane` `:905`, `/register` `:272`. „Żurek na domowym zakwasie” jest w `database/seeders/dane/tresc-zalazkowa.json:37`, a `kuking:sprawdz-poczte` w `app/Console/Commands/SprawdzPoczte.php:70`. W repo nie ma wyników sesji. | Działanie w całości po stronie właściciela (13 sesji). Opis odsyła do nieaktualnej wersji protokołu (patrz uwagi). |
| #18 Tag tygodnia i kalendarz | c | `tag_promotions` (`database/migrations/2026_09_07_100100_create_tag_promotions_table.php:76-90`) ma tylko `tag_id`, `position`, `note` i znaczniki czasu, bez kolumn od–do. Na `/home` nie ma bloku wyróżnienia (`resources/views/pages/home.blade.php` pokazuje tylko feed tagów obserwowanych, `:83`). W `app`, `resources/views` i `routes` nie ma trafień „tag tygodnia”. Nie ma PR-a. | Najpierw decyzja właściciela: rytm ręczny czy kolumny od–do (to zmiana schematu, więc migracja, DATABASE.md i rollback). Potem blok na `/home` z CTA, które preselektuje tag, wspólnym z #370. |
| #22 Grupy tematyczne (fotofora) | c (+e) | Kodu nie ma: brak tabel `groups` i `group_members` w `database/migrations`. Są tylko w `database/reference/schema_future.sql:3,13`. Bramka V1: `docs/ROADMAP.md:118`, `docs/FEATURES.md:98`. Odwołania w opisie się przesunęły: `tag_follows` jest w `User.php:299` (nie :273) i `Tag.php:143` (nie :123); `Tag::promowane()` to `scopePromowane` w `Tag.php:218`. | Nie ruszać do spełnienia bramki V1 (WAC/D30, `kuking:raport`). Poprawić numery linii i etykietę priorytetu. |
| #23 „Moja wersja” — fork przepisu | c (+e) | W kodzie nie ma `forked_from_id` ani `recipe_forks`, jest tylko `database/reference/schema_future.sql:21`. Bramka V1: `docs/ROADMAP.md:118`, `docs/FEATURES.md:99`. Nie ma PR-a. | Czeka na bramkę V1. |
| #27 Planer posiłków i lista zakupów | c (+e) | W migracjach nie ma `meal_plans` ani `shopping_lists`. `schema_future.sql:30` ma tylko `meal_plans`, **nie ma `shopping_lists`**, choć opis twierdzi inaczej. Korekta z 23.09 się zgadza: `PublishRecipe.php:142` (`cleanIngredients`), `Ingredient.php:39` (`findOrCreateByName`). Bramka V1: `ROADMAP.md:118`, `FEATURES.md:100-101`. | Czeka na bramkę V1. Poprawić odwołanie do `schema_future.sql` przy liście zakupów. |
| #28 V2: OCR i import z URL | c | W `app` i `routes` nie ma OCR ani importera. Istnieje pole `recipes.source_scan_media_id` (migracja `2026_09_05_000400_create_recipes_tables.php:72`, `PublishRecipe.php:311`) i `Recipe::SOURCE_EXTERNAL` (`Recipe.php:40`). Wpis V2 w `FEATURES.md:109-110`. Zastrzeżenie o `recipe-scrapers` nadal stoi w `docs/research/PUBLIC_REPOS.md:35,296`. | Pozostaje w V2. Przed pracami potrzebna decyzja prawnika (granica republikacji). |
| #29 Cold start: 20 użytkowników, Bramka A | d | Narzędzia istnieją: `/admin/bez-odpowiedzi` (`routes/web.php:1131`), `kuking:wac` (`ReportWeeklyActiveCooks.php:25`), `kuking:raport` (`RaportPowrotow.php:61`), tygodniowe podsumowanie (`WyslijPodsumowaniaTygodnia.php`, `Mail/PodsumowanieTygodnia.php`), treść zalążkowa (`TrescZalazkowaSeeder.php`), zaproszenia (`RegistrationInviteController`), tagi promowane. Powiązane otwarte issues: #594, #193, #617, #15. | Działanie właściciela (rekrutacja, onboarding, pomiar). Punkt „tag tygodnia zgodnie z #18” zależy od #18, którego nie ma w kodzie; wystarcza ręczna promocja tagu z panelu `admin/tag-promotions`. |
| #30 Materiały offline + claim „nie zginą” | d | Część kodowa jest gotowa: sekcja „Twoje dane / Możesz je zabrać ze sobą” w `landing.blade.php:233-259`, historia Garnek i Durszlak w `static/about.blade.php:17,23`, test blokujący obietnicę zamknięcia `OKukingWymieniaGarnekIDurszlakTest.php:73`. W `docs/DECISIONS.md` nie ma decyzji o claimie; w `docs/marketing/KAMPANIA_GARNEK.md:771,989` są tylko teksty zaproszeń i uwaga „URL + duży QR”, gotowego materiału do druku nie ma. | Właściciel: zatwierdzić materiał offline albo z niego zrezygnować; zapisać D-xxx o claimie (z D-114). Obietnica o zamknięciu serwisu przechodzi do #8. |
| #35 Web Push: najpierw limity/quiet hours | c (+e) | Brak kodu push na origin/main `a1d9943c`: `git grep -i "vapid\|webpush\|pushsubscription"` w app/config/database/public = 0 trafień; `public/sw.js` nie ma handlera `push`/`notificationclick`; brak quiet hours / limitu dobowego / preferencji per typ. `app/Domain/Notifications/Actions/NotifyUser.php:10-18` ma tylko własną akcję, blokadę, nieaktywne konta i okno doby dla STANU (nie limit dobowy). `docs/FEATURES.md:104` umieszcza Web Push w **V1** | Nie zaczynać w MVP (V1 wg FEATURES/ROADMAP). Etap 1 (reguły w `NotifyUser`/warstwie powiadomień) można rozpisać jako osobne issue. Ujednolicić kryteria z `docs/product/RETENTION_LOOPS.md:189-191` |
| #89 Automat a11y: /login i /register mierzone jako zalogowany | b | Poprawka pierwotna na main: `0e12cb3d` (PR #104), `scripts/dostepnosc.mjs:2365` (osobny kontekst gościa dla axe). Nadal błąd w `scripts/audyt-ux50plus.mjs:258-261` (każdy kontekst `storageState: stan`, pole `zalogowany` z `:43`/`:61-71` nie steruje kontekstem) i `:273` (tylko `status() >= 400`, brak kontroli `strona.url()`). Otwarty PR #1523 (`claude/89-dostepnosc-goscinne-ekrany`, +67/-11 w skrypcie + `scripts/fixtures/audyt-ux50plus-konteksty.test.mjs`), mergeable, ale wszystkie checki `cancelled` (kolejka runnerów) | Przepuścić CI dla PR #1523 (ponowić przebieg), przejrzeć pod 4 kryteria regresyjne z issue i scalić; po merge zamknąć |
| #119 HEIC: 30 dni danych + test iPhone | d | Kod gotowy: `app/Support/RozpoznanieZdjecia.php:62` (`heic_unsupported`), sygnał `photo_upload_failed` `app/Domain/Analytics/ZapiszSygnal.php:58`, D-064 `docs/DECISIONS.md:4664`; commity `0a7228c4` (PR #252), `7ab31e7b` (PR #1209), test `ObiecujemyTylkoFormatyKtoreUmiemyTest`. Brak raportu z mianownikiem (brak sygnału sukcesu uploadu; mianownik trzeba liczyć z tabeli `media`) | Właściciel: po ≥30 dniach produkcji policzyć `heic_unsupported` względem prób uploadu (media + failed), test na fizycznych iPhone'ach (aparat/Zdjęcia/Pliki), wynik do D-064. Ewentualnie małe issue: raport odczytowy z mianownikiem |
| #120 R2: bramka na prawdziwych bucketach | d | Kod: `app/Console/Commands/BramkaR2.php:62` (`kuking:bramka-r2`, PR #246 `2279cadc`), adapter bez ACL `app/Support/Storage/R2Adapter.php`, test `tests/Feature/ZapisDoR2BezAclTest.php:69`. `docs/infra/BRAMKA_R2.md:5` „część panelowa NIEPRZEJŚCIONA", `:7-11` `KUKING_R2_PUBLICZNE_ADRESY` nie istnieje na produkcji (17.09), tabela §3 `:389-403` pusta | Właściciel: ustawić `KUKING_R2_PUBLICZNE_ADRESY` (w tym historyczny r2.dev), `railway ssh -- php artisan kuking:bramka-r2 --zapis` na prawdziwym medium, sprawdzić panel Cloudflare, wpisać wynik z datą do §3 |
| #193 Offsite pg_dump: serwis kopia-bazy + restore drill | d | Kod: `docker/kopia/`, `.railway/railway.ts:975` (serwis `kopia-bazy`), commity `31a0b40b`, `2749b9e3`, `f44c7f8d`, `d895bde2` (karta `docs/infra/DR594_PIERWSZY_ZRZUT_WLASCICIEL.md`, testy `tests/skrypty/proba-odtworzenia.sh` 144 asercje). `docs/infra/KOPIE_I_ODTWORZENIE.md:614-633` — tabela wyników §5 PUSTA; `:145-147` „liczba kopii bazy wynosi ZERO" | Właściciel: bucket + tokeny + klucz (§7.3), uruchomić `kopia-bazy` w ramach #595, wykonać drill wg karty DR594 (P0–P13), wpisać wiersz z RPO/RTO do §5 |
| #204 EmailLabs: piksel otwarć w listach transakcyjnych | d | Repo zrobiło swoje: `app/Poczta/SladySledzeniaOtwarc.php`, `app/Console/Commands/SprawdzPiksel.php` (`kuking:sprawdz-piksel`), `tests/Feature/PikselSledzacyOtwarciaTest.php`; polityka `resources/legal/polityka-prywatnosci.md:35,36,52,77`; runbook `docs/infra/POCZTA_URUCHOMIENIE.md:764` (Krok 6). PR #1272 tylko odzyskuje test `ListyKontaPrzedEmailLabsTest` (pomiar #204), nie rozwiązuje issue | Właściciel: wyłączyć śledzenie otwarć w panelu EmailLabs (lub odpowiedź Vercomu z datą), `kuking:sprawdz-piksel` na surowym źródle, wynik z datą do runbooka; potem albo zdjąć zdania z polityki, albo decyzja w DECISIONS |
| #278 PWA: dyskretna zachęta do instalacji | d | Wdrożone: PR #631 (`7c153014`, Alfa 0.49), poprawka D-053 `74572338` (PR #718); test `tests/Feature/DecyzjaInstalacjiPwaTest.php`; sygnały `pwa_prompt_shown/…/pwa_installed` `app/Domain/Analytics/ZapiszSygnal.php:62-68`; manifest bez `orientation`. `docs/design/INSTALACJA_PWA_278.md:3,14` — „natywna instalacja na telefonach niezweryfikowana" | Właściciel: test na fizycznym Android Chrome/Edge i iOS Safari, wpisać wynik do INSTALACJA_PWA_278.md i zamknąć. Trigger (powrót po ≥24 h) jest opisany tylko w dokumencie projektowym — rozważyć wpis D-xxx, o który prosi issue |
| #342 CI żąda wyrejestrowanego runnera self-hosted | e (+d) | Założenie issue nieaktualne: runnery self-hosted znów działają — np. CI 36080941235 (25.09, 01:11 UTC) success, joby `labels:["self-hosted"]` na `woogitsu-wsl-02`, `woogitsu-ubuntu26-…`. Decyzja właściciela 21.09 opisana w `.github/workflows/ci.yml:58-126` (CI na własnych runnerach, tylko `port_funkcje` na `CI_RUNS_ON_BROWSER`), commit `f51504f2`; `runs-on` `ci.yml:249`. Obecne „queued" (np. CI 36081468903 na main) to kolejka przy dużej liczbie PR-ów, nie brak runnera. Kryteria („pełny CI na runnerach GitHuba") sprzeczne z decyzją 21.09 | Zamknąć jako nieaktualne, opisując stan z 25.09 (joby podejmowane przez własną pulę) i odsyłając do decyzji z 21.09. Przepustowość kolejki, jeśli boli, w osobnym issue / #611 |
| #369 Tagi 1/2: statystyki publiczne i próg | d (+e) | Scalone i wdrożone: PR #665 (`e22b79d`), `app/Domain/Tags/TagPublicStats.php:19-55` (1 zapytanie zbiorcze, ready media, publiczne wpisy, aktywni autorzy, widoczny przepis), próg w `config/kuking.php:380` + `resources/views/components/tag-public-stats.blade.php:2`; testy `tests/Feature/TagPublicStatsTest.php:21-115` (w tym `test_batch_ma_jedno_zapytanie_dla_dwoch_i_trzydziestu_tagow`), `TagPublicStatsPagesTest.php:28-94`. `docs/design/STATYSTYKI_TAGOW_369.md:3-9,84` — blokuje tylko odbiór produkcji (brak publicznych tagów z danymi) | Wystarczą kryteria z testów; zostało pokazanie niezerowych liczb na produkcji, gdy pojawią się publiczne tagi z ≥5 zdjęciami/3 osobami. Rozważyć zamknięcie z adnotacją o granicy dowodu. Treść issue nieaktualna („Not pushed", „Draft PR #665") |
| #370 Tagi 2/2: tag jako miejsce | d | Scalone i wdrożone: PR #669 (`397a742a`, Alfa 0.63), `app/Domain/Tags/TagCollage.php` (≤5 zdjęć, jedno na autora, jedno zapytanie okienkowe), `app/Http/Controllers/TagController.php:34-35`; odbiór `aa5dc9c9`, `c7c3588b` (PR #721); `docs/design/STRONY_TAGOW_370.md:3,129-139` — lokalne dowody 320–1440/oba motywy/140%, produkcja bez publicznego tagu z danymi | Jak #369: odbiór na produkcji po pojawieniu się danych; poza tym kryteria spełnione. Punkt „pytanie po #371/#372" poza zakresem |
| #372 Poradźcie 2/2: pytania, `/pytania`, kolejka | b (+d) | Kod w main: PR #680 (merge `8bd52f1e`), `app/Http/Controllers/QuestionController.php`, `app/Domain/Questions/QuestionList.php`, widoki `resources/views/pages/questions/{index,create,show}.blade.php`; flaga `config/kuking.php:21` (`KUKING_QUESTIONS_ENABLED=false`, `.env.example:756`). Testy: `QuestionIndexTest:17,25` (nazwa + zdanie D-147), `QuestionShowTest:114` (tylko widoczne odpowiedzi top-level, bez acceptedAnswer), `:21` (usunięta odpowiedź wypada z liczników/schema/kolejki), `QuestionListTest:36,70` (ukryte/zablokowane, filtr bez-odpowiedzi), `QuestionFormTest:126` (old()), `UnansweredQuestionsTest`. Brak na main: test „QAPage nie występuje na zwykłym daniu” — otwarty PR #1646 (mergeable, CI `unstable`). Licznik N nadal pełnym COUNT: `QuestionController.php:30`. Raport `docs/product/ODBIOR_PORADZCIE_372.md` (status „W TRAKCIE”) | Dalej: scalić PR #1646; wykonać pomiar PostgreSQL p95 + `EXPLAIN (ANALYZE, BUFFERS)` licznika „Czeka na odpowiedź (N)” (gość/konto/tag) przed włączeniem flagi; ogląd 320 px/duży tekst po ostatnich zmianach; potem beta i decyzja właściciela o włączeniu `KUKING_QUESTIONS_ENABLED` (D-221). Nie zamykać. |
| #492 Meta: domknąć spójność marki | b | Meta-indeks, bez własnej implementacji. Aktualny stan: `docs/audits/WERYFIKACJA_713_492_2026_09_20.md:86-105` (sekcja „#492 — tylko pozostałe luki”: „Pełny port pozostaje CZĘŚCIOWO”, lista braków: eksport D5/C3, logowanie C4, formularze, Komuś wyszło, PWA, poczta, urządzenia fizyczne); `docs/design/POZOSTALE_LUKI_492.md` oznaczony jako historyczny; raporty `ODBIOR_PUT_I_LINKU_492.md`, `NAKLADKI_ZOOM_492.md`, `LOGOWANIE_CIEMNE_STANY_492.md` w main. Otwarty PR #1504 (narzędzie `scripts/zoom-marki.mjs`, `dolTab` #492; mergeable, clean) — to poprawka przyrządu, nie domknięcie luk | Dalej: zgodnie z DoD — każdą lukę z tabeli WERYFIKACJA §#492 przypisać do własnego wąskiego issue albo jawnie odłożyć (urządzenia fizyczne, klienci poczty → issue „wymaga człowieka”); zaktualizować `MACIERZ_KOMPLETNOSCI_517.md`; dopiero wtedy zamknąć. PR #1504 scalić niezależnie. |
| #568 Wyszukiwarka >200 wyników — odbiór wdrożenia | d | Kod: PR #592 (merge `2144a114`), `app/Http/Controllers/SearchController.php`; testy `DalszeWynikiWyszukiwaniaTest`, `GraniceOkienWyszukiwaniaTest` (401 → 200/200/1, osoby niezależne, powrót). Odbiór produkcji: `docs/design/ODBIOR_DALSZYCH_WYNIKOW_568.md` §1–3 (Alfa 0.67 `8a2ecb2`; parametry `od_przepisu`/`od_osoby` działają na produkcji, pusty zakres i powrót 200 OK; produkcja ma ~41 wpisów — >200 trafień niewykonalne bez sztucznych danych); lokalny `docs/infra/ODBIOR_WDROZEN_568_601_2026_09_20.md`. Brak otwartego PR | Decyzja właściciela: albo zamknąć jako completed z linkiem do `ODBIOR_DALSZYCH_WYNIKOW_568.md` (kod działa na produkcji, kryterium >200 nieosiągalne przy obecnym wolumenie, pokryte testami na PostgreSQL), albo zostawić jako uśpione do czasu >200 prawdziwych przepisów. Rekomendacja: zamknąć — dalsze trzymanie nie ma wykonawcy. |
| #581 Port panelu moderacji do marki | d | Port: PR #587, menu mobilne PR #637 (Alfa 0.50 `af7d70d0`), dalsze #640–#643; `resources/css/marka-panel.css`; `docs/design/DOMKNIECIE_PANELU_581.md` §1 (14/14 ekranów na wspólnej ramie, 0/147 zaszytych kolorów, 0 złotej nawigacji), poprawka `.lead`→`text-lead` z testem `PanelUzywaTypografiiMarkiTest`; `PANEL_ODBIOR_FLOTY_581_2026_09_20.md` (lokalna macierz). Brak otwartego PR | Zostało wyłącznie działanie człowieka z dostępem do panelu produkcji: ogląd szczegółów zgłoszeń i odwołań z decyzjami oraz bramki 2FA na rzeczywistych danych (bez fikcyjnych rekordów). Po jednym komentarzu z odbiorem — zamknąć; jeśli brak danych produkcyjnych, zamknąć z odesłaniem tego punktu do #492. |
| #594 Pierwszy zrzut bazy i restore drill (DR) | d | Kod i procedura gotowe: PR #1639 (merge `a27f5dda`, commity `d895bde2`, `d31f6c96`) — karta właściciela `docs/infra/DR594_PIERWSZY_ZRZUT_WLASCICIEL.md`, lokalna próba 5,5 mln wierszy `docs/infra/DR594_RUNBOOK_LOKALNY.md`, `docs/infra/evidence/dr594/RAPORT.md`; wcześniej PR #699 (`3f315b3d`). Tabela wyniku produkcyjnego pusta: `docs/infra/KOPIE_I_ODTWORZENIE.md:614-633` | Właściciel wykonuje kartę DR594 (pg_dump → restore do izolowanej bazy → weryfikacja → RPO/RTO), wpisuje wiersz w `KOPIE_I_ODTWORZENIE.md` §5 i odczytuje w panelu Railway stan Volume Backups/PITR. Ostatni punkt DoD (automatyczna kopia offsite) należy do #193. |
| #595 `railway config apply` — web/worker/scheduler | d (+e) | Przygotowanie w repo: PR #1640 (merge `a1d9943c`, commit `93366c50`) — `.railway/railway.ts:148` `PRODUCTION_SPLIT_SERVICES = true`, `NAZWA_SERWISU_WWW = "kuking.pl"` (`:680-682`), apply tylko ręcznie w `.github/workflows/railway-iac.yml`, strażnik `scripts/railway/iac.test.mjs`; runbook `docs/infra/PRZELACZENIE_NA_3_SERWISY_595.md` (status 24.09: „nic nie wykonano”). Warunek #596 (wolumen) zamknięty. Powiązany otwarty PR #1459 (sekrety per rola, mergeable/clean) | Właściciel: krok 0 runbooka (potwierdzić nazwy zasobów), `railway config plan` → staging (`KUKING_IAC_STAGING_ROZBITY=true`) → produkcja; wcześniej świeży zrzut (#594) i zgoda na koszt. Rozważyć scalenie PR #1459 przed apply (role dostają właściwe sekrety). |
| #597 Cloudflare cache `/zdjecia/*` + dłuższy podpis publiczny | d (+e) | Punkt 2 zrobiony w kodzie: `config/kuking.php:288-290` `public_signed_url_minutes` = 60 (decyzja właściciela), `app/Http/Controllers/MediaController.php:145`; testy `ZdjeciaChronioneNieWyciekajaTest`, `CloudflareCachePrivacyTest`; middleware ochrony sesji/CSRF dla anonimowych zdjęć (`bootstrap/app.php`, opis w `docs/infra/CLOUDFLARE_CACHE_597_610.md`). Punkt 1: gotowe, wyłączone reguły `docs/infra/cloudflare-cache-rules-597-610.json` — nie zastosowane (PR #1640, `fd620de4`) | Właściciel: przegląd istniejącego rulesetu Cloudflare, dodanie reguły dla `/zdjecia/*` (najpierw staging), weryfikacja `cf-cache-status` dla gościa bez cookie i MISS/BYPASS przy sesji wg `CLOUDFLARE_CACHE_597_610.md`; potwierdzić `KUKING_MEDIA_PUBLIC_SIGNED_URL_MINUTES` na produkcji. Punkt 3 (cache Policy) świadomie odłożony. |
| #598 Connection budget PostgreSQL + próg alarmowy | b (+d) | `docs/DATABASE.md:4573-4750`: `max_connections=500` zmierzone 17.09.2026 (§A), budżet 16 dziś / 13 po #595 / `8R+8` per replika (§C), progi 50/125 w `config/kuking.php` `kuking.polaczenia.*` (§D), komenda `kuking:budzet-polaczen` co godzinę, logowanie kanałem `pomiary` (§F); `app/Domain/Polaczenia/AlarmPolaczen.php`; testy `BudzetPolaczenBazyTest`, `NieudanyDzwonekNieKupujeCiszyTest`. Brakuje: szczytu aktywnych połączeń (pojedyncza próbka, §E) i działającego kanału alarmu na produkcji (`LOG_BLAD_WEBHOOK_URL` nieustawione wg §E). Otwarty PR #1658 (próbkowanie okna, szczyt z dziennika; mergeable, CI `unstable`) | Scalić PR #1658 po zielonym CI; właściciel wykonuje pomiar szczytu (także w oknie deployu) i wpisuje go do `DATABASE.md` §598; ustawić `LOG_BLAD_WEBHOOK_URL` (#599), żeby alert realnie dzwonił. Wtedy zamknąć, przekazując decyzję o PgBouncerze do #600 (§E: liczby jej dziś nie uzasadniają). |
| #599 Monitoring, queue/DB, uptime, alerty | b (+d) | Zrobione w kodzie: webhook błędów z PII-scrub (`app/Logging/WebhookBleduHandler.php`, `docs/infra/MONITORING_BLEDOW.md`); luka „tłumienie po nieudanej wysyłce” z treści issue naprawiona — `AlarmPolaczen.php:274-299` (`kanalPrzyjal` sprawdza `ostatniaWysylkaSieUdala()`), testy `NieudanyDzwonekNieKupujeCiszyTest:279,311,363,412`, `AlarmPrzyAwariiCacheTest:64`, `AlarmKopiiNieUfaBrakowiWyjatkuTest` (commity `682037e1`, `65a6d9ca`, `2384c1ea`); czujki połączeń i kolejki. Odbiór lokalny `docs/infra/MONITORING_ODBIOR_2026_09_20.md`. Otwarty PR #1659 (kolejki osobno `high/default/media/low`, puls harmonogramu; scalać po #1658) | Brakuje: dashboardu p50/p95/p99, RPS, CPU/RSS per usługa, zewnętrznego uptime monitora na `kuking.pl`/`/health`, `LOG_BLAD_WEBHOOK_URL` na produkcji + test na stagingu, kontaktu alarmowego, budżetu kosztów Railway, metryk Cloudflare — głównie panele (właściciel, kroki B1–B6 z PR #1659). Rozdział metryk per usługa dopiero po #595. Nie zamykać. |
| #600 Druga replika web, PgBouncer, worker `media` | c | Żaden warunek wstępny niespełniony poza #596 (zamknięte): #595 nie zastosowane (`PRZELACZENIE_NA_3_SERWISY_595.md`), #598/#599 otwarte. `.railway/railway.ts:796,888,949,1033` — wszędzie `numReplicas: 1`; brak osobnej usługi worker `media`. `docs/DATABASE.md` §598 E: liczby dziś nie uzasadniają PgBouncera. Brak PR | Czekać na #595 → #599 → #598. Po nich: decyzja availability/TCO o 2. replice, zapis „PgBouncer jeszcze niepotrzebny” z liczbami z §598 (spełnia jeden punkt DoD), worker `media` dopiero przy mierzalnym queue lag. |
| #601 Media job: jedno dekodowanie — odbiór produkcyjny | d (+e) | Kod: `app/Jobs/ProcessUploadedImage.php:159` (jedno `read($original)` przed pętlą), `:167` (warianty z `core()->native()`); #625 `8b4528a0`; sierota przerwanego joba naprawiona `a821e000` (`Media::METADANE_WARIANTY_W_TRAKCIE`). Testy: `tests/Feature/JednoDekodowanieZdjeciaTest.php`, `tests/Feature/PrzerwanePrzetwarzanieNieZostawiaSierotyTest.php`. Lokalny odbiór 12/24/48 MP przez `queue:work` (1 dekodowanie, 9 wariantów identycznych z baseline): `docs/infra/ODBIOR_WDROZEN_568_601_2026_09_20.md`, sonda `scripts/odbior-media-601.php`. Niespełnione: „nowa ścieżka wykonała się na produkcji” (`docs/infra/ODBIOR_JEDNEGO_DEKODOWANIA_601.md:50`). Otwarty PR #1660 (mergeable, CI unstable): test 1600 px + poprawka SELECT-u w karcie (`?` → `jsonb_exists`). | Scalić #1660, potem właściciel wykonuje kartę „Odbiór #601 na produkcji” (ODBIOR_WDROZEN_568_601…md §„karta dla właściciela”, ok. 5 min): jeden prawdziwy upload + SELECT tylko do odczytu (`ready`, 3 warianty, brak `warianty_w_trakcie`). Po wyniku zamknąć komentarzem: „Kod #625 + a821e000, testy JednoDekodowanie/PrzerwanePrzetwarzanie, odbiór produkcyjny <data, media id> — ready, 3 warianty”. |
| #602 Direct upload do prywatnego R2 quarantine | d (+e) | Brak kodu direct upload (zgodnie z P2). Dane z #605 (`docs/infra/evidence/obciazenie605/2026-09-20-gpt/RAPORT.md`): pierwszym wąskim gardłem jest SQL/zaleganie HTTP, nie upload — przesłanka do wariantu „not planned”. Otwarty PR #1661 (tylko dokument `docs/infra/DIRECT_UPLOAD_R2_ANALIZA_602.md`, mergeable) — rekomendacja: lifecycle R2 dla `livewire-tmp/`, direct upload dopiero po warunkach. Zweryfikowane: `.railway/railway.ts:325` `LIVEWIRE_TEMPORARY_FILE_UPLOAD_DISK: "r2"`, `config/livewire.php:135,157` → surowy plik kreatora (z GPS) leży w `livewire-tmp/` na R2 do sprzątnięcia; `StoreUploadedImage.php:188` usuwa GPS dopiero przy zapisie oryginału. | Scalić #1661; właściciel ustawia w panelu R2 regułę lifecycle na prefiks dokładnie `livewire-tmp/` (1 dzień). Następnie zamknąć jako `not planned` z odnośnikiem do RAPORT #605 i analizy #1661 (DoD wariant 1) — albo, jeśli ryzyko `livewire-tmp` ma zostać śledzone, przenieść je do osobnego issue (P1/bezpieczeństwo). |
| #603 Redis/Valkey dla queue/cache/locks | d | `docs/infra/REDIS_HA_DECYZJE_603_604.md` (wniosek „na razie zostawić kolejkę i cache w PostgreSQL”, koszty Redis 11–43 zł/mies., skutki awarii), `docs/infra/REDIS_HA_ODBIOR_603_604.md`, przyrząd `scripts/infra603/`, dowody `docs/infra/evidence/redis-ha/`; merge `a608bb19` (PR #1076). `config/queue.php:18`, `config/cache.php:20` = `database`. Raport sam mówi: brak produkcyjnego udziału SQL wg klas (sessions/cache/jobs), p95/p99, lock waits — „bez podstaw do zamknięcia”. §3 raportu = progi do zatwierdzenia. | Właściciel zatwierdza wariant A („zostajemy na Postgresie”) i progi z §3 raportu jako warunek powrotu; wtedy zamknąć jako `not planned` z odnośnikiem do REDIS_HA_DECYZJE_603_604.md. Pomiaru produkcyjnego udziału SQL nie da się zrobić bez dostępu do produkcyjnej bazy / #599. |
| #604 Railway PostgreSQL HA — koszt, read scaling, warunki | d | Ten sam pakiet `a608bb19`: `REDIS_HA_DECYZJE_603_604.md` — HA z automatycznym failover potwierdzone w konfiguracji usługi (2 standby, 3 etcd, 3 HAProxy), model kosztu 43–90 zł/mies. (160 zł wariant większy), odczyt standby możliwy, ale wymaga decyzji o spójności; §6 decyzje właściciela (RTO/RPO). Niewykonane: kontrolowany failover na stagingu, zachowanie aplikacji przy reconnect, budżet z PgBouncerem (zależne od #598/#595). | Właściciel wybiera RTO/RPO (§6) i okno testu. Potem test failover na odrębnym stagingu (checkboxy 3, 7, 8). Checkboxy 1, 2, 5, 6 można odhaczyć z odnośnikiem do raportu. |
| #605 Mixed load test do nasycenia | b (+e) | Przyrząd i korpus: `047460da`…`34b6ceb3`, merge `ea689192` (PR #1064); test przyrządu w CI (`ci / przyrzad_605`, `scripts/przyrzad-605.test.mjs`). Raport lokalny `docs/infra/evidence/obciazenie605/2026-09-20-gpt/RAPORT.md`: 200 tys. wpisów, prawdziwe fotografie, nasycenie 15/s OK → 30/s 72,1% błędów (timeout), wąskie gardło SQL + zaleganie HTTP, brak samoczynnego powrotu, JIT potwierdzony. Brakuje (raport, sekcja „Co sprawdzono”): R2/signed redirect, Cloudflare cache hit, docelowa topologia po #595, izolowany host, CPU/RSS osobno web/worker/media. | Nie powtarzać lokalnej rampy. Zaktualizować opis issue o wynik 20.09 i zawęzić do brakującej części: ponowna seria po #595 na stagingu z R2/Cloudflare. Decyzje właściciela z raportu (JIT dla połączeń aplikacji, limit czekania/odrzucania) rozważyć jako osobne issues. |
| #610 Cloudflare Cache Rules dla stron gościa | d | `a2fbbe23` (Refs #610, na main): `app/Support/PublicznyHtmlGoscia.php`, zmiany `PreventSharedSessionCache`, `StartSessionExceptAnonymousMedia`, `PreventRequestForgeryExceptMediaCookie`; flaga `config/kuking.php:457` `KUKING_HTML_EDGE_CACHE_SECONDS` (domyślnie 0, max 300); test `tests/Feature/CacheHtmlGosciaTest.php`; reguła do wklejenia `docs/infra/cloudflare-cache-rules-597-610.json` + `docs/infra/CLOUDFLARE_CACHE_597_610.md` §„#610 — HTML gościa na brzegu”. Pkt 4 (purge przy edycji) świadomie niezrobiony — TTL jedynym ograniczeniem. | Właściciel: zaakceptować okno nieświeżości (proponowane N=120 s), sprawdzić w panelu Always Online / Serve stale / Bot Fight `__cf_bm`, ustawić zmienną na stagingu, wkleić regułę, sonda `--cache-gate` (CACHE_KIND=html, drugie pobranie = HIT), potem produkcja. Kolejność: po #597 (otwarte, P0). Zamknąć po odbiorze sondy; purge przy edycji ewentualnie jako osobne issue. |
| #611 Uprościć CI/CD | b (+d) | Zrobione: mapa 20 jobów `docs/infra/MAPA_CI_611.md`, `docs/infra/UPROSZCZENIE_CI_611.md`; wspólna akcja `.github/actions/php/action.yml` (`3277a812`, `eddcf481`, `4d070ab6`, `f4fef83d`, `f42c6365`), jeden filtr zakresu `6586a870`; kanoniczna ścieżka deployu opisana (GitHub → Railway autodeploy → `deploy.verify`); smoke odmawia zielonego pominięcia — test `tests/Feature/TestDymnyNieJestZielonyGdyNieChodziTest.php`; stan repo zapisany (`delete_branch_on_merge=true`, brak ochrony `main`, rulesets `[]`). Brakuje: decyzja bramki scalenia/ruleset, retencja niescalonych gałęzi, `preview / smoke` daje sukces przy braku URL, historyczny `.github/workflows/audyt-a7-final-check.yml` (wyzwalacz na gałąź `audit/a7-20260909`) wciąż w repo. | Właściciel wybiera wariant A/B bramki scalenia i politykę gałęzi (MAPA_CI_611.md §„Decyzje i odbiory”). Kod: usunąć `audyt-a7-final-check.yml` (jeśli gałąź nie istnieje) i domknąć kontrakt `preview/smoke` (brak URL ≠ sukces). Potem zamknąć. |
| #612 Benchmark FrankenPHP Classic vs Octane | c | Brak benchmarku; `Dockerfile:37` świadomie bez trybu worker (Octane), `composer.json` bez `laravel/octane`. Warunki wstępne otwarte: #595 (P0), #599 (P0); #601 czeka na odbiór prod.; #605 częściowo. Raport #605 (RAPORT.md §„Co sprawdzono”) wprost: Octane nie jest następnym uzasadnionym krokiem — wąskie gardło SQL. | Zostawić uśpione do spełnienia checklisty. Dopisać w issue wniosek z raportu #605 (SQL przed Octane). Kandydat do `not planned`, jeśli po optymalizacji SQL tryb klasyczny ma zapas. |
| #613 Benchmark GD vs Imagick (libvips warunkowo) | c | Brak benchmarku; `composer.json:11` `intervention/image ^3.0`, driver GD (`ProcessUploadedImage`, `PodgladOdRazu.php:110` `ImageManager::gd`). Warunki: #595/#599/#600 otwarte, #601 bez odbioru prod. Raport #605: przy pierwszej degradacji media nie były ograniczeniem (kolejka max 7). | Zostawić do spełnienia kolejności. Dopisać wniosek z #605. Kandydat do `not planned` po #601/#600, jeśli GD ma zapas. |
| #614 Meta-issue: kanoniczny plan techniczny | e (+c) | Uzgodnienie z kodem: `docs/infra/PLAN_TECHNICZNY_614.md` (odczyt 20.09, sekcja „Rozjazdy dokumentacyjne”), merge `3c8c00d8`. Checklisty w issue nieaktualne: #585, #609, #596, #607, #608 zamknięte, a nieodhaczone; brak statusów częściowych #601 (kod wdrożony), #603/#604 (materiał decyzyjny `a608bb19`), #605 (lokalne nasycenie 20.09), #610 (kod za flagą `a2fbbe23`), #611 (mapa + composite action). | Meta-issue ma zostać otwarte, ale wymaga aktualizacji checklist wg `PLAN_TECHNICZNY_614.md` + stanu z 25.09 (odhaczyć zamknięte, oznaczyć „kod gotowy / czeka na właściciela” przy #601, #603, #604, #610, #611). Nie zamykać. |
| #615 Rewizja hostingu wg TCO/SLA (Railway vs alternatywy) | (c) + (e) | Brak commitów z `#615` na main (a1d9943c). Jedyny ślad to `docs/infra/PLAN_TECHNICZNY_614.md:60` ("OPEN, warunkowe… Wrócić dopiero przy mierzalnej przeszkodzie"). Żadnego PR. | Zostawić otwarte jako issue warunkowe/parkowane. Wracamy, gdy pojawi się któryś z mierzalnych sygnałów z treści (koszt, SLA/RTO/RPO, latency, brak funkcji po #604). Dziś nie ma nic do zrobienia. |
| #617 DR zdjęć R2 przed logicznym usunięciem | (b) + (d) | Na main jest tylko rekomendacja: `docs/infra/LOKALIZACJA_DANYCH_R2.md` §6a (oznaczona „[REKOMENDACJA — NIE WYKONANA]”, rygiel nie na `incoming/`, wariant B: kopia/migawki) oraz sprostowanie o Bucket Lock (ca03724d). W tabeli §5 wiersz 7 „bucket kopii zdjęć — jeszcze nie istnieje”. Otwarty PR #1497 (`claude/617-dr-zdjec-r2`, mergeable clean, base 529d0c1d, 39 commitów za main) dokłada: token per bucket, dysk `r2_kopia_zdjec`, komendę `kuking:sprawdz-kopie-zdjec`, runbook `docs/infra/DR_ZDJEC_R2.md`, D-257 (na main najwyższe D-256, więc numer bez kolizji), testy `KopiaZdjecSprawdzanaTylkoOdczytemTest` i `PoswiadczeniaBucketowR2Test`. Opis PR: „Kopii dziś nie ma… Refs”. | Zrecenzować i scalić #1497 (CI jeszcze niepotwierdzone, test PHPUnit „sprawdzi CI”). Potem właściciel musi: założyć bucket kopii i token tylko do odczytu, ustalić retencję i koszt (łącznie z ryzykiem operacji klasy A, które PR sam wskazuje), zrobić restore drill na kilku mediach, zapisać RPO/RTO i to, kto może usuwać i odtwarzać kopie. Żaden checkbox z DoD nie jest dziś spełniony. |
| #619 R2 EU Jurisdiction a polityka prywatności | (d) + usterka tekstu | Kod gotowy: strażnik hosta `App\Support\Storage\DozwolonyHostR2` przepuszcza tylko `<konto>.eu.r2.cloudflarestorage.com` (D-255, ff84a710, `LOKALIZACJA_DANYCH_R2.md` §3a). Sprawdzenie 12 w `kuking:bramka-r2` jest w `LOKALIZACJA_DANYCH_R2.md` §3. PR #1278 (b15b1a43) złagodził politykę: `resources/legal/polityka-prywatnosci.md:9` i `:51` mówią teraz „Nie potwierdziliśmy…”; test `DokumentyPrawneNieKlamiaTest::test_polityka_nie_obiecuje_jurysdykcji_r2_bez_pokrycia_w_endpoincie`. Tabela panelu (§5 `LOKALIZACJA_DANYCH_R2.md`) jest nadal pusta, a `R2_ODBIOR_2026_09_20.md:99` mówi: „Nie zamykamy #619 samym złagodzeniem zdania”. **Sprzeczność:** `polityka-prywatnosci.md:83` dalej twierdzi „dziś tak jest w przypadku hostingu, bazy, **zdjęć** i poczty” (dane w UE). Test sprawdza tylko zdanie z :9 i wiersz tabeli, więc tego zdania nie łapie. | Właściciel: odczytać w panelu Cloudflare każdy bucket (typ lokalizacji / `eu`, data) i uruchomić `kuking:bramka-r2 --zapis` na produkcji. Wynik wpisać do §5, bez ID konta. Programista, od razu: poprawić :83 zgodnie z :9/:51 (razem z #8) i rozszerzyć test o to zdanie. Zamknąć dopiero po datowanym odczycie panelu. |
| #647 Tagi w opisie: podpowiedzi po # i liczniki | (d) | Scalone #649-seria / #701 / PR #714 (f984e42a, f692ecf5). `resources/js/tagi-w-opisie.js` + `tagi-w-opisie.test.mjs`. Licznik publiczny w `app/Domain/Tags/PodpowiedziTagow.php`, test `tests/Feature/PodpowiedziTagowEndpointTest.php`. Sprzeczność ze zrzutu naprawiona w `resources/views/components/tagi-formularz.blade.php:23-28` (`$pasujeDokladnie = $juzWybrany \|\| …`). Bez JS: `<details open>` przy frazie, 3 testy renderu w `TagiWpisowTest`. Tagi stoją pod opisem (`pages/posts/create.blade.php:111-117`). Stare copy „poniżej” już nie występuje. Tabela kryteriów w `docs/design/TAGI_W_OPISIE_647.md` („Odbiór PR #701”). Brakuje odbioru popupu na produkcji, bo wymaga zalogowania. Otwarty PR #1645 (Refs #647, mergeable, base = main a1d9943c) usuwa z `TagSuggester.php:150` nieużywany `withCount` (liczył także wpisy nie-publiczne). | Scalić #1645 jako porządki. Właściciel na swoim koncie, na telefonie: `#` w opisie → podpowiedź z licznikiem → publikacja → edycja z usunięciem taga. Jeśli wszystko działa, zamknąć komentarzem odsyłającym do `docs/design/TAGI_W_OPISIE_647.md` i tego odbioru. |
| #666 Liczniki wykonań nie nazywają zdarzeń osobami | (a) | bdc56b8c (PR #671), c7c3588b (PR #721), odbiór produkcji aa5dc9c9 i 2a17a5be (#673). `resources/views/pages/recipes/show.blade.php:490` („Zrobię ponownie: X z Y odpowiedzi”, próg ≥3). `:676` (`Odmiana::rzeczownik(…'wykonanie','wykonania','wykonań')`). `:678` („% odpowiedzi”). `RecipeController.php:427-428` (komentarz #666 „wykonania i odpowiedzi, bez deduplikacji”). Test `tests/Feature/CookedCountsUnitsTest.php`: `test_repeated_cooking_counts_events_with_correct_unit`, `test_conflicting_answers_from_one_person_remain_separate_answers`, `test_unanswered_event_does_not_reach_the_three_answer_threshold`. Rodziny `KomuWyszlo…`, `LicznikUgotowan…`, `PrzepisyLiczniki…`, `WykonczenieProduktu…` zaktualizowane. Dowody: `docs/design/LICZNIKI_WYKONAN_666.md`. | „Zrobione w #671 (bdc56b8c) i domknięte w #721. Liczniki na stronie przepisu opisują teraz wykonania (odmiana 0/1/2/5/12/22) i odpowiedzi (`show.blade.php:490,676-678`), bez deduplikacji osób. Regresja: `CookedCountsUnitsTest` (wielokrotne wykonania jednej osoby, sprzeczne odpowiedzi, próg 3), dowody w `docs/design/LICZNIKI_WYKONAN_666.md`, odbiór produkcji w aa5dc9c9. Zamykam.” |
| #681 Szersza /tagi i fotograficzne kafle A–Z | (a) | PR #688 (06c8c7e5, a228675b), odbiór produkcji PR #694 (b65b91f8), wyjaśnienie „0 wpisów” PR #705 (aea56b13). `resources/css/marka-rama.css:66` (`.app-main:has(> .tag-directory-intro)` → `max-width:none`). `resources/views/pages/tags/index.blade.php:93` (`x-tag-directory-card` z `$collages` dla wszystkich tagów strony). `TagController.php:95-98` (`collage->forTags` polecane + A–Z, widz). `components/tag-directory-card.blade.php` (zdjęcie, wariant bez zdjęcia, autor zdjęcia). Testy: `TagPlacePagesTest` (`test_directory_cards_have_public_photos…`, `test_directory_uses_no_photo_when_only_private_post_has_one`, `test_rendering_thirty_promoted_collages_does_not_add_queries_per_card`), `ZeroWpisowWSpisieTagowTest`. Dowody w `docs/design/FOTOGRAFICZNE_TAGI_681.md` i `evidence/tagi681/`. | „Zrobione w #688 i odebrane na produkcji w #694 (b65b91f8). /tagi wykorzystuje ramę marki, wszystkie tagi A–Z mają kafle z publicznym zdjęciem albo wariant bez zdjęcia, bez N+1 (`TagPlacePagesTest`). Zgłoszone „0 wpisów” wyjaśnione w #705: licznik D-087 liczy tylko wpisy publiczne, strona tagu mówi to autorowi (`ZeroWpisowWSpisieTagowTest`). Zamykam.” |
| #684 Widget „Wygląd” zasłania treść dla myszy/dotyku | (b) | Część 2 (podpowiedź `aside`) zrobiona: 2bab564d (PR #707), `resources/js/szybki-wyglad.js:186-211` (`wheel`/`pointerdown`/`touchstart` chowają podpowiedź), strażnik Playwright 1dcbc6dd (PR #1293, `scripts/szybki-wyglad.mjs`). Część 1 (pływający przycisk nad kaflem) na main nie jest zrobiona: przejście „w przepływ” dotyczy tylko `.field-error` (:78-88) i fokusu (:89-93). Otwarty PR #1653 (`claude/684-wyglad-mysz`, base = main a1d9943c, mergeable, stan unstable = CI w toku) dodaje `elementsFromPoint` w `geometry()` i test `scripts/przegladarka/wyglad-nawigacja.test.mjs`. Uwaga: dotknięcie samej podpowiedzi nad kaflem jej nie chowa (`hint.contains`), chowa ją dopiero przewinięcie lub dotknięcie obok. | Doprowadzić #1653 do zielonego CI i scalić. Sprawdzić scenariusze z issue: 320/140% z kolażem i 360/390/100% z podpowiedzią (dziś tylko „Refs”). Zdecydować, czy wystarczy, że podpowiedź ustępuje przy wheel/tap obok. Potem zamknąć z dowodem pomiaru wskaźnikiem. |

## Do zamknięcia od razu

- **#666**: zrobione w PR #671 (`bdc56b8c`) i #721, odebrane na produkcji (`aa5dc9c9`). Test: `tests/Feature/CookedCountsUnitsTest.php`. Komentarz zamykający jest w tabeli.
- **#681**: zrobione w PR #688, odebrane na produkcji w #694 (`b65b91f8`), „0 wpisów” wyjaśnione w #705. Testy: `TagPlacePagesTest`, `ZeroWpisowWSpisieTagowTest`.
- **#342** (jako nieaktualne): runnery self-hosted podejmują joby (np. przebieg CI 36080941235 z 25.09 zakończony sukcesem). Kryterium „pełny CI na runnerach GitHuba” przeczy decyzji właściciela z 21.09 (`.github/workflows/ci.yml:58-126`). Zamknięcie wymaga potwierdzenia właściciela.
- **Kandydaci po jednym ruchu:** #89 po scaleniu #1523, #684 po scaleniu #1653, #647 po odbiorze produkcyjnym. Przy #568 DoD „>200 wyników na produkcji” jest przy obecnym wolumenie niewykonalne i potrzebna jest jawna decyzja o zamknięciu.

## Źle opisane / etykiety

### #5–#30

- **#8, opis nieaktualny.** Zdania „Rejestru czynności przetwarzania nadal nie ma w żadnym pliku” (w ramce i w liście do zrobienia) są fałszywe od 387482a1 (22.09): rejestr i rejestr umów powierzenia istnieją w `docs/legal/`. Kryterium „rejestr istnieje jako plik” jest spełnione; zostały pola do uzupełnienia przez właściciela i przegląd prawnika. Pytanie 2 („zamknięcie wymagałoby osobnej zmiany w kodzie”) też jest nieaktualne, bo jest przełącznik `KUKING_REGISTRATION_OPEN` (`config/kuking.php:461`, `RegisterController.php:41`). Numery linii testów się przesunęły: `:90`→`:92`, `:141`→`:143`, `:216`→`:320`, `:256`→`:360`, `:299`→`:403`, `:408`→`:512`, `:437`→`:541`, `:567`→`:671`, `:835`→`:939`. `routes/web.php:231-233`→`:272-274`.
- **#15, opis nieaktualny.** Tabela „§1–§9” i „dziesięć zadań” nie pasuje do obecnego `TESTY_Z_UZYTKOWNIKAMI.md`: ma on §1–§7 i protokół na **osiem** zadań. Rejestracja, komentarz, usuwanie, blokowanie i eksport są w nim wprost wyjęte z tej rundy (§1), a nie ma w nim §9 o rekrutacji. Numery linii tras są nieaktualne (np. `/register` :231→:272, `/dodaj/zdjecie` :583→:631, `/zeszyt` :690→:739).
- **#22, #23, #27 mają P1, choć są za bramką V1** (`ROADMAP.md:118`: „Planner/groups/forks dopiero gdy WAC i D30…”). Przy regule „P0→P1→P2” z CLAUDE.md agent może je wziąć przed zadaniami MVP z P2. Warto zmienić na P2 albo dodać etykietę „V1/zablokowane bramką”. (#28 z V2 ma P2 i to jest spójne.)
- **#27:** zdanie „`shopping_lists`… schemat poglądowy w `schema_future.sql`” jest nieprawdziwe, bo plik ma tylko `meal_plans` i `pantry_items`.
- **#22:** nieaktualne numery linii (`User.php:273`→`:299`, `Tag.php:123`→`:143`) i nazwa `Tag::promowane()`, która w kodzie jest scope'em (`scopePromowane`, `Tag.php:218`).
- **#18:** w tytule jest „kalendarz polskiej kuchni”, a opis sam mówi, że kalendarz może zostać dokumentem. Pierwszym krokiem powinna być decyzja właściciela „ręczny rytm czy daty”; bez niej kryteria odbioru są niewykonalne. Rozważyć etykietę „wymaga decyzji”.
- **#5 i #30** mają P2, ale #5 jest zablokowane przez #15 (P0). Etykiety są spójne, tylko brakuje oznaczenia „blocked”.
- Żaden otwarty PR (`prrefs.txt`) nie odwołuje się do tych dziesięciu issues.

### #35–#370

- #89: tylko etykieta `P1`; brak `typ: błąd` i obszaru (np. `obszar: ux`/`obszar: infra`).
- #35: `P1` przy funkcji z V1 (`docs/FEATURES.md:104`) — sprzeczne z zasadą MVP-first; kryteria zgody („po realnym zdarzeniu", pierwszy przypadek `cooked_event.created`) rozjeżdżają się z `docs/product/RETENTION_LOOPS.md:191` („po 3. wpisie", 2 typy zdarzeń: ugotowanie + odpowiedź na pytanie).
- #342: etykiety `P0` + `typ: błąd` nieaktualne; tytuł i kryteria („runnery GitHuba") sprzeczne z decyzją właściciela z 21.09 w `ci.yml:58-126`.
- #369: sekcje „PR #665 — dostarczenie w toku" i „Implementation status 18.09 … Not pushed" są nieaktualne (PR scalony, wdrożone); sekcja po angielsku z „?" zamiast myślnika. Zdanie „Nie istnieje jeszcze `TagPublicStats`" fałszywe od 18.09.
- #370: sekcja „Stan po audycie 16.09" mówi, że nie ma kolażu ani statystyk — nieaktualne od PR #669/#665.
- #193: `docs/infra/KOPIE_I_ODTWORZENIE.md:145-147` nadal mówi „nie ma nigdzie bucketu R2, tokenów…" — zweryfikować, czy nie zostało zmienione nocą 17/18.09 (`bd8745c9`).
- #278: issue prosi o zapis triggera jako decyzji — w `docs/DECISIONS.md` brak osobnego D-wpisu dla #278 (są tylko odwołania, np. `:12857`).
- #120: w treści issue wymagane punkty są spójne z `BRAMKA_R2.md`, ale tabela §3 ma też punkty 7, 8, 11, 12 (15 MB, formaty, kasowanie, alert), których issue nie wymienia — warto ujednolicić listę.

### #372–#600

- **#581**: jedyna etykieta to `P1` — brak `typ:` i `obszar:` (proponuję `typ: zadanie`, `obszar: ux`, `obszar: marka`). Tytuł „Pełny port kompozycji…” jest nieaktualny — port jest wdrożony; zostały wyłącznie odbiory produkcyjne (sugestia: „Panel moderacji: produkcyjny odbiór szczegółów zgłoszeń/odwołań i bramki 2FA”). Priorytet P1 wyższy niż meta #492 (P2), mimo że zakres to już tylko ogląd — do rozważenia obniżenie do P2.
- **#594, #595, #597, #598, #599**: brak etykiety `typ:` (tylko `P0` + `obszar: infra`); **#600** też bez `typ:`. Większość zostało „działaniem właściciela” — przydałaby się etykieta w rodzaju „wymaga właściciela/produkcji”, jeśli projekt taką ma.
- **#595**: opis nieaktualny — cytuje `.railway/railway.ts:120` (flaga jest dziś w `:148`) i twierdzi, że „nazwy serwisów w IaC nie odpowiadają niczemu w Railway (`web`)” — od PR #1640 (`93366c50`) IaC używa `NAZWA_SERWISU_WWW = "kuking.pl"`; krok „najpierw sprawdzić wolumen” jest już spełniony (#596 zamknięte). Treść powinna odsyłać do `docs/infra/PRZELACZENIE_NA_3_SERWISY_595.md`.
- **#597**: nieaktualne odwołania do linii — `config/kuking.php:275` (dziś 288–290) i `MediaController.php:163` (dziś logika TTL od `:145`). Punkt 2 („dłuższy podpis publiczny”) jest już zrobiony (60 min), a opis tego nie odnotowuje. Tytuł łączy z #610 w jednym dokumencie — warto dopisać w treści link do `CLOUDFLARE_CACHE_597_610.md`.
- **#599**: sekcja „Potwierdzona luka dostarczenia alarmu — main 341d350” jest nieaktualna: na main `AlarmPolaczen::kanalPrzyjal` sprawdza potwierdzenie kanału, a `NieudanyDzwonekNieKupujeCiszyTest` pokrywa scenariusz 500/wyjątek → następna próba 200. Należy dopisać, że ta luka jest zamknięta.
- **#594 vs #193**: częściowe nakładanie — ostatni punkt DoD #594 („automatyczna kopia logiczna poza SPOF”) to zakres #193; karta DR594 sama to rozdziela. Proponuję wykreślić ten punkt z #594 i zostawić tylko odwołanie do #193.
- **#598**: pierwsze trzy punkty „Co ustalić” są częściowo zrobione (`max_connections` z 17.09) — w treści brak odnośnika do `docs/DATABASE.md` §„Budżet połączeń PostgreSQL — issue #598”.
- **#568**: tytuł i treść poprawne; DoD („pełne zakresy >200 na produkcji”) jest przy obecnym wolumenie niewykonalne (udokumentowane w `ODBIOR_DALSZYCH_WYNIKOW_568.md` §2) — potrzebna jawna decyzja o zamknięciu.
- **#372**: treść aktualna; checkbox „Testy obowiązkowe” nie są odhaczone, choć większość testów jest na main (lista w tabeli) — warto odhaczyć, zostawiając QAPage-na-daniu (PR #1646) i ogląd 320 px.

### #601–#614

- Żadne z #601–#613 nie ma etykiety `typ:` (np. `typ: zadanie`); ma ją tylko #614.
- #601 (P1): cała pozostała praca to odbiór produkcyjny właściciela — tytuł już to mówi, ale sekcja „Stan po audycie 16.09” nadal twierdzi, że job „dziś wykonuje `read($original)` osobno dla każdego wariantu” (nieaktualne od #625) i checklisty testów są nieodhaczone mimo testów na main.
- #602: zdanie „nieoczyszczony oryginał nie trafia dziś do trwałego storage” jest nieścisłe — kreator Livewire zapisuje surowy plik (z GPS) w `livewire-tmp/` na dysku `r2` (`.railway/railway.ts:325`, `config/livewire.php:135`). Wymaga korekty opisu; ryzyko warte osobnego issue z wyższym priorytetem niż P2.
- #603/#604: opisy nie odnotowują raportu `REDIS_HA_DECYZJE_603_604.md` (`a608bb19`); checkboxy częściowo spełnione.
- #605: opis („Co baseline naprawdę pokazuje”) opiera się wyłącznie na baseline 15.09 `load581`; brak wzmianki o rampie 20.09 (`obciazenie605/2026-09-20-gpt/RAPORT.md`). Zakres do zawężenia.
- #610: sekcja „Stan” nieaktualna (sprzed `a2fbbe23`); issue ma P1, a #614 umieszcza je w „P2 / gdy dane uzasadnią” — rozjazd priorytetu.
- #612 i #613: P3 zgodne z #614; oba warto uzupełnić o wniosek z raportu #605 (pierwsze ograniczenie = SQL).
- #614: etykieta P2 dla meta-issue zawierającego listę P0 jest myląca (rozważyć brak priorytetu lub etykietę meta); checklisty nieaktualne jak wyżej.
- PR-y #1405 i #1391 wspominają #605 tylko pośrednio (dotyczą #821/#1058) — nie rozwiązują żadnego issue z tej grupy.

### #615–#684

- #666 i #681: kryteria są spełnione i odebrane na produkcji (aa5dc9c9, b65b91f8), a issues nadal są otwarte. Do zamknięcia.
- #615, #617, #619 nie mają etykiety `typ:` (tylko `obszar: infra` + priorytet). #615 przydałaby się też etykieta w rodzaju „warunkowe/odłożone”, bo to parking z warunkiem powrotu, a nie zadanie do wzięcia.
- #619 ma `P0`, ale część kodowa jest skończona (D-255, strażnik hosta, złagodzona polityka). Został odczyt panelu przez właściciela i jedno zdanie w polityce. Rozważyć etykietę „wymaga właściciela”.
- #619: treść odsyła do `docs/infra/BRAMKA_R2.md`, a faktyczny dokument to `docs/infra/LOKALIZACJA_DANYCH_R2.md` (+ `R2_ODBIOR_2026_09_20.md`). Stan opisany w issue (polityka mówi „UE”) jest nieaktualny po #1278, poza zdaniem `polityka-prywatnosci.md:83`.
- #617: treść nie wspomina, że §6a `LOKALIZACJA_DANYCH_R2.md` już zawiera rekomendację (bez rygla na `incoming/`, wariant B). PR #1497 dodatkowo prostuje tam lifecycle 31 dni.
- #647: kryterium „podstawowa ścieżka z JS bez osobnego szukania przyciskiem” i późniejsze wymaganie z 18.09 (tagi przy opisie) są zrobione. Opis PR #1645 („uproszczenie sekcji tagów zostaje otwarte”) jest częściowo nieaktualny względem main.
- #684: tytuł mówi, że odsłanianie działa „tylko dla klawiatury”. Po 2bab564d to już nieprawda dla podpowiedzi, zostaje przycisk. Warto zaktualizować opis.
