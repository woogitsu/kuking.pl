# Audyt A3 — otwarte issues #1100+ (stan na 2026-09-25)

Stan kodu: `origin/main` = `a1d9943c` (merge PR #1640). Zakres: 84 otwarte issues
o numerach ≥ 1100 (najniższy otwarty to #1245), bez pozycji będących PR-ami. Dla każdego:
odczyt kryteriów akceptacji, sprawdzenie kodu na main (plik:linia), `git log --grep '#N'`,
przegląd otwartych PR-ów (diff względem `origin/main` na nazwanych refach, bez `FETCH_HEAD`)
i stanu `mergeable_state` z API GitHub. Nie uruchamiano pełnego zestawu testów;
dowody pochodzą z odczytu kodu i testów w PR-ach.

Kategorie: **a** zrobione na main · **b** częściowo · **c** otwarte · **d** wymaga
właściciela/produkcji · **e** duplikat / nieaktualne / źle opisane. Kategorie się łączą,
np. „c + e (brak etykiet)”.

## Podsumowanie liczbowe

| Kategoria (główna) | Liczba | Issues |
|---|---|---|
| a — zrobione na main | **0** | — |
| b — częściowo na main | **5** | #1313, #1315, #1346, #1351, #1390 |
| c — otwarte (problem nadal na main) | **77** | pozostałe; 75 z nich ma otwarty PR (bez PR-a: #1657, #1662) |
| d — tylko właściciel (główna) | **1** | #1334 (propozycja funkcji poza MVP) |
| e — duplikat (główna) | **1** | #1401 = duplikat #969 |

Dodatkowo:
- **d jako dopisek** (kod w PR, ale zostaje decyzja/działanie człowieka): #1297, #1302, #1304, #1306, #1310, #1324, #1365, #1385, #1394 (9).
- **e jako dopisek**: 67 z 84 issues nie ma żadnej etykiety; nieaktualny opis/ścieżki w #1310, #1313, #1317, #1329, #1346, #1354, #1364, #1368, #1390, #1534.
- **Bez żadnego PR-a**: #1334, #1657, #1662.
- **Stan PR-ów**: prawie wszystkie `unstable` (czerwone lub niekompletne checki); konflikt (`dirty`): #1519, #1590, #1624, #1656 (draft).
- Wniosek główny: nowe issues są „zrobione w PR-ach, nie na main”. Wąskim gardłem nie jest
  implementacja, tylko doprowadzenie ~70 PR-ów do zielonego CI i scalenie ich w bezkonfliktowej kolejności.

## Tabela

| Issue | Kategoria | Dowód | Proponowany komentarz zamykający (a) / co dalej |
|---|---|---|---|
| #1245 Eksport ZIP niesie komentarze od zablokowanych/niedostępnych autorów | c + e (brak etykiet) | Na main `app/Domain/Users/Exports/CollectUserExportData.php:204,258,294` ładuje `comments.replies.author.profile` bez `widoczneDla($user)`; `Comment::replies()` (`app/Models/Comment.php:139-144`) filtruje tylko status. Brak commitów z #1245. PR #1586 (unstable) dodaje `foreignCommentRelations()` z `widoczneDla` na korzeniach i odpowiedziach + `EksportKomentarzyTaSamaGranicaCoEkranTest` (blokada, zdjęcie blokady, `moje_komentarze`, skan wszystkich plików json/html ZIP). | Co dalej: dociągnąć checki PR #1586 do zielonych i scalić; pokrywa kryteria (sprawdzić, czy test obejmuje osobno `banned` i `pending_delete` oraz wszystkie trzy typy treści). |
| #1280 Sitemapa: lastmod profilu tylko z `profiles.updated_at` | c | Na main `app/Http/Controllers/SitemapController.php:124` — `lastmod` = `$profile->updated_at`. PR #1598 (unstable) liczy max `updated_at` publicznych opublikowanych wpisów/przepisów (także trashed/ukrytych) dwoma zapytaniami na partię 500 + `MapaStronyLastmodProfiluTest`. Sam PR deklaruje znaną lukę: zmiana public→prywatny nie przesuwa daty (kryterium 3 częściowo). | Co dalej: scalić #1598 lub zaakceptować jawnie lukę public→private (albo dopisać ją jako osobne issue); PR łączy też #1032 (lista hubów) — sprawdzić kolizje w `SitemapController`. |
| #1289 Powitalna: „Zobacz…/Zajrzyj…” odsyłają gościa do logowania | c | Na main `resources/views/pages/landing.blade.php:157,163,246,252` linkują do `posts.create`/`recipes.create`/`collections.index` (grupa `auth`). Landing renderuje się tylko gościom (`FeedController::landing()` zwraca `home()` dla zalogowanego), więc wariant „zalogowany” jest n/d. PR #1505 (unstable): linki do kotwic `help#dodawanie-zdjecia`, `help#kto-widzi`, `search?sekcja=przepisy`, usuwa „Zajrzyj do zeszytu” + `LandingPodgladNieOdsylaDoLogowaniaTest` i kontrola ujemna w alfa08. | Co dalej: scalić #1505; kryterium „test jako zalogowany” można uznać za n/d (w komentarzu zamykającym to zaznaczyć). Tekst „Zobacz przepisy innych” warto przepuścić przez przegląd głosu marki. |
| #1295 DemoSeeder wypisuje hasło z `KUKING_DEMO_HASLO` | c + e (brak etykiet) | Na main `database/seeders/DemoSeeder.php:717` — `info('...'.$this->hasloDemo())` bez rozróżnienia źródła. PR #1619 (unstable): hasło z env nigdy nie wypisywane, wylosowane tylko bez `--no-interaction`; `DemoSeederNieWypisujeHaslaTest` + kontrola ujemna w alfa08. | Co dalej: scalić #1619; po scaleniu przejrzeć jeden log joba CI z `migrate:fresh --seed` (kryterium 4 — ręczne). |
| #1296 Tablica dnia: kilka redakcyjnych dań jednego autora | c + e (brak etykiet) | Na main `app/Http/Controllers/Admin/DailyBoardController.php:82-104` waliduje tylko UUID/max 6, `DailyBoard::fromCuratedPicks()` (`app/Domain/Feed/DailyBoard.php` ~l.178-215) bez unikalności autora. PR #1549 (unstable, +903/-187, łączy #1027/#1329): walidacja w panelu z polskim komunikatem, `->unique('author_id')` przy odczycie zastanych picków, `TablicaDniaJednoDanieOdAutoraTest` (3 testy). | Co dalej: scalić #1549 (duży PR, refaktor panelu — ryzyko konfliktu z #1590/#1584/#1628 w `DailyBoard.php`). |
| #1297 Publiczny zeszyt ujawnia obcym liczbę prywatnych zapisów | c + d (decyzja produktowa) + e (brak etykiet) | Na main `app/Http/Controllers/CollectionController.php:316` liczy `niewidoczne` dla każdego widza, `resources/views/pages/collections/show.blade.php:87-99` je renderuje; `ZeszytNiedostepneZapisyTest` utrwala wyciek. PR #1590 (**dirty — konflikt**, zawiera w sobie cały #1584) liczy `niewidoczne` tylko właścicielowi + `LicznikiZeszytuDlaWidzaTest`, zmienia stary test. #1584 (unstable) tylko wspomina #1297, nie naprawia. | Co dalej: właściciel potwierdza decyzję „liczba tylko dla właściciela” (issue wprost wymaga świadomej decyzji; dopisać D-xxx do `docs/DECISIONS.md`), rozwiązać konflikt #1590 po scaleniu #1584. |
| #1300 Nagłówek wejść obiecuje Google i Facebooka przy jednym dostawcy | c + e (brak etykiet) | Na main `resources/views/components/wejscia-zewnetrzne.blade.php:62` stały `@props naglowek`, `resources/views/auth/register.blade.php:28` stały tekst. PR #1556 (unstable, łączy #1345/#1381): nagłówek składany z listy działających dostawców, prop `rodzaj`, `NaglowekWejscZewnetrznychTest`. | Co dalej: scalić #1556; sprawdzić, że test czyta DOM bloku, nie całe HTML (kryterium 3). |
| #1302 Tryb gotowania gubi Wake Lock przy zmianie kroku | c + d (test na urządzeniu) + e (brak etykiet) | Na main `resources/js/app.js:390+` włącza Wake Lock tylko po `change`, bez pamięci. PR #1587 (**clean**, łączy #1368/#1304): `utworzPamiecWyboru()` w sessionStorage kluczowanym slugiem przepisu, kasowanie przy „Zakończ gotowanie”/„Ugotowałem”, testy `wake-lock-gotowania.test.mjs`. | Co dalej: scalić #1587; kryterium „test na urządzeniu” wymaga ręcznej próby na telefonie po wdrożeniu. |
| #1304 Zdjęcia kroków bez opisu dla czytnika | c (PR częściowy) + e (brak etykiet; powiązane #769) | Na main `resources/views/pages/recipes/show.blade.php:586`, `cooking.blade.php:125` — `<x-photo>` bez `alt`; `resources/views/exports/recipe.blade.php:97` `alt="Krok N"`. PR #1587 dodaje fallback „Zdjęcie do kroku N” i przenosi `media.alt_text` na 3 powierzchnie + `ZdjecieKrokuOpisILadowanieTest`, ale **nie dodaje pola opisu w edytorze** (`szczegoly.blade.php`, `RecipeController::withStepPhotos()` bez zmian) — autor nie ma jak wpisać opisu, kryterium 1 niespełnialne z UI. | Co dalej: po scaleniu #1587 zostawić issue otwarte (lub wydzielić) na opcjonalne pole opisu zdjęcia kroku; nie zamykać automatycznie przez „Closes” w #1587. |
| #1305 Odwołanie bez potwierdzenia/alarmu przy awarii | c + e (brak etykiet) | Na main `app/Domain/Moderation/Actions/FileAppeal.php:76` i `FileReporterAppeal.php:85` — `Appeal::create()` bez transakcji. PR #1579 (unstable, łączy #1384): `DB::transaction` obejmuje odwołanie, powiadomienia adminów i (kolejka `database`, w tej samej transakcji = trwałe zlecenie) potwierdzenie mailowe; audyt wyniesiony za transakcję jako `recordBezWywracania`; `ZlozenieOdwolaniaJestAtomoweTest` + kontrole ujemne w alfa08. | Co dalej: scalić #1579; zwrócić uwagę, że atomowość zależy od `QUEUE_CONNECTION=database` — przy zmianie kolejki gwarancja znika (warto test/komentarz). |
| #1306 Weryfikacja pochodzenia żądań i token krawędzi | c + d (Railway/Cloudflare) + e (brak etykiet) | Na main brak implementacji `X-Kuking-Edge-Token` (tylko komentarze w `app/Http/Middleware/NormalizeForwardedFor.php:76`, `app/Support/ZaufaneHosty.php:91`); `.railway/railway.ts:288` `KUKING_ZAUFANE_PRZESKOKI=1`. PR #1626 (unstable, tylko „Refs”, nie Closes): `TokenKrawedzi` z trybami wyłączony/obserwacja/egzekwowanie, rotacja, wyjątek `/health`/`/up`, `TokenKrawedziTest` (9 przypadków). | Co dalej: scalić #1626; właściciel inwentaryzuje domeny Railway/Cloudflare (kryterium 1), ustawia `KUKING_EDGE_TOKEN`, regułę Transform w Cloudflare, mierzy w trybie obserwacji, dopiero potem egzekwowanie. |
| #1308 Równoległe zmiany kolejności promowanych tagów | c + e (brak etykiet, P2 tylko w tytule) | Na main `app/Http/Controllers/Admin/TagPromotionController.php:98` `max(position)+1` bez blokady, `przesun()` (~l.185) liczy sąsiada przed transakcją. PR #1593 (unstable): `PromowaneTagi` z `pg_advisory_xact_lock`, przenumerowanie, spójne blokady w `MergeTags`, test dwóch połączeń `KolejnoscPromowanychTagowRownolegleTest` + `KolejnoscPromowanychTagowTest`. | Co dalej: scalić #1593; potwierdzić, że `Tag::scopePromowane` ma drugie kryterium sortowania (historyczne remisy). |
| #1309 Raporty materializują UUID zamkniętych kont | c + e (brak etykiet, P2 tylko w tytule) | Na main `app/Domain/Analytics/CookEligibility.php:60,65` — `pluck('id')` do PHP. Kod naprawy jest w PR #1593 (`NOT EXISTS`, `RaportyNieNiosaHistoriiZamknietychKontTest`), a pomiar EXPLAIN/pamięci w PR #1628 (`docs/pomiary/1309-...md`, `scripts/pomiar-1309*`); żaden nie ma „Closes #1309”. | Co dalej: po scaleniu #1593 i #1628 zamknąć ręcznie z linkiem do pomiaru (wg #1628 main pada przy ~16 tys. zamkniętych kont na limicie 65 535 parametrów — argument za pilnym scaleniem #1593). |
| #1310 Tablica na powitalnej pobiera wariant 960 px | c + d (pomiar w przeglądarce) + e (opis częściowo nieaktualny, brak etykiet) | Na main `resources/views/components/kuking-board/posts.blade.php:36` — `url($naPowitalnej ? 'feed' : 'thumb')` bez `srcset`. PR #1596 (unstable, łączy #1326): `Media::srcset()` + `sizes`, test `MiniaturyWybierajaWariantZdjeciaTest`; autor PR stwierdza, że założenie o 120 px na powitalnej jest nieaktualne (tam zdjęcie ma szerokość karty). Pomiar bajtów 1×/2×/3× nie wykonany. | Co dalej: scalić #1596; ręczny pomiar DevTools (zimny cache) wymagany przez kryterium 1; poprawić opis issue co do 120 px. |
| #1313 IaC: PR do staging może trafić na produkcję | b + e (opis nieaktualny, brak etykiet); PR #1624 w toku, dirty | Na main 93366c50 (#595/#597) usunął `closed` z `on.pull_request` i apply chodzi już tylko z `workflow_dispatch` na `refs/heads/main` (.github/workflows/railway-iac.yml:101-102, 194-198) — połowa „apply po merge'u do staging” nie istnieje. Plan nadal bez `branches: [main]` ani `base.ref == 'main'` (railway-iac.yml:142-145), więc PR do staging dostałby `RAILWAY_TOKEN_PRODUCTION` w planie. PR #1624 (dirty) zbudowany na starej wersji z `closed`/apply-po-merge'u; ma test `IacProdukcjaTylkoZPrDoMainTest` i kontrolę ujemną. | Co dalej: zaktualizować opis (apply już ręczny z main), rebase #1624 na main i zostawić tylko filtr gałęzi dla planu + bramkę w kroku + test; kryterium „apply po merge'u” przepisać na workflow_dispatch. |
| #1315 Awans roli a sesje sprzed awansu | b (kod zrobiony na main, brakuje testów z kryteriów); PR #1489 w toku, unstable, pokrywa resztę | c6dd5b8d (merge 45a5afd9): `ChangeUserRole` woła `invalidateSessions()` w tej samej transakcji co `promoteTo()` i audyt (app/Domain/Users/Actions/ChangeUserRole.php:52-63), więc atomowość i „awaria audytu nie odwołuje” spełnione konstrukcyjnie; SECURITY_BASELINE.md:112 opisuje. Test na main (`NadanieRoliTest::test_zmiana_roli_w_obie_strony_...`) wstawia wiersze `sessions` ręcznie — brak dwóch klientów z cookie/HTTP na `/admin/**`, brak testu „bez 2FA → ekran konfiguracji” i testu, że awaria audytu zostawia sesje. PR #1489 dodaje `AwansRoliWymagaNowejSesjiTest` (HTTP, database driver, remember cookie, 2FA) i przypadek w `AtomowaZmianaRoliTest`. | Co dalej: naprawić checki #1489 i scalić; wtedy zamknąć #1315 komentarzem: „Kod w c6dd5b8d (#1425), testy HTTP z dwoma klientami i cookie w #1489.” |
| #1316 Autozapis opublikowanego przepisu bez wersji | c + e (brak etykiet); PR #1604 w toku, clean, pokrywa | Na main snapshot i audyt nadal tylko pod `if ($publish)` (app/Domain/Recipes/Actions/PublishRecipe.php:467-476), a `bedziePubliczny` utrzymuje status published (PublishRecipe.php:159). PR #1604 (clean) zmienia `SnapshotRecipeVersion`, kreator, DATABASE.md, dodaje `PoprawkaOpublikowanegoPrzepisuMaWersjeTest`; łączy z niezwiązanym #1082 (menu karty). | Co dalej: review i merge #1604; sprawdzić, czy opisano regułę sklejania autozapisów (kryterium 1). |
| #1317 Ukryta odpowiedź a usunięcie korzenia | c + e (ścieżka w opisie nieaktualna, brak etykiet); PR #1554 w toku, unstable | Logika przeniesiona z `CommentController::destroy()` do app/Domain/Comments/Actions/DeleteComment.php:29-32 — nadal `replies()->exists()`, a `Comment::replies()` filtruje `status = published` (app/Models/Comment.php:139-143). `RestoreContent` nie sprawdza korzenia. PR #1554 zmienia DeleteComment + RestoreContent, test `KorzenZUkrytaOdpowiedziaTest`. | Co dalej: poprawić ścieżkę w opisie (DeleteComment.php), zielone checki #1554, sprawdzić pokrycie wpis/przepis/wykonanie. |
| #1318 Własny wpis followers znika ze Startu | c + e (brak etykiet); PR #1567 w toku, clean, pokrywa; #1561 nakłada się | `FollowingFeed::isEmptyFor()` nadal pyta tylko `following()` (app/Domain/Feed/FollowingFeed.php:115-130), wybór źródła w FeedController.php:112-113. PR #1567 (clean) dodaje własne wpisy do feedu zastępczego + test `WlasneWpisyWFeedzieZastepczymTest`; zawiera w sobie commit 0b372d26 z PR #1561 (#1338, ten sam `FeedTagowTylkoOpublikowaneTest`). | Co dalej: scalić #1561 najpierw albo zamknąć go na rzecz #1567 (#1567 jest nadzbiorem). |
| #1319 Licznik zeszytu i „Ostatnio zapisane” a niedostępny przepis | c + e (brak etykiet); PR #1590 w toku, dirty, nadbudowany na #1584 | `index()` liczy `posts_count` tylko `widoczneDla` + autor wpisu (app/Http/Controllers/CollectionController.php:63-64), `ostatnioZapisane()` bez `zWidocznymPrzepisem`/`recipe.author` (CollectionController.php:143-146), podczas gdy `show()` je ma (:209, :227). PR #1590 (dirty, +1000) zawiera cały PR #1584 (#1377/#1378) + test `LicznikiZeszytuDlaWidzaTest`; łączy też #1297. | Co dalej: scalić #1584, potem rebase #1590 i rozwiązać konflikt. Powiązane #774 (inny aspekt licznika). |
| #1323 Zgłoszenie prawne: ponowienie nie naprawia potwierdzenia | c + e (brak etykiet); PR #1576 w toku, unstable, pokrywa | Po konflikcie unikalności metoda wraca `$istniejace` od razu (app/Domain/Moderation/Actions/ZglosNielegalnaTresc.php:110-127), notify i `receipt_sent_at` poza transakcją (:142-146). PR #1576 dodaje dokańczanie potwierdzenia + `ZgloszeniePrawnePonowienieTest` (awaria zlecenia, awaria znacznika, dwa kliknięcia, brak e-maila). | Co dalej: zielone checki #1576, sprawdzić kontrolę ujemną w skrypcie kontroli negatywnych. |
| #1324 Sygnały produktowe po wymazaniu konta | c + e (brak etykiet: „prywatność”) + d (zmiana polityki prywatności) ; PR #1617 w toku, unstable | `EraseAccountData` nie dotyka `product_signals` (brak trafień grep w app/Domain/Users/Actions/EraseAccountData.php). PR #1617 odpina `user_id`, zmienia `ZapiszSygnal` (współbieżność), DATABASE.md, `resources/legal/polityka-prywatnosci.md` i dodaje `WymazanieKontaOdpinaSygnalyProduktoweTest`. | Co dalej: właściciel akceptuje zmianę tekstu polityki prywatności (#8 — weryfikacja prawnika), potem merge #1617. |
| #1325 Zgłoszenie prawne: inny adres = to samo wysłanie | c + e (brak etykiet); PR #1576 w toku, unstable, pokrywa | `zgloszenieZTegoWyslania()` nie porównuje `notifier_name`/`notifier_email` (ZglosNielegalnaTresc.php:174-192). PR #1576 (wspólny z #1323) ma test z data providerem dla obu pól `test_ten_sam_klucz_z_innymi_danymi_kontaktowymi_to_nowa_sprawa`. | Co dalej: jak #1323 — jeden merge zamknie oba. |
| #1326 Katalog tagów zawsze ładuje wariant feed | c + e (brak etykiet); PR #1596 w toku, unstable, częściowo pokrywa | resources/views/components/tag-directory-card.blade.php:5 — `src="{{ $photo->url('feed') }}"` bez `srcset`/`sizes`. PR #1596 wydziela generator srcset do `Media`, używa go w karcie i tablicy (#1310), test `MiniaturyWybierajaWariantZdjeciaTest`. Kryterium 4 (pomiar przeglądarki bajtów przy DPR 1/2) nie widać w diffie. | Co dalej: merge #1596; kryterium pomiaru przeglądarki wykonać ręcznie albo jawnie odpuścić w komentarzu. |
| #1329 Wybór tablicy/kolażu bez audytu | c + e (numery linii w opisie nieaktualne, brak etykiet); PR #1549 w toku, unstable, pokrywa | `DailyBoardController::update()` — transakcja :133, audyt po niej :165; `destroy()` (:185) bez `daily_board.cleared`; `HeroKolazController` audyt poza transakcją :144, :164. PR #1549 wydziela `ZapiszTabliceDnia`/`ZapiszKolaz`, ARCHITECTURE.md, test `WyborRedakcjiAudytWTransakcjiTest`; łączy #1027 i #1296. | Co dalej: zielone checki #1549 i merge (duży PR, +903/-187 — review). |
| #1330 Test harmonogramu digestu nie pilnuje ciszy nocnej | c + e (brak etykiet); PR #1580 w toku, unstable, pokrywa | routes/console.php:431-433 `dailyAt('08:30')`, ale żaden test nie czyta godziny zadania (ObietnicaTygodniowegoMailaTest wspomina 08:30 tylko w komentarzu :24). PR #1580 dodaje `HarmonogramDigestuSzanujeCiszyNocnejTest`. | Co dalej: merge #1580 (wspólny z #1331, #1333). |
| #1331 Nieudane sprzątanie paczek bez alarmu | c + e (brak etykiet, „prywatność”); PR #1580 w toku, unstable, pokrywa | app/Console/Commands/CleanUpDataExports.php:104,120 — zawsze `SUCCESS` mimo `$nieudane`; na main brak czujki zaległych paczek. PR #1580 dodaje `AlarmZaleglychPaczekDanych`, komendę `SprawdzSprzatanieEksportow`, wpis w MONITORING_BLEDOW.md i test `SprzatanieEksportowDajeAlarmTest`. | Co dalej: merge #1580; po wdrożeniu właściciel sprawdza, że `blad_webhook` jest ustawiony na produkcji. |
| #1333 Przypomnienie o terminach odwołań bez limitu dobowego | c + e (brak etykiet); PR #1580 w toku, unstable, pokrywa | app/Console/Commands/PilnujTerminowOdwolan.php:83 wysyła `notify()` przy każdym wywołaniu, bez rezerwacji; harmonogram routes/console.php:389 (07:10). PR #1580 dodaje tabelę `przypomnienia_dobowe` (migracja z `down()` = drop, DATABASE.md), `PrzypomnienieDobowe` i test `TerminOdwolaniaJedenListNaDobeTest`. | Co dalej: merge #1580; sprawdzić test równoległy (kryterium atomowości) i kontrolę ujemną. |
| #1334 „Dopisz przepis” do własnego wpisu ze zdjęciem (propozycja) | d + e (brak etykiet; poza MVP) | Brak kodu i PR; `docs/ROADMAP.md` §3 Post / §5 Recipes nie przewidują konwersji wpisu w przepis, a V1 gate (l. 117) odkłada nowe funkcje do czasu danych WAC/D30. Samo issue mówi „nie jest wymagane do MVP” i uzależnia od walidacji w #29. | Co dalej: oznaczyć jako propozycję po alfie (P3, `typ: funkcja`, `obszar: produkt`), zaparkować do wyników #29 / closed alpha gate; nie implementować teraz. |
| #1335 Ekran potwierdzenia adresu obiecuje list przy niedziałającej poczcie | c + e (brak etykiet) | Na main `resources/views/auth/verify-email.blade.php:27` bezwarunkowo „Wysłaliśmy wiadomość…”, `EmailVerificationController.php:85` bez `Poczta::dziala()`. PR #1589 (clean, mergeable) dodaje `pocztaDziala` do widoku, ukrywa przycisk i „Spam”, wczesny `return` w `resend()` przed pulą; testy `PotwierdzenieAdresuBezPocztyTest` (log/array + działający transport). | Co dalej: zrecenzować i scalić #1589 (pokrywa kryteria; łączy też #1084). |
| #1337 Edycja komentarza po odpowiedzi zmienia sens rozmowy | c + e (brak etykiet) | Na main `app/Policies/CommentPolicy.php:92-95` — brak warunku `replies()`, komentarz l. 84-86 nieprawdziwy. PR #1656 (DRAFT, dirty = konflikt) dodaje bramkę w Policy, `EditComment` z rewalidacją pod blokadą, widok odmowy 409, test Feature + test dwóch połączeń. | Co dalej: rozwiązać konflikt #1656, wyjść z draftu, uruchomić CI; merytorycznie pokrywa kryteria 1–4. |
| #1338 Feed tagów pokazuje własny ukryty wpis | c + e (brak etykiet) | Na main `app/Domain/Feed/TagFeed.php:56` i `:128` — `widoczneDla()` bez `published()`. PR #1561 (unstable) dodaje `->published()` w obu zapytaniach + `FeedTagowTylkoOpublikowaneTest` (hidden/draft, wybór źródła, kontrola dodatnia). | Co dalej: dokończyć CI i scalić #1561; uwaga na sąsiedni hunk w #1540 (TagFeed::maTresci). |
| #1339 Zmiana nazwy zeszytu → 500 przy równoczesnej kolizji | c + e (brak etykiet) | Na main `app/Http/Controllers/CollectionController.php:398` `$collection->update($data)` bez `try/catch`. PR #1553 (unstable) owija w `DB::transaction` + `catch UniqueConstraintViolationException` z błędem `name`; test `ZmianaNazwyZeszytuKolizjaTest` (okno wyścigu, wolna nazwa). Łapie każdą UniqueConstraint (issue prosiło o rozpoznanie indeksu) — dziś jedyny unikalny indeks tabeli, więc akceptowalne. | Co dalej: scalić #1553 po zielonym CI; ew. doprecyzować rozpoznanie nazwy ograniczenia. |
| #1341 Lista obserwujących przewija się w bok przy dużej czcionce | c + e (brak etykiet) | Na main `resources/css/app.css:6210` `min-width: 14rem`. PR #1583 (unstable) zmienia na `min(14rem, 100%)` + `min-width:0; overflow-wrap:anywhere`, dodaje pomiar Playwright `scripts/lista-osob-szerokosc.mjs` z kontrolą ujemną i wpina go w CI. | Co dalej: scalić #1583 (pokrywa kryteria; łączy #1366). |
| #1344 Callback Facebooka rzutuje tablicowe signed_request | c + e (brak etykiet) | Na main `app/Http/Controllers/Auth/FacebookDeauthorizeController.php:64` `(string) $request->input('signed_request')`. PR #1552 (unstable) sprawdza typ przed rzutowaniem, test `test_tablica_zamiast_podpisu_konczy_sie_spokojnym_czterysta`; ten sam PR niesie #1025 z migracją (+626 linii, 12 plików). | Co dalej: zrecenzować #1552 (duży zakres, migracja — rollback i DATABASE.md są w PR); ewentualnie wydzielić #1344 do osobnego małego PR. |
| #1345 Panel kontaktu pokazuje zbyt wczesny termin usunięcia | c + e (brak etykiet) | Na main `app/Domain/Compliance/PrzedawnioneWiadomosciDoOperatora.php:123` `addMonthsNoOverflow` + nieprawdziwy komentarz l. 106 („jedyny wyjątek 29 lutego”). PR #1556 (unstable) koryguje termin progiem `progRetencji`, test `RetencjaTerminuNaKoncuMiesiacaTest` uruchamia `posprzataj()` minutę przed/po dla 31.01+1, +3, 29.02+12 i zwykłej daty. | Co dalej: scalić #1556 (łączy #1300, #1381); brak przypadku domyślnych 12 mies. dla 31.01 — drobne. |
| #1346 Równoległe żądania usunięcia konta → dwie sprawy RODO w toku | b + c + e (brak etykiet) | Na main wyścig aplikacyjny domknięty przez #980: `User::markForDeletion()` (`app/Models/User.php:1217-1220`) czyta świeży wiersz pod blokadą i rzuca `BladDlaCzlowieka`, więc druga transakcja się wycofuje. Brakuje gwarancji bazy — `2026_09_21_100000_utworz_potwierdzenia_zadan_rodo.php` bez unikalnego indeksu częściowego. PR #1609 (unstable) dodaje indeks `WHERE wynik='w_toku'` z odmawiającym rollbackiem, DATABASE.md, test dwóch połączeń. | Co dalej: scalić #1609; w opisie issue zaznaczyć, że część „bez blokady” jest nieaktualna po #980. |
| #1347 Żądanie usunięcia konta przyjęte mimo błędu audytu | c + e (brak etykiet) | Na main `DataSettingsController.php:420-424` transakcja kończy się przed `AuditLogEntry::record()` (l. 443). PR #1609 (unstable) przenosi zapis do akcji `PrzyjmijZadanieUsunieciaKonta` w jednej transakcji; test `test_awaria_dziennika_cofa_zadanie_a_ponowienie_daje_jeden_komplet` + kontrola dodatnia. | Co dalej: scalić #1609 (razem z #1346, #1364). |
| #1349 Zdjęcie oznaczane `rejected` przed retry | c + e (brak etykiet) | Na main `app/Jobs/ProcessUploadedImage.php:244-261` — `catch` zapisuje `STATUS_REJECTED` przy każdej próbie (`$tries = 3`, l. 50). PR #1550 (unstable) dodaje `bedzieKolejnaProba()` (attempts < tries), zdjęcie zostaje `processing`; test `ZdjecieCzekaNaPonowienieWPrzetwarzaniuTest` (pierwsza porażka, udany retry, wyczerpanie, timeout, kasowanie). | Co dalej: scalić #1550 (pokrywa kryteria). |
| #1350 Scalony tag przekierowuje 302 zamiast 301 | c + e (brak etykiet) | Na main `app/Http/Controllers/TagController.php:121` `redirect()->route(...)` bez statusu. PR #1553 (unstable) dodaje `status: 301` i `assertStatus(301)` w `TagiObserwowanieTest`. Brak testu HTTP łańcucha dwóch scaleń (kryterium 3) — `MergeTags` spłaszcza łańcuch, test tylko w `ScalanieTagowTest:257` na poziomie danych. | Co dalej: scalić #1553; opcjonalnie dopisać test 301 po podwójnym scaleniu. |
| #1351 Powiadomienie o odwołaniu widoczne po odebraniu roli admina | b + c + e (brak etykiet) | Na main commit 282d6ec2 (merge 04bb3baf): `app/Models/Notification.php:666-667` chowa `appeal.filed` przed nie-adminem, `NotificationController::open()` ta sama granica; test `ZawieszonyModeratorBezUprawnienTest::test_zawiadomienie_o_odwolaniu_znika_po_odebraniu_roli_i_wraca_p…`. Nie zrobione: ta sama zasada dla `post.first` (kryterium 3) — robi to PR #1565 (unstable) z 2 testami. | Co dalej: główne kryterium spełnione na main; zamknąć po scaleniu #1565 (post.first). Komentarz: „Rdzeń naprawiony w 282d6ec2 (#1420); `post.first` domyka #1565.” |
| #1352 WyslijOdpowiedz nie sprawdza prawa do listu | c + e (brak etykiet) | Na main `app/Domain/Contact/Actions/WyslijOdpowiedz.php:37` — `handle()` bez Gate. PR #1588 (unstable) dodaje `Gate::forUser($moderator)->authorize('reply', $wiadomosc)` przed zapisem; test `WyslijOdpowiedzUprawnieniaTest` (zwykły/zdegradowany odmawia, czynny moderator wysyła). | Co dalej: scalić #1588 (pokrywa kryteria). |
| #1354 Log awarii oceny awatara bez UUID zdjęcia | c + e (tytuł nieaktualny, brak etykiet) | Ocena awatara wyłączona (D-240): `app/Jobs/PrzeanalizujAwatar.php:39-42` ma pusty `handle()`, więc część o awatarze jest nieaktualna. Nadal otwarte: `app/Moderacja/OcenaModelem.php:223-225` loguje `image_preparation` bez `media_id` (dotyczy zdjęć wpisów oceniane przez `PrzeanalizujTresc`). PR #1597 (unstable) dodaje `media_id` z testem i kontrolą „tylko zepsute zdjęcie” w `ModeracjaBezTresciWyjatkowTest`; PR #1588 tylko wspomina (Refs), nie zmienia tej części. | Co dalej: zmienić tytuł na „Log przygotowania zdjęcia do oceny modelem nie ma UUID zdjęcia”, usunąć z opisu część o `PrzeanalizujAwatar` i scalić #1597 po zielonym CI. |
| #1356 Preflight akceptuje DATABASE_URL | c + e (brak etykiet) | `docker/entrypoint.sh:96` nadal sprawdza `${DB_URL}${DATABASE_URL}`, a `config/database.php:92` (pgsql) czyta wyłącznie `DB_URL`. PR #1591 (unstable) wprowadza `sprawdz_konfiguracje_bazy()` (DB_URL albo DB_HOST+DB_DATABASE+DB_USERNAME, bez mapowania i bez wypisywania URL), dodaje test `tests/skrypty/preflight-bazy.sh` i wpis w `scripts/check.sh`. Kryteria są pokryte, a kwestia `config:cache` jest wyjaśniona tylko w komentarzu. | Co dalej: scalić #1591 po zielonym CI. |
| #1357 zend.exception_ignore_args w obrazie | c + e (brak etykiet) | Na main w `docker/php.ini` nie ma dyrektywy, a `#[SensitiveParameter]` występuje tylko raz (`app/Models/User.php:899`). PR #1591 dodaje `zend.exception_ignore_args=On` i test `tests/skrypty/php-ini-slady.sh` z kontrolą dodatnią. Test uruchamia PHP z samym `docker/php.ini` (`-n -c`), a nie zbudowany obraz, i PR nie dodaje `#[SensitiveParameter]` (w issue ten punkt był opcjonalny). | Co dalej: scalić #1591. Po wdrożeniu sprawdzić w kontenerze `php -i \| grep exception_ignore_args` (web, worker i scheduler). |
| #1358 Hasło zapisywane przed ZamekKonta | c + e (brak etykiet) | Na main zapis hasła stoi poza blokadą: `SecuritySettingsController.php:67` (zapis), `:73` (sesje), `:85` (anulowanie) oraz `PasswordResetController.php:202/213/248`. PR #1484 (unstable, Closes) wprowadza `UstawNoweHaslo`: hasło, sesje, token resetu, anulowanie i audyt w jednej transakcji `ZamekKonta`. Dołącza test na dwóch połączeniach `tests/Dwa/HasloPodBlokadaKontaTest.php` i `HasloIZmianaAdresuWJednejTransakcjiTest`. PR #1500 (#1046) tylko wspomina #1358 i nie rusza tych kontrolerów. | Co dalej: scalić #1484. Jeśli #1500 wejdzie wcześniej lub później, puścić ponownie testy Dwa, bo #1500 zmienia `User::invalidateSessions()`, które #1484 woła wewnątrz transakcji. |
| #1359 Moderator czyta szkice przepisów | c + e (brak etykiet) | `app/Policies/RecipePolicy.php:15-16` wpuszcza moderatora do każdego nieopublikowanego przepisu, także do `draft`. PR #1551 (unstable) otwiera moderatorowi tylko `hidden` i `removed`, a szkic zostawia autorowi. Dodaje tabelaryczny `ModeratorWidziTylkoSprawyModeracyjneTest`, poprawia testy utrwalające stare zachowanie (`ZgloszenieNieUjawniaPrywatnejTresciTest`) i dopisuje mutacje. | Co dalej: scalić #1551. |
| #1360 Moderator ma dostęp do każdego zdjęcia | c + e (brak etykiet) | `app/Domain/Media/DostepDoZdjecia.php:256` (`wlascicielLubModerator`) daje każdemu moderatorowi skrót przed sprawdzeniem rodziców. PR #1551 zostawia skrót tylko właścicielowi i dodaje wąski wyjątek `celemZgloszeniaDlaObslugi()`: zdjęcie będące celem zgłoszenia albo zdjęcie wpisu public/followers z otwartym zgłoszeniem, dla moderatora z 2FA. Testuje `moze()` i `rozstrzygnij()`. | Co dalej: scalić #1551. W przeglądzie sprawdzić, że wyjątek dla `target_type=media` nie filtruje po statusie zgłoszenia, więc obejmuje także zgłoszenia zamknięte. |
| #1364 Zawieszone konto nie może złożyć żądania usunięcia | c + e (połowa opisu nieaktualna, brak etykiet) | Na main w `app/Http/Middleware/EnsureAccountIsActive.php:70-82` nadal brakuje `settings.data.delete`. Drugi zarzut jest nieaktualny: `User::markForDeletion()` już zeruje `status_expires_at` (`app/Models/User.php:1229`) i przenosi karę do `punishment_status`. PR #1609 (unstable) dodaje trasę do allowlisty i testy HTTP dla zawieszenia bez terminu i czasowego (przez CHECK) oraz dla błędnego hasła; łączy to z #1346/#1347. | Co dalej: dopisać w issue, że reset terminu już jest na main, i scalić #1609. |
| #1365 Eksport ready bez metadanych | c + d + e (brak etykiet) | `app/Models/DataExport.php:117-122` (`isDownloadable()`) sprawdza tylko status i termin, a CHECK pilnuje tylko nazwy stanu. PR #1577 (unstable) dodaje migrację z audytem istniejących wierszy, która odmawia przy niekompletnych, oraz CHECK `data_exports_ready_complete_check`. Poprawia też `isDownloadable()`, fixture i `docs/DATABASE.md`, dodaje rollback i test `GotowyEksportMaKompletMetadanychTest`. Świadomie nie wymaga relacji `expires_at`/`completed_at` (uzasadnione przez `EraseAccountData`). | Co dalej: scalić #1577. Przed wdrożeniem właściciel uruchamia na produkcji SELECT z komunikatu migracji, bo migracja odmówi przy niekompletnych wierszach `ready`. |
| #1366 Pełna lista zablokowanych osób | c + e (brak etykiet) | `app/Http/Controllers/Settings/PrivacySettingsController.php:19` wywołuje `blocking()->...->get()` bez limitu. PR #1583 (unstable) używa `cursorPaginate(20)` po `blocks.blocked_id` z kotwicą `#zablokowane`. Dodaje 5 testów `UstawieniaPrywatnosciStronicujaBlokadyTest`: limit modeli, dalsza strona, odblokowanie tam oraz stany pustej listy i końca wyników. | Co dalej: scalić #1583, uważając na konflikt w `.github/workflows/ci.yml`. |
| #1368 Zdjęcie bieżącego kroku ładowane leniwie | c + e (brak etykiet, nieaktualny numer linii) | `resources/views/pages/recipes/cooking.blade.php:125` (w issue podano :111) wywołuje `<x-photo>` bez `priority`, więc obraz dostaje `loading="lazy"`. PR #1587 (clean) dodaje do `photo.blade.php` prop `leniwie` i w trybie gotowania ustawia `:leniwie="false"` bez `fetchpriority`. Test `ZdjecieKrokuOpisILadowanieTest` jest dołączony. Kryterium 1 (pomiar LCP przy 320 px, na tablecie i na desktopie) nie zostało wykonane; wybrano bezwarunkowy brak lazy. | Co dalej: scalić #1587 (jest zielony). Pomiar LCP można zostawić jako uwagę, bo reguła bez `fetchpriority` jest zachowawcza. |
| #1370 HowToStep.image w JSON-LD | c + e (brak etykiet) | `resources/views/pages/recipes/show.blade.php:154`: `HowToStep` ma tylko `position` i `text`. PR #1643 (unstable) dodaje `image` pod warunkiem `maWariantDoPokazania('feed')` z bezwzględnym URL-em. Test `ZdjeciaKrokowWJsonLdPrzepisuTest` sprawdza URL zgodny z `src`, różny od zdjęcia głównego, brak `name` oraz brak `Recipe` dla szkicu i treści niepublicznej. PR łączy też #1008 i #1033 (okruszki, WebSite). | Co dalej: scalić #1643. |
| #1371 post.first prowadzi do kolejki | c + e (brak etykiet) | `app/Models/Notification.php:344`: `TYPE_FIRST_POST => route('admin.unanswered')`. PR #1586 (unstable) wprowadza `pierwszyWpis()` z autoryzacją `Gate::forUser(...)->allows('view')` i fallbackiem do kolejki z komunikatem; test `PowiadomienieOPierwszymWpisieProwadziDoWpisuTest` jest dołączony. `adresDocelowy()` wykonuje teraz zapytanie na każde powiadomienie `post.first` na liście (możliwe N+1, małe). | Co dalej: scalić #1586, skoordynować z #1565 i #1584 (wszystkie trzy zmieniają `Notification.php`). |
| #1377 Wpis z własną treścią znika z list | c + e (brak etykiet) | `app/Models/Post.php:276-284` (`scopeZWidocznymPrzepisem`) odrzuca cały wpis z niewidocznym przepisem. PR #1584 (unstable) dodaje `zWidocznymPrzepisemAlboWlasnaTrescia()` (body `\S` albo `post_media`, zgodnie z `czyJestZapowiedziaPrzepisu()`) i `ukryjNiedostepnePrzepisy()` na kartach. Zmiana obejmuje odkrywanie, feed obserwowanych, tag, profil, tablicę i sąsiedni wpis; test `ListyWpisuZWlasnaTresciaTest` ma 8 scenariuszy. PR #1590 (dirty) zawiera #1584 i tylko wspomina #1377. | Co dalej: scalić #1584 przed #1590. |
| #1378 Powiadomienie o odpowiedzi po ukryciu korzenia | c + e (brak etykiet) | `app/Models/Notification.php:613` (`scopeVisibleTo`) nie sprawdza widoczności `pc.parent_id`. PR #1584 dodaje `korzenWatkuWidoczny()`: ten sam cel, status published, brak soft delete, autor nieukryty i brak blokady w obie strony. Test `PowiadomienieOdpowiedziZaNiewidocznymKorzeniemTest` obejmuje blokadę i odblokowanie, moderację, bana i kontrolę dodatnią. Filtr ręcznie powiela `Comment::scopeWidoczneDla`, więc może się z nim rozjechać. | Co dalej: scalić #1584. Sprawdzić, czy licznik nieprzeczytanych i eksport używają tego samego `visibleTo()` (kryterium 4). |
| #1380 Archiwum: pusty rok udaje pusty profil | c + e (brak etykiet) | main: `resources/views/pages/profile/show.blade.php:306-307` przy `posts->count()===0` pokazuje „Twoje archiwum jest jeszcze puste”, nawigacja `lata-archiwum` (:329) w gałęzi `else`; brak commitów z #1380. PR #1563 (5ce045f5, unstable, +116/-27): osobny stan „Nie ma wpisów z R roku” + „Pokaż całe archiwum”, 2 nowe testy HTTP w `WspomnieniaTest` (własny/cudzy, naprawdę puste). | Co dalej: dopilnować zielonego CI #1563 i scalić; pokrywa kryteria. |
| #1381 Zgłoszenie: limit opisu dopiero po błędzie | c + e (brak etykiet) | main: `resources/views/pages/report.blade.php:38` `x-field details` bez `licznikZnakow`/informacji o limicie; `ReportController.php:66,73` `max:2000`. PR #1556 (8d400853, unstable) łączy 3 issues (#1345, #1300, #1381); dla #1381 +4 linie widoku i `LimitDopiskuZgloszeniaWidocznyTest` (aria-describedby, 2000/2001 z polskimi znakami). | Co dalej: zielone CI #1556; rozważyć, czy nie brakuje przypadku emoji (kryterium 3). |
| #1384 Wyjęcie ze wszystkich zeszytów nieatomowe | c + e (brak etykiet) | main: `app/Domain/Collections/Actions/SaveRecipeToCollection.php:126-156` i `SavePostToCollection.php:76-100` — pętla `detach()` bez `DB::transaction`; `CollectionController.php:508,623` bez transakcji. PR #1579 (23630d79, unstable) owija `remove()` w `DB::transaction`, test `WyjecieZeWszystkichZeszytowJestAtomoweTest` z awarią przy 2. odpięciu + kontrola ujemna w `kontrole-negatywne-alfa08.py`; łączy z #1305. | Co dalej: zielone CI #1579 i scalenie; pokrywa kryteria. |
| #1385 Powiadomienie o komentarzu pod własnym wykonaniem znika po ukryciu przepisu | c + d + e (brak etykiet) | main: `app/Models/Notification.php:825-848` `wierszWykonaniaWidoczny()` dopuszcza właściciela tylko przy `ce.recipe_id IS NULL`. PR #1566 (e8c8b9f4, clean) zawiera w sobie całe #1503 (D-259) i test `PowiadomienieOKomentarzuPodWlasnymWykonaniemTest`; ALE wg D-259 powiadomienie zostaje także przy blokadzie z autorem przepisu — sprzeczne z kryterium 2 issue („blokada kucharza z autorem przepisu nadal odcina dostęp”). | Co dalej: najpierw decyzja właściciela o D-259 (#1503); jeśli przyjęta — poprawić kryterium 2 w issue i scalić #1566 po #1503; jeśli odrzucona — przebudować #1566 bez D-259. |
| #1387 Refaktor kreatora Livewire (Form Objects) | c (wieloetapowe) | main: `resources/views/components/recipe-wizard.blade.php` ma 1646 linii, brak `app/Support/KreatorPrzepisu/`. PR #1633 (45006355, unstable, bez „Closes” — poprawnie) = krok 1: `KrokOPrzepisie` (reguły + 23 komunikaty kroku 1) + `tests/Unit/KrokOPrzepisieKreatoraTest`. Nie ruszone: składniki/kroki, test kontraktowy z formularzem bez JS, kontrola ujemna `#[Locked]`. | Co dalej: scalić #1633 po zielonym CI; zostawić issue otwarte, dopisać checklistę etapów (1 zrobiony w #1633; 2 składniki/kroki; 3 test kontraktowy; 4 markup). Uwaga na styk z #1604/#1594/#1543/#1526 (ten sam plik). |
| #1389 Preview: sam URL deploymentu = gotowość | c + e (brak etykiet) | main: `.github/workflows/preview.yml:164-185` bierze `payload.web_url`/`environment_url` i ustawia `found=true` bez odczytu statusów deploymentu. PR #1595 (75ebef8e, unstable) wspólny z #1390: `scripts/czekaj-na-preview.sh` czeka na `success`, porażka/limit = czerwony job, `deployments: read`, test na atrapie `gh` (`tests/skrypty/kontrola-czekania-preview.sh`, `PreviewIIacNieZgadujaStanuTest`). | Co dalej: naprawić CI #1595 (unstable) i scalić; pokrywa kryteria. |
| #1390 Railway IaC nie przekazuje KUKING_WAIT_FOR_CI | b + c + e (brak etykiet, opis nieaktualny) | main po 93366c50 (#595/#597): `.github/workflows/railway-iac.yml:168,220` JUŻ przekazuje `vars.KUKING_WAIT_FOR_CI` do plan i apply — główna teza issue nieaktualna. Nadal brak: walidacji wartości i odmowy przy braku (`.railway/railway.ts:637` puste = `false`), rozróżnienia w `docs/infra/CI_BEZ_ACTIONS.md`. PR #1595 (unstable) dokłada walidację w workflow, odmowę w `railway.ts` i runbook. | Co dalej: zaktualizować opis issue (przekazanie zrobione w 93366c50; zostały kryteria 1-3 i 4), scalić #1595. Ustawienie `vars.KUKING_WAIT_FOR_CI` = decyzja właściciela (D-010). |
| #1392 Strona tagu: „widzisz tylko Ty” przy wpisie dla obserwujących | c | main: `resources/views/pages/tags/show.blade.php:48,77-79` zawsze „widzisz tylko Ty” dla `private`+`followers`. PR #1495 (22ee846d, clean): 3 warianty zdań + odmiana liczebnika, testy w `ZeroWpisowWSpisieTagowTest` (mieszane, 5 prywatnych, gość/obcy, moderator). | Co dalej: scalić #1495 (checki zielone); pokrywa kryteria. |
| #1394 Blokada z autorem przepisu: własne wykonanie z linkiem do 403 | c + d | main: `app/Policies/CookedEventPolicy.php:80` odmawia właścicielowi przy blokadzie z autorem przepisu; `ProfileController.php:399-403` `tylkoZWidocznychPrzepisow()` przy `$isOwner` nie filtruje. PR #1503 (aedd6656, clean) odwraca dotychczasową regułę, dodaje D-259 do `docs/DECISIONS.md`, test `WykonaniePoBlokadzieAutoraPrzepisuTest` + mutacje. Sam PR oznacza się „Do decyzji właściciela”. | Co dalej: właściciel akceptuje/odrzuca D-259; potem scalić #1503 (i dopiero wtedy #1566). Numer D-259 zajmuje też #1584 — pilnować kolejności. |
| #1395 Usunięty przepis zostawia pustą zapowiedź w archiwum | c | main: `app/Http/Controllers/ProfileController.php:325-327` `tylkoWidoczne()` wraca dla właściciela przed filtrem przepisu. PR #1496 (177c0eb0, clean, +83): filtr zapowiedzi z miękko usuniętym przepisem (z wyjątkiem wpisów z treścią/zdjęciem), test z DataProvider „bez/z komentarzami”, komentarze zostają w bazie. | Co dalej: scalić #1496; pokrywa kryteria. Brak zapisu w DATABASE/DECISIONS nie jest potrzebny (bez zmiany schematu). |
| #1396 Odpowiedź pod ukrytym korzeniem przechodzi bramkę zgłoszeń | c | main: `app/Policies/CommentPolicy.php:43-78` `view()` nie sprawdza `parent_id`/korzenia. PR #1502 (b2dc5bd4, clean): `korzenWidoczny()` + `OdpowiedzPodUkrytymKorzeniemTest` (moderacja, blokada w obie strony, ban, usunięcie, kontrola dodatnia, furtki autora/moderatora) + mutacja. | Co dalej: scalić #1502; upewnić się, że test obejmuje oba wejścia `create()` i `store()` (kryterium 3). |
| #1400 Publiczny domyślny zeszyt a przyszłe szybkie zapisy | c | main: `resources/views/pages/collections/edit.blade.php` bez słowa o przyszłych zapisach; szybkie „Zapisuję” w `recipes/show.blade.php`/`post-card.blade.php` bez nazwy celu. PR #1493 (804c3dcc, unstable): notka przy edycji domyślnego, opis celu z `aria-describedby` przy przycisku, test pełnej drogi + kontrola ujemna. | Co dalej: zielone CI i scalenie. Ryzyko: copy „widzą inne zalogowane osoby” stanie się nieprawdą po PR #1533 (#965 — publiczny zeszyt bez konta). |
| #1401 „Oznacz wszystkie” odczytuje ukryte blokadą | e (DUPLIKAT #969) + c | main: `app/Http/Controllers/NotificationController.php:152` bez `visibleTo`. #969 (otwarte, z etykietami) opisuje to samo; #1616 sam pisze „#1401 (duplikat #969)”. Tę samą linię zmieniają TRZY PR-y: #1541 (clean, Closes #969 #1401), #1555 (unstable, #1402) i #1616 (unstable, Closes #759 #1401). | Co dalej: zamknąć #1401 jako duplikat #969; scalić tylko #1541 (Closes #969), a z #1616 usunąć „Closes #1401” i dublujący test. |
| #1402 Aktywne „Oznacz wszystkie” bez nieprzeczytanych | c + e (brak etykiet) | main: `resources/views/pages/notifications.blade.php:15` (`total()>0`) i `:36` (`count()>0`) bez sprawdzenia nieprzeczytanych; komunikat stały w `NotificationController.php:154`. PR #1555 (dbe4283a, unstable): `saNieprzeczytane` po wszystkich stronach, komunikat „Nie było nic do oznaczenia…” przy 0 wierszach, test `OznaczWszystkieTylkoGdyJestCoOznaczycTest`; zawiera też `visibleTo` z #1541. | Co dalej: scalić po #1541 (w konflikcie brać wersję #1555), potem zielone CI. |
| #1429 Awaria audytu po zleceniu eksportu → 500 | c + e (brak etykiet) | Na main `app/Http/Controllers/Settings/DataSettingsController.php:214` nadal rzucające `AuditLogEntry::record('data.export_requested')` po transakcji z :175; brak commitu z #1429. PR #1614 (wspólny dla #1429/#1530/#1573), stan unstable, base main: zamienia na `recordBezWywracania()` (D-249 klasa 2, dopisek w DECISIONS.md) + `test_awaria_audytu_eksportu_potwierdza_przyjecie_zamiast_500` i kontrola dodatnia. | Co dalej: zazielenić checki #1614 i zmergować. Brakuje jawnego testu „dwuklik po awarii audytu nie tworzy drugiego eksportu”, choć ponowienie kończy się wcześniej w `odpowiedzNaTrwajacy()`, jeszcze przed audytem. |
| #1435 Równoległe potwierdzenia tego samego e-maila → 500 | c | Na main `app/Domain/Users/Actions/ConfirmEmailChange.php:144` (`exists()`) i :159 (`save()`) bez obsługi 23505. PR #1514 w stanie clean: obsługa konfliktu indeksu, test na dwóch połączeniach `tests/Dwa/RownoleglePotwierdzeniaAdresuTest.php`, a w `PotwierdzenieAdresuKonfliktIndeksuTest` wariant z wielkimi literami i przypadek „inny konflikt nie udaje zajętego adresu”. | Co dalej: #1514 pokrywa kryteria i jest gotowy do merge'a. Wiersz „przegrany zachowuje sesje/remember_token” warto sprawdzić przy review. |
| #1456 Awaria enqueue po uploadzie → media pending bez joba | c | Na main `app/Domain/Media/Actions/StoreUploadedImage.php:270`: `dispatch` poza `try/catch` (:202–264). PR #1467, stan unstable: `Media::create` i dispatch w jednej transakcji na połączeniu `media`. Test `ZlecenieZdjeciaWTransakcjiTest` ma 5 przypadków: odmowę `jobs`, ponowienie, kontrakt „kolejka bazodanowa, bez afterCommit” i formularz. Kontrola ujemna jest w `kontrole-negatywne-alfa08.py`. | Co dalej: zazielenić i zmergować #1467. Edge case „wiele zdjęć, enqueue pada na kolejnym” nie ma osobnego testu. |
| #1461 Dokumentacja prywatności opisuje wyłączoną analizę awatarów | c + e | Na main `docs/legal/SYGNALY_AUTOMATU.md:392` (tabela §9.2: „`AvatarSettingsController` … zleca `PrzeanalizujAwatar`”) i `docs/DATABASE.md:2343` („model ocenia je po przetworzeniu”). Dopiski o D-240 są tylko w :256 i :469. PR #1519 jest dirty i ma base `claude/994-retencja-logow-polityka` (PR #1499 dla #994, wciąż otwarty). Właściwa zmiana to jeden commit `50b4ec40` (6 plików, strażnik `DokumentacjaAwataraZgodnaZKodemTest`), reszta z 243 plików pochodzi z gałęzi bazowej. | Co dalej: przepiąć #1519 na main (rebase/cherry-pick `50b4ec40`) albo najpierw zmergować #1499. W obecnej postaci nie mergować. |
| #1479 Moderator przywraca własną ukrytą treść | c | Na main `app/Http/Controllers/Admin/ModerationController.php:542-574` sprawdza tylko `moderate`. `app/Domain/Moderation/Actions/RestoreContent.php` nie porównuje aktora z autorem. PR #1491, stan unstable: wyjątek `WlasnejTresciNiePrzywracasz` w akcji i w `ResolveAppeal`, 8 testów (wpis, komentarz, kontrole dodatnie, odwołanie administratora) oraz `tests/mutacje/przywracanie.txt`. | Co dalej: #1491 pokrywa kryteria. Przed merge'em trzeba rozwiązać konflikt z #1485 w `ResolveAppeal.php`. |
| #1530 Awaria audytu zużywa jednorazowy link logowania | c + e (brak etykiet) | Na main `app/Http/Controllers/Auth/LoginLinkController.php:386/395` (`delete()` w `ZamekKonta`), :414 (`record` po transakcji), :434 (`Auth::login`). PR #1614, stan unstable: wpis przeniesiony do callbacku `ZamekKonta::zablokuj` (klasa 1 w DECISIONS). Testy: awaria bez 2FA z ponowieniem, awaria z 2FA i kontrola dodatnia. | Co dalej: zmergować razem z #1614. Przy review sprawdzić, że po awarii przy 2FA kod zapasowy nie jest zużyty; kryterium jest w teście :86. |
| #1534 Retencja spraw i eksportów: częściowa porażka = SUCCESS | c + e (tylko P2) | Na main `app/Console/Commands/SprzatajSprawyModeracyjne.php:70` i `CleanUpDataExports.php:120` zwracają `SUCCESS` po `warn` o błędach. Numery linii w issue (64/108) są już nieaktualne. PR #1545, stan unstable: niezerowy kod i 6 testów, także przez `Harmonogram::artisan()` (jest na main w `app/Support/Harmonogram.php:38`). | Co dalej: zazielenić i zmergować #1545. #1535 (dla #835) tylko wspomina #1534 i scala się z #1545 bez konfliktu. |
| #1572 Błąd zdjęcia bez aria-describedby | c + e (brak etykiet) | Na main `resources/views/pages/posts/create.blade.php:94` i `cooked/create.blade.php:45` mają `aria-describedby="f-photos-help"` bez id błędu i bez `aria-invalid`. W `questions/create.blade.php:32` input nie ma `aria-describedby` wcale. PR #1621, stan unstable: 4 testy HTTP (z kontrolą dodatnią) i dodatkowo kreator przepisu. | Co dalej: #1621 pokrywa kryteria, ale koliduje z #1571 (#874) w trzech widokach `*/create.blade.php` i w CHANGELOG. Trzeba ustalić kolejność merge'a. |
| #1573 Awaria audytu po blokadzie → błąd mimo skutecznej blokady | c | Na main `app/Domain/Social/Actions/BlockUser.php:92`: `AuditLogEntry::record` po `ZamekPary::zablokuj` (:77). PR #1614, stan unstable: `recordBezWywracania` (semantyka fail-safe, D-249 klasa 2) i testy awarii oraz kontroli dodatniej w `AwariaAudytuNiePrzewracaZatwierdzonejZmianyTest`. | Co dalej: zmergować z #1614. Kryterium „ponowiona blokada nie mnoży wpisów audytu” nie ma widocznego testu, trzeba je dopisać albo potwierdzić przy review. |
| #1599 Zgłoszenie konta po username, nie po ID | c + e (brak etykiet) | Na main `app/Http/Controllers/ReportController.php:321` (`Profile::where('username', $id)`) i `resources/views/pages/profile/show.blade.php:268` (`'id' => $p->username`). PR #1630 (wspólny z #1600), stan unstable: zgłoszenie po UUID, 5 testów (zmiana nazwy, stary odnośnik, 404). | Co dalej: #1630 pokrywa kryteria. Zazielenić i zmergować. |
| #1600 Onboarding: ukryte `oczekiwani` blokują poprawny wybór | c | Na main `resources/views/pages/onboarding/people.blade.php:89/131/174` renderują `oczekiwani` dla każdej osoby. `app/Http/Controllers/OnboardingController.php:201` ma `max:20`. PR #1630, stan unstable: filtr do zaznaczonych, testy 8+5+8, zmiana właściciela nazwy i ponad 20 zaznaczeń. | Co dalej: zmergować #1630. Kontrola ujemna (przywrócenie limitu) nie jest osobnym testem, ale scenariusz 8+5+8 oblałby na starym kodzie. #1630 scala się bez konfliktu z #1541. |
| #1636 Zgłoszenie prawne ufa ścieżce obcego URL, surowy adres w mailu | c | Na main `app/Http/Controllers/ZgloszenieNielegalnejTresciController.php:181` (tylko `PHP_URL_PATH`), `PotwierdzenieZgloszeniaNielegalnejTresci.php:48` i `DecyzjaWSprawieZgloszenia.php:75` (`->line(target_url)`). PR #1647, stan unstable: parser `AdresZgloszenia`, oznaczenie w panelu, 8 testów (obcy host, Markdown w obu mailach, panel). | Co dalej: #1647 pokrywa większość kryteriów. Przy review sprawdzić przypadki userinfo, schematu względnego i sufiksu hosta, bo nazwy testów ich nie wymieniają wprost. |
| #1657 Retencja prostych tabel w jednej nieograniczonej transakcji | c + e (brak etykiet) | Na main masowe `->delete()`: `app/Domain/Analytics/PrzedawnioneSygnaly.php:29`, `Compliance/PrzedawnioneWpisyAudytu.php:51`, `PrzedawnionePowiadomienia.php:70`, `PrzedawnioneSesje.php:74`, `PrzedawnionePotwierdzeniaRodo.php:95`. Komentarz w `PrzedawnioneSygnaly.php:13-21` („równie bezpieczny na przerwanie”) wprowadza w błąd. Brak PR. | Co dalej: nowa praca (wspólny helper partiami z budżetem). Nie jest duplikatem #998, który dotyczy spraw moderacyjnych i hydratacji. |
| #1662 Przejściowy błąd modelu = sukces bez retry | c | Na main `app/Jobs/PrzeanalizujTresc.php:57` (`$tries = 1`) z komentarzem o deterministyczności (:50-56) i `catch (Throwable)` (:138). `app/Moderacja/KlientOpenAI.php:66-160` zwraca `null` dla każdej porażki. Brak PR. | Co dalej: nowa praca. Rozdzielić wynik klienta (ocena / błąd trwały / błąd przejściowy) i ograniczyć retry w budżecie #829. Nie rozpoczynać przed ustaleniem budżetu z #829. |

## Do zamknięcia od razu

- **#1401** — duplikat #969 (ten sam błąd: `NotificationController::markAllRead()` na main, `app/Http/Controllers/NotificationController.php:152`, aktualizuje wszystkie nieprzeczytane bez `visibleTo`). #969 jest starsze, otwarte i ma etykiety; naprawę niesie PR #1541 („Closes #969”). Proponowany komentarz: „Zamykam jako duplikat #969 — ten sam błąd `markAllRead()` bez `visibleTo`; naprawa w PR #1541.” Z PR #1616 usunąć „Closes #1401” i zdublowany test `OznaczWszystkieTylkoWidoczneTest`.

Żadne inne issue nie jest zrobione na main w całości. Kandydaci do zamknięcia **zaraz po scaleniu jednego PR-a**, bo rdzeń jest już na main:
- #1315 — kod na main w c6dd5b8d (merge 45a5afd9); brakujące testy HTTP/2FA dodaje PR #1489.
- #1351 — `appeal.filed` ukryte przed nie-adminem od 282d6ec2 (merge 04bb3baf); `post.first` dokłada PR #1565.
- #1346 — blokada wiersza od #980; indeks częściowy i test dwóch połączeń w PR #1609.
- #1390 — `vars.KUKING_WAIT_FOR_CI` przekazywane od 93366c50 (`.github/workflows/railway-iac.yml:168,220`); walidację wartości dokłada PR #1595.
- #1309 — nie ma PR-a z „Closes”: kod w #1593, pomiar w #1628; zamknąć ręcznie po obu.

## Źle opisane / etykiety

Wszystkie proponowane etykiety istnieją w repo (sprawdzone przez API: `typ: usterka/zadanie/funkcja/bezpieczeństwo`, `obszar: backend/frontend/ux/seo/media/moderacja/bezpieczeństwo/infra/produkt`, `prywatność`, `dostępność`, `P0–P3`). Zapis „obszar: media/frontend” oznacza dwie etykiety.

- 1245: brak etykiet → `P2`, `typ: usterka`, `obszar: backend`, `prywatność`.
- 1295: brak etykiet → `P2`, `typ: bezpieczeństwo`, `obszar: bezpieczeństwo`.
- 1296: brak etykiet → `P2`, `typ: usterka`, `obszar: backend`.
- 1297: brak etykiet → `P2`, `typ: usterka`, `prywatność`, `obszar: backend`; dodać, że wymaga decyzji produktowej (zapis w `docs/DECISIONS.md`).
- 1300: brak etykiet → `P3`, `typ: usterka`, `obszar: ux`.
- 1302: brak etykiet → `P2`, `typ: usterka`, `obszar: frontend`, `obszar: ux`.
- 1304: brak etykiet → `P2`, `typ: usterka`, `obszar: ux`, `obszar: media`; dopisać powiązanie z #769. Ścieżki w opisie lekko przesunięte (show.blade.php ~586 zamiast 539, cooking ~125 zamiast 111).
- 1305: brak etykiet → `P2`, `typ: usterka`, `obszar: moderacja`, `obszar: backend`.
- 1306: brak etykiet → `P2`, `typ: bezpieczeństwo`, `obszar: bezpieczeństwo`; zaznaczyć, że część to działanie właściciela (Railway/Cloudflare).
- 1308: priorytet tylko w tytule → etykiety `P2`, `typ: usterka`, `obszar: backend`.
- 1309: priorytet tylko w tytule → `P2`, `typ: zadanie`, `obszar: backend`.
- 1310: priorytet tylko w tytule → `P2`, `typ: usterka`, `obszar: frontend`, `obszar: media`; opis zakłada 120 px na powitalnej, a według PR #1596 tam zdjęcie ma szerokość karty (120 px dotyczy szyny) — skorygować.
- 1280, 1289: etykiety kompletne.
- Wszystkie 14 (#1313, 1315, 1316, 1317, 1318, 1319, 1323, 1324, 1325, 1326, 1329, 1330, 1331, 1333): brak etykiet. Propozycja:
  - #1313: P1, typ: bezpieczeństwo, obszar: backend (infra/CI).
  - #1315: P1, typ: bezpieczeństwo, obszar: bezpieczeństwo.
  - #1316: P2, typ: usterka, obszar: backend.
  - #1317: P2, typ: usterka, obszar: moderacja.
  - #1318: P2, typ: usterka, obszar: backend (feed) + ux.
  - #1319: P2, typ: usterka, obszar: backend, prywatność.
  - #1323: P1, typ: usterka, obszar: moderacja (DSA).
  - #1324: P1, typ: usterka, obszar: backend, prywatność.
  - #1325: P2, typ: usterka, obszar: moderacja.
  - #1326: P3, typ: zadanie, obszar: media/frontend.
  - #1329: P2, typ: usterka, obszar: moderacja/backend.
  - #1330: P3, typ: zadanie, obszar: backend (testy).
  - #1331: P1, typ: usterka, obszar: backend, prywatność.
  - #1333: P2, typ: usterka, obszar: moderacja.
- #1313: opis mówi o `closed` i apply po merge'u PR-a — na main od 93366c50 apply chodzi wyłącznie z `workflow_dispatch` na main. Poprawka: przepisać problem na „plan z tokenem produkcyjnym dla PR-a do dowolnej gałęzi”, a kryterium 1 i 3 na zdarzenia opened/synchronize + workflow_dispatch.
- #1317: opis wskazuje `CommentController::destroy()`; logika jest teraz w app/Domain/Comments/Actions/DeleteComment.php:29. Poprawić ścieżkę.
- #1329: numery linii (102-139, 147-155, 112-152) nieaktualne; obecnie DailyBoardController.php:133/165/185, HeroKolazController.php:114/144/158-165.
Żadne z 14 issues nie ma etykiet. Propozycje:
- #1334: P3, `typ: funkcja`, `obszar: produkt`. W tytule lub opisie dopisać „po alfie / po walidacji #29”, żeby nikt tego nie zaczął w MVP.
- #1335: P2, `typ: usterka`, `obszar: backend`, `obszar: ux`.
- #1337: P2, `typ: usterka`, `obszar: backend`, `obszar: ux`.
- #1338: P2, `typ: usterka`, `obszar: backend`.
- #1339: P3, `typ: usterka`, `obszar: backend`.
- #1341: P2, `typ: usterka`, `obszar: frontend`, `dostępność`.
- #1344: P3, `typ: bezpieczeństwo`, `obszar: backend`.
- #1345: P3, `typ: usterka`, `obszar: backend`, `prywatność`.
- #1346: P2, `typ: usterka`, `obszar: backend`, `prywatność`. Opis jest częściowo nieaktualny: po #980 `markForDeletion()` czyta już świeży wiersz pod blokadą. Zostaje brak indeksu i testu dwóch połączeń.
- #1347: P2, `typ: usterka`, `obszar: backend`, `prywatność`.
- #1349: P2, `typ: usterka`, `obszar: media`, `obszar: backend`.
- #1350: P2, `typ: usterka`, `obszar: seo`.
- #1351: P2, `typ: bezpieczeństwo`, `obszar: moderacja`, `prywatność`. Dopisać, że rdzeń naprawił 282d6ec2.
- #1352: P2, `typ: bezpieczeństwo`, `obszar: backend`.
Duplikatów nie znaleziono. Pokrewne, ale odrębne issues są wskazane w opisach: #1025, #1084, #1318, #853, #996.
- Wszystkie 14 (#1354, 1356, 1357, 1358, 1359, 1360, 1364, 1365, 1366, 1368, 1370, 1371, 1377, 1378) nie mają żadnych etykiet. Proponuję:
  - #1354: P3, typ: usterka, obszar: moderacja, obszar: backend
  - #1356: P3, typ: usterka, obszar: backend
  - #1357: P2, typ: bezpieczeństwo, obszar: bezpieczeństwo, obszar: backend
  - #1358: P1, typ: bezpieczeństwo, obszar: bezpieczeństwo, obszar: backend
  - #1359: P1, typ: bezpieczeństwo, obszar: moderacja, prywatność
  - #1360: P1, typ: bezpieczeństwo, obszar: moderacja, obszar: media, prywatność
  - #1364: P1, typ: usterka, obszar: backend, prywatność
  - #1365: P2, typ: usterka, obszar: backend, prywatność
  - #1366: P3, typ: usterka, obszar: backend, obszar: ux
  - #1368: P3, typ: usterka, obszar: frontend
  - #1370: P3, typ: zadanie, obszar: seo
  - #1371: P2, typ: usterka, obszar: ux, obszar: moderacja
  - #1377: P2, typ: usterka, obszar: backend
  - #1378: P2, typ: usterka, obszar: backend, prywatność
- #1354: tytuł i opis dotyczą oceny awatara, która od D-240 nie istnieje (`PrzeanalizujAwatar` jest pusty, powiązane #1461/PR #1519). Poprawka: zmienić tytuł na „Log przygotowania zdjęcia do oceny modelem nie wskazuje UUID zdjęcia” i skreślić część o zewnętrznym `catch` zadania awatara.
- #1364: zarzut „`markForDeletion()` nie czyści `status_expires_at`” jest nieaktualny, bo main zeruje ten termin w `User.php:1229`. Poprawka: dopisać komentarz, że zostaje tylko allowlista w middleware i testy.
- #1368: odwołanie `cooking.blade.php:111` jest nieaktualne (obecnie :125). Kosmetyka.
- #1380, #1381, #1384, #1389, #1401, #1402: brak etykiet → proponowane: #1380 „P2, typ: usterka, obszar: ux”; #1381 „P3, typ: usterka, obszar: ux”; #1384 „P2, typ: usterka, obszar: backend”; #1389 „P2, typ: usterka, obszar: infra”; #1402 „P3, typ: usterka, obszar: ux”; #1401 — zamiast etykiet zamknąć jako duplikat #969.
- #1385: brak etykiet → „P2, typ: usterka, obszar: backend”. Kryterium 2 („blokada kucharza z autorem przepisu nadal odcina dostęp”) sprzeczne z proponowanym D-259 (PR #1503/#1566) — po decyzji właściciela zaktualizować.
- #1390: brak etykiet → „P1, typ: usterka, obszar: infra” (dotyczy bramki produkcyjnej, styk z P0 #595). Opis nieaktualny: przekazanie `vars.KUKING_WAIT_FOR_CI` do plan/apply jest już na main (93366c50, `railway-iac.yml:168,220`); zostawić tylko walidację wartości, odmowę przy braku i runbook.
- #1387: opis mówi o 1640 liniach, na main jest 1646 — kosmetyka; dopisać checklistę etapów z odnośnikiem do #1633 jako kroku 1.
- #1394: PR #1503 powołuje się na commit c26de5f3 („blokada ma pierwszeństwo przed prawem do własnej treści”) — tego skrótu nie ma w repo; warto wskazać właścicielowi właściwą decyzję/commit.
- #1429: brak etykiet. Proponowane: `P2`, `typ: usterka`, `obszar: backend`, `prywatność` (eksport danych, RODO).
- #1530: brak etykiet. Proponowane: `P1` (utrata jedynej drogi logowania 50+) lub `P2`, `typ: usterka`, `obszar: backend`, `obszar: bezpieczeństwo`.
- #1534: jest tylko `P2`. Dodać `typ: usterka` i `obszar: backend` (ew. `obszar: infra` jak #835). Ścieżki linii są nieaktualne (`:64` → `:70`, `:108` → `:120`).
- #1572: brak etykiet. Proponowane: `P2`, `typ: usterka`, `obszar: ux`, `obszar: frontend`. Opis mówi o trzech formularzach, a PR #1621 obejmuje też kreator przepisu, co warto dopisać.
- #1599: brak etykiet. Proponowane: `P2`, `typ: usterka`, `obszar: moderacja`, `obszar: bezpieczeństwo` (zgłoszenie niewłaściwej osoby).
- #1657: brak etykiet. Proponowane: `P2` (lub `P3`, bo issue samo nie twierdzi, że produkcja ma duży backlog), `typ: usterka`, `obszar: backend`.
- #1461: etykiety są. PR #1519 ma zły base, a issue jest opisane poprawnie.

## Uwagi: kolizje PR-ów, kolejność scalania, ryzyka

- **#1590 ma konflikt (dirty)** i zawiera w sobie cały #1584 (ta sama baza commitów: DailyBoard, DiscoverFeed, FollowingFeed, TagFeed, Post.php). Najpierw #1584, potem rebase #1590.
- **Obszar kolizji plików:** `app/Domain/Feed/DailyBoard.php` zmieniają #1549, #1584, #1590 i #1628; `CollectionController.php` zmieniają #1590 i #1628. #1628 wprost wymaga scalenia po #1584/#1590. Zalecana kolejność: #1584 → #1590 → #1549 → #1593 → #1628.
- #1309 nie ma PR-a z „Closes” — kod w #1593, pomiar w #1628; zamknąć ręcznie. Nie jest to kolizja, tylko rozproszone pokrycie.
- #1587 ma „Closes #1304”, choć nie daje autorowi możliwości wpisania opisu zdjęcia — scalenie zamknie issue przedwcześnie; usunąć „Closes #1304” albo dodać pole w edytorze.
- #1598 zamyka #1280 z jawną luką (public→prywatny nie przesuwa `lastmod`) — decyzja, czy to akceptowalne.
- #1306 i #1310 mają kryteria, których kod nie spełni sam (inwentaryzacja domen, pomiar bajtów w przeglądarce) — po scaleniu zostają zadania dla człowieka.
- Wszystkie PR-y poza #1587 (clean) i #1590 (dirty) są w stanie `unstable` (czerwone lub niekompletne checki) — przed scaleniem trzeba sprawdzić, czy to realne błędy, czy tylko nieskończone joby.
- #1624 (#1313) dirty i merytorycznie przestarzały: dodaje warunki do joba `apply` wyzwalanego na `closed`, który main już usunął. Wymaga przepisania, nie samego rebase'u.
- #1567 zawiera commit PR-a #1561 (#1338) — kolizja/stos: scalić #1561 najpierw albo zamknąć go jako wchłonięty. Oba wymieniają #1318 w nazwie gałęzi, ale tylko #1567 ma „Closes #1318”.
- #1590 (dirty) zawiera cały #1584 (#1377/#1378); kolejność: #1584 → rebase #1590.
- #1580 (unstable) zamyka trzy issues naraz (#1330, #1331, #1333) i dodaje migrację; #1576 zamyka dwa (#1323, #1325); #1549 zamyka trzy (#1027, #1329, #1296); #1604 łączy niezwiązany #1082 z #1316 — trudniejszy review i rollback.
- #1617 zmienia treść polityki prywatności — potrzebna akceptacja właściciela (powiązane z #8).
- Prawie wszystkie PR-y są `unstable` (czerwone/niekompletne checki) — przed merge'em uruchomić `./scripts/check.sh`.
- **#1656 (#1337)**: draft i konflikt (`mergeable=false`, dirty). To jedyny PR tej grupy, którego nie da się scalić bez rebase.
- **PR-y łączące kilka issues**:
  - #1552 niesie #1344 razem z #1025 i migracją `dodaj_granice_zgody_dostawcy`. Scalenie drobnej poprawki typu zależy od recenzji migracji.
  - #1556 łączy 3 issues (#1345, #1300, #1381).
  - #1583 łączy #1341 z #1366 i zmienia `.github/workflows/ci.yml`. Długi regex w filtrze widoku łatwo skonfliktuje się z innymi PR-ami, które go edytują.
  - #1609 obejmuje #1346, #1347 i #1364.
  - #1553 obejmuje #1339 i #1350.
- **Możliwe konflikty tekstowe**:
  - #1561 i #1540 (#853) zmieniają `TagFeed::maTresci()` w sąsiednich hunkach (@@ -125/-131).
  - #1561 wspomina też #1318, który ma osobny PR #1567 (Closes). Sprawdzić, czy #1561 nie zamknie przypadkiem #1318 (w tytule go nie ma).
- **PR-y bez zielonych checków**: poza #1589 (clean) wszystkie są `unstable`, czyli mają czerwone albo niekompletne checki. Każdy trzeba przejrzeć w CI przed scaleniem.
- **Drobne luki**:
  - #1553 (#1350): brak testu 301 po łańcuchu scaleń.
  - #1553 (#1339): łapie każdą `UniqueConstraintViolationException`, a nie tylko indeks nazwy.
  - #1556 (#1345): brak przypadku 31.01 przy domyślnych 12 miesiącach.
- #1358: dwa PR-y, ale bez kolizji zamknięć. #1484 zamyka (Closes), a #1500 dotyczy #1046 i wspomina #1358 tylko w sekcji testów. Oba wpływają jednak na `User::invalidateSessions()`: #1500 przepisuje je na surowy `UPDATE ... RETURNING` z generacją sesji, a #1484 woła je wewnątrz `ZamekKonta`. Po scaleniu drugiego z nich trzeba puścić `tests/Dwa` i `SesjaPoUniewaznieniuNieWracaTest`. #1484 zapowiada też trywialny konflikt w `tests/Dwa/bin/scenariusz.php`, który zmienia również #1609.
- #1354: PR #1588 ma w nazwie gałęzi `1354`, ale go nie zamyka (Refs); właściwy jest #1597.
- #1377: PR #1590 jest `dirty` (konflikt) i zawiera commity #1584 („scalać po #1584”). Scalić najpierw #1584, potem odświeżyć #1590.
- Pliki zmieniane przez kilka PR-ów naraz, z ryzykiem konfliktów przy kolejnych scaleniach:
  - `app/Models/Notification.php`: #1586, #1584 i #1565.
  - `app/Domain/Users/Exports/CollectUserExportData.php`: #1586, #1584 i #1500.
  - `tests/Feature/ZawieszonyModeratorBezUprawnienTest.php`: #1551 i #1565.
  - `.github/workflows/ci.yml` (długi regex filtra widoku): #1583.
- #1365 (PR #1577): migracja celowo odmawia, jeśli na produkcji są niekompletne wiersze `ready`. Przed wdrożeniem potrzebna decyzja lub działanie właściciela.
- #1357 (PR #1591): test sprawdza plik ini, a nie zbudowany obraz. Jest to akceptowalne, bo obraz kopiuje `docker/php.ini` do `conf.d`, ale ustawienie warto potwierdzić w działającym kontenerze.
- #1368 (PR #1587): pomiar z kryterium 1 nie został wykonany. PR dotyka też `app/Jobs/GenerateUserExport.php` i `exports/recipe.blade.php` w ramach #1304 (alt zdjęcia kroku), co poszerza zakres przeglądu.
- Stan PR-ów: #1587 jest `clean`, #1590 `dirty`, pozostałe (#1484, #1500, #1551, #1577, #1583, #1584, #1586, #1588, #1591, #1597, #1609, #1643) `unstable`: czerwone albo niekompletne checki, bez konfliktu.
- Kolizja #1401: trzy PR-y zmieniają tę samą linię `NotificationController::markAllRead()` — #1541 (clean), #1555 (unstable), #1616 (unstable); #1541 i #1616 dodają plik `tests/Feature/OznaczWszystkieTylkoWidoczneTest.php` o tej samej nazwie klasy (konflikt add/add). Kolejność zalecana w opisach: #1541 → #1555 → #1616; z #1616 usunąć „Closes #1401” i dublujący test.
- Kolizja #1402: #1541 wspomina #1402 tylko pośrednio (map bez „C”), zamyka go #1555; merytorycznie się uzupełniają, konflikt tekstowy w `markAllRead()` — w konflikcie brać wersję z #1555.
- #1394/#1385: #1566 jest zbudowany na #1503 (zawiera jego commity i D-259) — scalanie #1566 przed decyzją o #1503 przemyci zmianę reguły blokady. Numer D-259 powołuje też #1584 (#1377/#1378); na main ostatni numer D-256 (+D-1009), a D-257/D-258 rezerwują #1497/#1503/#1513 — ryzyko zderzenia numeracji decyzji.
- #1595 łączy #1389 i #1390 (workflow preview + IaC produkcji) i jest unstable — łatwiej przeglądać, ale czerwone CI blokuje oba.
- #1556 i #1579 łączą niepowiązane issues (#1345/#1300/#1381, #1305/#1384) — przy rewizji jednego z nich blokują też pozostałe.
- #1400 vs #1533 (#965): tekst „widzą inne zalogowane osoby” w #1493 będzie nieprawdziwy, jeśli publiczny zeszyt stanie się dostępny bez konta — uzgodnić copy przy drugim scalanym PR-ze.
- #1387: #1633 dotyka `recipe-wizard.blade.php`, który zmieniają też #1604, #1594, #1543, #1526 — spodziewane konflikty; scalać krok 1 szybko albo po nich.
- PR-y unstable (czerwone/niekompletne checki): #1563, #1556, #1579, #1633, #1595, #1493, #1616, #1555. Clean: #1566, #1495, #1503, #1496, #1502, #1541. Żaden z PR-ów grupy nie ma konfliktu z main (mergeable=true).
- **#1519 (#1461)** ma base `claude/994-retencja-logow-polityka` (PR #1499, otwarty, nieobecny w prstate.txt) i stan dirty. Diff wobec main to 13 plików, a właściwa zmiana to tylko commit `50b4ec40` (6 plików). Potrzebny rebase na main albo najpierw merge #1499.
- **#1614** zamyka trzy issues naraz (#1429, #1530, #1573). Jeśli któreś kryterium zablokuje review, wszystkie trzy utkną. Nie widać testów idempotencji audytu przy ponowionej blokadzie i dwukliku eksportu.
- **#1630** zamyka dwa issues (#1599, #1600).
- **Kolizja #1491 (#1479) ↔ #1485 (#950/#933/#989):** realny konflikt treści w `app/Domain/Moderation/Actions/ResolveAppeal.php` (merge-tree) i w CHANGELOG. #1485 sam to zapowiada w opisie.
- **Kolizja #1621 (#1572) ↔ #1571 (#871/#872/#874):** oba zmieniają `posts/`, `cooked/` i `questions/create.blade.php` oraz CHANGELOG. Git scala widoki automatycznie, więc konflikt treści jest tylko w CHANGELOG. Semantykę `aria-describedby` po złączeniu trzeba sprawdzić ręcznie.
- #1545 i #1535 scalają się bez konfliktu. #1535 tylko wspomina #1534 i go nie zamyka.
- Wszystkie PR-y z grupy poza #1514 (clean) i #1519 (dirty) są w stanie unstable (czerwone lub niekompletne checki). Przed merge'em potrzebne `./scripts/check.sh`.
- #1657 i #1662 nie mają PR-ów. #1662 zależy projektowo od budżetu czasu z #829 (otwarte).
