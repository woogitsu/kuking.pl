# Audyt po fali scaleń — 26 września 2026

**Zakres.** Od `a7483d52` do `origin/main` = `61fb0b0e8`: 50 merge'y
pierwszego rodzica. 33 z nich scalono między 06:28 a 07:39, 17 kolejnych
między 07:39 a 07:44. Scalanie trwało w czasie audytu, więc to jest migawka.
Szukane było to, czego **żaden pojedynczy PR nie mógł zobaczyć**: kolizje
między niezależnie scalonymi zmianami, rozjazd dokumentów z kodem, kotwice
kontroli negatywnych, UX 50+ w widokach zmienionych tego dnia.
Wzór metody: `docs/audyt/2026-09-25-PO-FALI.md`.

**Metoda.**
- Harmonogram czytany z `php artisan schedule:list` (zdarzenia, nie tekst),
  z autoloaderem worktree (`tests/bootstrap.php`).
- Trasy: `route:list --json` (nazwy, metoda+URI). Do tego przejście po
  trasach W KOLEJNOŚCI REJESTRACJI: czy wcześniejsza trasa łapie adres
  późniejszej (np. /x/{id} przed /x/nowy — przykład, nie trasa serwisu). Detektor ma kontrolę
  dodatnią na sztucznej parze tras.
- Duplikaty kluczy w `config/*.php` wyszukane parserem PHP
  (`nikic/php-parser`), z kontrolą dodatnią na pliku z duplikatem.
- `.env.example` porównany z każdym `env('…')` dodanym w fali.
- Kotwice kontroli negatywnych sprawdzone wykonaniem preflightu
  `scripts/kontrole-negatywne-alfa08.py` w pamięci. Sprawdzone też, czy
  każdy filtr testu trafia w istniejącą klasę albo metodę.
- Numery D-xxx sprawdzone na `main` oraz w nowych nagłówkach wszystkich
  149 otwartych PR-ów (`git diff origin/main...<gałąź>`).
- CHANGELOG: duplikaty na poziomie WPISU (punkt listy z liniami kontynuacji),
  do tego podobieństwo tekstu > 0,6.
- Pliki zmienione przez więcej niż jeden PR fali przeczytane parami.
- Pełny `php artisan test` na `main` dwukrotnie (wyniki niżej).

## Wynik w skrócie

| # | Znalezisko | Waga | Co zrobione |
|---|---|---|---|
| 1 | Runbooki IaC pokazują `railway config plan/apply` bez `KUKING_WAIT_FOR_CI`, a od #1595 (#1390) `railway.ts` bez tej zmiennej rzuca wyjątkiem. Instrukcja #595 pisze wręcz „zmienna tylko, jeśli krok 0.3” | średnia | gałąź `claude/audyt-2609-2-iac-bramka-ci` + strażnik |
| 2 | `CI_BEZ_ACTIONS.md:81` (tekst #1595 pisany przed #595) opisuje apply „po scaleniu”. Od #595 apply idzie tylko ręcznie | niska | ta sama gałąź 2 |
| 3 | CHANGELOG: wpisy #1377 i #1378 dwa razy, słowo w słowo (`CHANGELOG.md:63-64` i `:70-71`) | niska | gałąź `claude/audyt-2609-1-changelog-duplikaty` + strażnik |
| 4 | **D-281** zajmują jednocześnie otwarte PR-y #1846 i #1849 | średnia (przed scaleniem) | **nie poprawione** — patrz niżej, pytanie |
| 5 | Migracja `2026_09_24_120000_add_appeal_id_to_moderation_actions` łamie AGENTS.md §6 („DDL na istniejącej tabeli”) | niska | pytanie (migracja już na `main`) |
| 6 | Minuta 35 nadal zajęta dwa razy (`routes/console.php:449` i `:546`) | niska | znane z 25.09. Poprawka czeka w PR #1791 |
| 7 | `KUKING_HEALTH_TOKEN` (#1672) istnieje tylko w panelu, nie w `railway.ts` — ta sama klasa co pkt 12 audytu z 25.09 | do sprawdzenia | pytanie |
| 8 | `docker/entrypoint.sh:237` odsyła do `checks.media` w `/health`. Od #1672 pole widać tylko z nagłówkiem tokenu | kosmetyczna | propozycja, bez zmiany (entrypoint uruchamia ciężkie joby i ma kotwice kontroli) |
| 9 | Nowy `WyjatekD262ZamknietaListaTest` nie ma kontroli dodatniej PRZED mutacją — dziewiąty taki wpis | kosmetyczna | dopisać do PR z propozycji D z 25.09 (`claude/audyt-d-kontrola-dodatnia`) |
| 10 | Pełny `php artisan test`: 14 porażek na `0bd139ce` i 3 na `61fb0b0e8`. Wszystkie wynikają ze środowiska | środowisko, nie regresja | opis niżej |

## Szczegóły

### 1–2. `KUKING_WAIT_FOR_CI` obowiązkowe (#1595) × runbooki (#595, #1624)

PR #1595 (#1390, scalony 07:38) zmienił `.railway/railway.ts:839-858`.
Dawniej brak zmiennej dawał po cichu `checkSuites: false`. Teraz
`railway.ts` rzuca wyjątkiem, jeśli `KUKING_WAIT_FOR_CI` nie jest dokładnie
`"true"` albo `"false"`. W workflow wszystko się zgadza: plan i apply mapują
zmienną repozytorium. Dokumenty operatora nie nadążyły:
- `DEPLOYMENT_RUNBOOK.md:1888` i `:1900`: gołe `railway config plan`
  i `railway config apply` w kroku 9. Sąsiednia sekcja §8 „Zamiast
  stagingu” (#1624, scalony 06:32) każe uruchomić ten plan przed apply;
- `PRZELACZENIE_NA_3_SERWISY_595.md:91`: `# zmienna tylko, jeśli krok 0.3`.
  Krok 0.3 i komendy stagingu (`:78-79`) też traktują zmienną jako opcję;
- `LISTA_KROKOW_ALFA.md:336`: „zmienna tylko, jeśli A1 pokazał włączone”;
- `CI_BEZ_ACTIONS.md:81`: „plan w PR, apply po scaleniu”. Ten wiersz
  #1595 napisał 24.09 o 17:24, a #595 przeniósł apply na `workflow_dispatch`
  tego samego dnia o 22:02.

Skutek: operator idący po runbooku dostaje wyjątek w kroku, w którym
najmniej chce improwizować. Komunikat wyjątku podaje wprawdzie poprawną
komendę, więc to nie jest cicha szkoda.

**Poprawka (gałąź 2):**
- wszystkie komendy dostają `KUKING_WAIT_FOR_CI=true`, z dopiskiem, że
  `false` oznacza świadome wyłączenie bramki;
- krok 0.3, komentarz `:91` i B2 w `LISTA_KROKOW_ALFA.md` mówią „obowiązkowa”;
- `CI_BEZ_ACTIONS.md:81` mówi „apply wyłącznie ręcznie”.

Nowy `KomendyIacWDokumentachPodajaBramkeCiTest` przechodzi po blokach kodu
w `docs/**.md` (bez `docs/audyt/`). Każde wywołanie `railway config
plan|apply` musi tam mieć `KUKING_WAIT_FOR_CI=true|false`.
- Kontrola dodatnia na sztucznym tekście: odróżnia komendę od wzmianki
  i od komentarza.
- Kontrola ujemna: na dokumentach z `origin/main` test oblewa na czterech
  liniach.
- Nowy wpis w `kontrole-negatywne-alfa08.py`.

Scala się bez konfliktu z otwartym PR #1792 (runbook po #595). Sprawdzone
przez `git merge-tree`.

### 3. CHANGELOG: #1377 i #1378 dwa razy

Źródło: gałąź #1297/#1319 (PR #1590) wciągnęła gałąź #1377/#1378. Potem
konflikty rozwiązywano zasadą „obie strony”, a obie strony niosły ten sam
wpis. Na `a7483d52` tych wpisów nie było wcale. Na `main` są dwa razy.

**Poprawka (gałąź 1):**
- usuwa drugą kopię;
- dodaje `ChangelogBezZdublowanychWpisowTest`. Test łączy punkt listy
  z liniami kontynuacji i oblewa przy dwóch identycznych wpisach
  w całym pliku, podając numery linii.

Kontrola dodatnia na sztucznym pliku z duplikatem wieloliniowym. Kontrola
ujemna: z `CHANGELOG.md` z `origin/main` test oblewa i wskazuje 63/70
i 64/71. Dostał też wpis w `kontrole-negatywne-alfa08.py`. Strażnik NIE łapie
duplikatów sformułowanych inaczej. Taki jest przypadek #887/#889/#890
z 25.09: `CHANGELOG.md:38-39` i `:85-87`. Jego poprawka czeka w PR #1793.

### 4. D-281 w dwóch PR-ach

`claude/1801-licznik-komentarzy` (#1846) i stos
`claude/1814-metryki-doboru` (#1849) dodają oba nagłówek `## D-281`.
Pozostałe powtórzenia numerów w otwartych PR-ach to stosy (#1734–#1738,
#1823–#1849): każdy PR zawiera zmiany poprzedniego, więc to nie są kolizje.
Koordynator przydzielił #1849 numer **D-283**. Przygotowałem zamianę
(19 wystąpień w 12 plikach gałęzi #1849), ale **nie wypchnąłem jej**:
push do gałęzi cudzego PR-a zatrzymało uprawnienie środowiska tej sesji.
Worktree posprzątany, gałąź #1849 nietknięta.

### 5. Migracja `appeal_id` a „DDL na istniejącej tabeli”

`database/migrations/2026_09_24_120000_add_appeal_id_to_moderation_actions.php`:
- `:52`: klucz obcy bez `NOT VALID`;
- `:55`: `CREATE UNIQUE INDEX` bez `CONCURRENTLY`;
- `:56`: `ADD CONSTRAINT … CHECK` bez `NOT VALID`;
- wszystko w jednej transakcji.

AGENTS.md §6 wymaga czego innego: `CONCURRENTLY` w migracji
z `$withinTransaction = false` oraz `NOT VALID` + osobne `VALIDATE`.
Łagodzi to kilka rzeczy:
- `moderation_actions` to mała tabela, na którą nie czeka feed;
- `lock_timeout = 5s` (`LimitBlokadMigracji`) i tak przerwie DDL;
- indeks powstaje na samych `NULL`-ach.

Żaden test tej reguły nie pilnuje (`CONCURRENTLY` sprawdza tylko
`IndeksyKluczyObcychNaGoracychSciezkachTest`, dla jednej migracji).
Znacznik czasu 24.09 przy scaleniu 26.09 znaczy, że na produkcji ta migracja
pójdzie PO migracjach z 25.09, a na świeżej bazie PRZED nimi. Sprawdziłem, że
migracje z 25.09 nie dotykają `moderation_actions`, więc kolejność nie zmienia
wyniku.

### 6. Minuta 35

Tak samo jak 25.09: `kuking:doslij-pilne-alarmy` i
`kuking:dosylaj-potwierdzenia-zgloszen` na `35 * * * *`. W tej fali nikt nie
zmienił `routes/console.php` poza nowym `queue:prune-failed` o 05:20 (#1669),
a ten termin jest wolny. Poprawka z wczoraj czeka w PR #1791.

### 7–9. Pozostałe

**7.** `DEPLOYMENT_RUNBOOK.md:671` mówi wprost: `KUKING_HEALTH_TOKEN` „NIE
jest jeszcze przewleczona w `.railway/railway.ts`”. Pytanie z 25.09 (pkt 12)
dotyczy więc teraz czterech zmiennych: czy `railway config apply` usuwa
zmienne serwisu spoza pliku?

**8.** `docker/entrypoint.sh:237`: „Sprawdź stan przez /health — pole
checks.media mówi, co dokładnie padło.” Po #1672 bez nagłówka
`X-Kuking-Health-Token` tego pola nie ma. Runbook #1672 poprawił, tego
komunikatu nie.

**9.** `kontrole-negatywne-alfa08.py`: preflight przechodzi, każdy z 89
wpisów trafia w kod dokładnie raz, każdy filtr trafia w istniejący test.
Bez `run_test(…, True)` przed mutacją jest teraz dziewięć testów.
Doszedł `WyjatekD262ZamknietaListaTest` (#1825). Pętla i tak uruchamia test
na zielono po przywróceniu źródła, więc luki w ochronie nie ma.

### 10. Pełny zestaw testów

**Przebieg A — `0bd139ce`** (33 merge'y). Worktree, `APP_BASE_PATH`,
PostgreSQL 16.13, zmienne środowiska kontenera bez zmian.
- Wynik: 7248 testów, 107 665 asercji, **14 porażek**, 0 błędów.
- 11 porażek to `/health` = `degraded` i `SesNieUzywaPoswiadczenR2Test`
  (`proxy-injected`). Kontener wstrzykuje `AWS_ACCESS_KEY_ID`
  i `AWS_SECRET_ACCESS_KEY`, co daje `magazyn_r2_zly_host`. Po
  `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY` wszystkie te klasy
  przechodzą (sprawdzone klasa po klasie);
- 2 porażki to `RiskyTestFailsGateTest`: przy `CLAUDECODE`/`AI_AGENT`
  `artisan test` wypisuje JSON dla agentów. Bez tych zmiennych: 2/2;
- 1 porażka to `TestyChodzaNaPostgresieTest`: serwer ma PG 16, próg to 18.

**Prawdziwych porażek: zero.**

**Przebieg B — `61fb0b0e8`** (50 merge'y), bez zmiennych AWS:
- Wynik: 7309 testów, 108 138 asercji, **3 porażki**, 0 błędów.
- Porażki: `RiskyTestFailsGateTest` ×2 (wyjście JSON dla agentów)
  i `TestyChodzaNaPostgresieTest` (PG 16). Oba powody opisane wyżej.
- **Prawdziwych porażek: zero.** Nowe testy fali są zielone, w tym
  `EksportObejmujeKazdaTabeleKontaTest`, `MigracjeMajaLimitBlokadTest`
  i `StraznikTekstuMaKontroleDodatniaTest`.

Krok `tests/skrypty/kopia-bazy.sh` (`pg_restore` 16 kontra fikstura z PG 18)
i pełny `./scripts/check.sh` nie były uruchamiane. Rozstrzyga CI.

## Sprawdzone bez zastrzeżeń

- **Trasy:** 213 tras, brak zdublowanych nazw i par metoda+URI. Żadna
  trasa nie przesłania późniejszej.
- **`config/*.php`:** brak zdublowanych kluczy. Nowe klucze fali:
  `turnstile.hosty_stagingu`, `health.*`, `limits.health`,
  `livewire.release_token`.
- **`.env.example`:** brak zdublowanych zmiennych. Nowe `env()` z fali
  (`TURNSTILE_HOSTY_STAGINGU`, `KUKING_HEALTH_TOKEN`,
  `KUKING_HEALTH_PROBKA_MAGAZYNU_SEKUND`) mają wpis.
  `RAILWAY_GIT_COMMIT_SHA` wstrzykuje Railway, tak jak dotąd.
  `KUKING_ZAMROZONY_CZAS` stoi w komentarzu i czyta go `ZamrozonyCzas`.
- **Migracje:** w fali jedna, opisana w `DATABASE.md:2784` razem
  z rollbackiem, który odmawia (D-088). Drugiej migracji tej samej tabeli
  w fali nie ma. Ten sam znacznik `2026_09_24_120000` ma
  `add_contact_reply_delivery_markers`, ale to niezależna tabela.
- **D-xxx na `main`:** 236 nagłówków bez duplikatów. Jedyny nowy numer to
  D-274. Nowych odwołań do numeru bez nagłówka nie ma (stare z D-066–D-243
  opisuje audyt A4).
- **Inwentarz danych konta:** nowa kolumna `moderation_actions.appeal_id`
  wskazuje na `appeals`, nie na `users`, więc do inwentarza nie należy.
  `EksportObejmujeKazdaTabeleKontaTest` jest zielony. Klucz
  `w_czym_jestem_dobra` (#1758) nie został w żadnym widoku, dokumencie
  prawnym ani teście, poza strażnikami, które go celowo wymieniają.
- **Pary PR-ów w tych samych plikach:**
  - `PostPolicy` (#1683 `dopiszPrzepis` × #1516 podgląd moderatora):
    „Dopisz przepis” wymaga `published`, podgląd ukrytego wpisu jest
    tylko do odczytu (bez zapisu, zgłoszenia i komentarza);
  - `post-card` (#1532, #1544, #1683, #1516) spójny;
  - `CollectionController` (#1590 × #1655): licznik niedostępnych
    i porządkowanie liczą z tego samego `WidocznaZawartoscZeszytu`;
  - `PrzeanalizujTresc` (#1675 × #1622): ponowienia `release()` na `low`
    zgodne z nowym podziałem procesów;
  - `failed_jobs` z 30-dniową retencją (#1669) × `OdbiorZdjec` (#1829)
    × ślad #1662: opisane w `DATABASE.md`, `COMPLIANCE.md` i D-047.
    `mail_failures.failed_job_uuid` to nie klucz obcy, więc przycinanie go
    nie zablokuje.
- **UX 50+ w 48 plikach widoków, CSS i JS z fali:** w dodanych liniach
  brak `text-xs`/`text-sm`/`text-[..px]`, `hover:` jako jedynej drogi,
  `group-hover`, `opacity-0`, placeholderów zamiast etykiet i rozmiarów
  < 18 px. Wyjątek: `offline.html` `a.drugi:hover`, czysto dekoracyjny.
  Nowe akcje destrukcyjne („Wyjmij niedostępne zapisy” #1655, blokada
  z nazwą #1822) idą przez `x-confirm-button`. „Pokaż więcej” bez JS jest
  uczciwym odnośnikiem „Następna strona” (#1544), więc nie ma martwego
  przycisku.
- **Wyjątek D-262** (AGENTS.md §5 po #1825): numery linii CSS nadal
  wskazują komentarze D-262, mimo zmian `app.css` w #1543 i #1822.
- **Znaczniki konfliktu:** brak.

## Gałęzie z poprawkami

| Gałąź | SHA | Zawartość |
|---|---|---|
| `claude/audyt-2609-1-changelog-duplikaty` | `018e1529` | pkt 3 + `ChangelogBezZdublowanychWpisowTest` + wpis w kontrolach negatywnych |
| `claude/audyt-2609-2-iac-bramka-ci` | `d9c7dbfd` | pkt 1–2 + `KomendyIacWDokumentachPodajaBramkeCiTest` + wpis w kontrolach negatywnych |

Obie gałęzie dopisują wpis na końcu `checks` i linię `run_test`
w `kontrole-negatywne-alfa08.py`. Przy scaleniu drugiej: obie strony.

## Pytania do właściciela

1. **D-281 (#1846 × #1849):** czy #1849 ma przejść na D-283, jak przydzielił
   koordynator? Zamianę może wykonać sesja z prawem push do tej gałęzi.
   To 19 wystąpień w 12 plikach, w tym `kontrole-negatywne-alfa08.py`.
2. **Migracja `appeal_id` (pkt 5):** czy wystarczy zostawić ją jak jest
   i dopisać strażnika reguły §6 dla PRZYSZŁYCH migracji (issue)? Czy też
   jeszcze przed pierwszym wdrożeniem przepisać ją na `CONCURRENTLY`
   i `NOT VALID`? Drugie ma sens tylko wtedy, gdy produkcja jeszcze jej nie
   wykonała.
3. **Zmienne tylko z panelu (pkt 7):** czy `railway config apply` zachowuje
   zmienne spoza `railway.ts`? Dotyczy `KUKING_EDGE_TRYB`,
   `KUKING_TAG_TYGODNIA`, `KUKING_HTML_EDGE_CACHE_SECONDS`
   i `KUKING_HEALTH_TOKEN`.

## Propozycje issues

**A. Strażnik DDL na istniejącej tabeli (P2).** Test przechodzi po
migracjach nowszych niż ustalony punkt odcięcia. Oblewa na `CREATE INDEX`
bez `CONCURRENTLY` i na `ADD CONSTRAINT` (FK/CHECK) bez `NOT VALID` przy
tabeli, która istniała przed tą migracją. Potrzebuje kontroli dodatniej
i listy wyjątków z uzasadnieniem.

**B. Komunikat entrypointu po #1672 (kosmetyczne).** `docker/entrypoint.sh:237`
wymienia nagłówek `X-Kuking-Health-Token`. Do zrobienia przy najbliższej
zmianie entrypointu.
