Pracujesz nad portalem Kuking.pl (Laravel, Blade, PostgreSQL).

Katalog roboczy:  C:\Users\matma\Documents\kuking-flota\gpt-r2-emaillabs-procedura
Gałąź:            gpt/r2-emaillabs-procedura  (już istnieje, odgałęziona od main = 4c811cc7)
Repozytorium kanoniczne: C:\Users\matma\Documents\Codex\kuking.pl
  — tam wykonujesz WYŁĄCZNIE polecenia gita, nigdy nie zapisujesz plików.

NAJPIERW przeczytaj w całości:
  C:\Users\matma\Documents\kuking-flota\_wspolne\ZASADY_FLOTY.md
potem AGENTS.md w korzeniu swojego katalogu. AGENTS.md jest JEDYNYM
źródłem prawdy projektu; CLAUDE.md to tylko wskaźnik.

TWARDE ZAKAZY
- Nie pushujesz i nie otwierasz PR-ów. Pchanie idzie szeregowo przez
  kolejkę; dwa równoległe pchnięcia zderzają się na bazie i dają fałszywe
  porażki (142 failures, deadlocki — zdarzyło się dwa razy jednego dnia).
- Nie uruchamiasz `git worktree prune`. Z Windows każdy worktree spod
  /home/ wygląda na „prunable" i jedno polecenie wypruwa wszystkie naraz.
- Nie używasz `git reset --hard`, `--no-verify` ani gołego `git stash`
  (stos jest WSPÓLNY dla wszystkich stanowisk — użyj tymczasowego commita).
- PostgreSQL wyłącznie 127.0.0.1:55439, nigdy współdzielony 5432.
- Nie pracujesz w C:\Users\matma\Documents\Codex ani w podkatalogach.
- Nie wysyłasz wiadomości do ludzi ani instytucji.

ZASADY PROJEKTU, KTÓRE OBOWIĄZUJĄ ZAWSZE
- Bugfix = test regresyjny. Test MUSISZ zobaczyć NA CZERWONO przed
  poprawką — inaczej nie wiesz, czy mierzy to, co obiecuje.
- Testy chodzą na PostgreSQL, nie na SQLite.
- Poprawne dane użytkownika nigdy nie znikają: po błędzie walidacji
  formularz odtwarza to, co człowiek wpisał, a nie to, co stoi w bazie.
- Błędy i komunikaty PO POLSKU, mówiące CO ZROBIĆ, nie co się stało.
- UUID ani nazwa w adresie to NIE autoryzacja — każde wejście przez Policy.
- Nie zostawiaj martwych przycisków (D-053); i odwrotnie — działający
  endpoint bez drogi dla człowieka to ta sama usterka od drugiej strony.
- UX 50+: tekst >= 18 px, przyciski >= 48 px, bez hover i bez swipe.
- `status` i `role` użytkownika NIGDY w `$fillable`.
- Zmiana schematu = migracja + test + docs/DATABASE.md + plan wycofania.
  Wszystkie cztery, nie trzy.
- Przed zgłoszeniem gotowości: `vendor/bin/pint`.

ZANIM ZACZNIESZ KODZIĆ — ZMIERZ ZACHOWANIE NA NIETKNIĘTYM DRZEWIE.
Dziś w tym projekcie dwa zgłoszenia okazały się już naprawione, a jedno
straszyło jako blokada startu. Zamknięcie zgłoszenia z pomiarem jest
pełnoprawnym wynikiem i oszczędza czyjś tydzień.

JEŚLI COŚ OKAŻE SIĘ PYTANIEM PRODUKTOWYM, A NIE USTERKĄ — nie domykaj
tego asercją. Asercja zabetonuje zachowanie, którego nikt nie wybrał.
Wypisz warianty z kosztem jako decyzję dla właściciela.

URUCHAMIANIE TESTÓW (Git Bash na Windows; przedrostek MSYS_NO_PATHCONV=1
jest OBOWIĄZKOWY — bez niego ścieżka /mnt/c/... zostaje zepsuta):
  MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/c/Users/matma/Documents/kuking-flota/_wspolne/przygotuj-runtime.sh gpt-r2-emaillabs-procedura
  MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/c/Users/matma/Documents/kuking-flota/_wspolne/testuj.sh gpt-r2-emaillabs-procedura --filter TwojTest
Skrypt sam zakłada .env i APP_KEY. Po KAŻDEJ zmianie w kodzie uruchom go
ponownie, zanim odpalisz testy.

PUŁAPKI, NA KTÓRE KTOŚ JUŻ SIĘ NADZIAŁ
- Porażka poza Twoim obszarem NIE jest „zastana", dopóki jej nie
  odtworzysz. Trzy stanowiska dziś tak odpisały i dwa razy była to
  prawdziwa usterka oprzyrządowania. Jedyny znany wyjątek:
  ProbaOdtworzeniaTest pada, bo wszystkie stanowiska trafiają w tę samą
  bazę kuking_zrodlo_proby_glowny — wolno go pominąć, ale wymień z nazwy.
- Skan, który nie znajduje żadnego pliku, PRZECHODZI. Każdy nowy strażnik
  potrzebuje kontroli dodatniej na wzór `test_skan_naprawde_czyta_modele`.
- Pod `set -o pipefail` konstrukcja `printf '%s' "$X" | grep -q "$WZ"` daje
  warunek FAŁSZYWY mimo trafienia (SIGPIPE). Pisz `grep -qE "$WZ" <<< "$X"`.
- `<env>` w phpunit.xml bez `force` nie nadpisze prawdziwej zmiennej
  środowiskowej, ale nadpisze `.env`.
- `.gitignore` zaczyna się od `*.log` — dowody w .log wymagają `git add -f`.

JAK MELDUJESZ
Na koniec podaj: SHA commitów, co zmierzyłeś SAM (a co przejąłeś, z
adnotacją „[pomiar cudzy: źródło]"), czego NIE zrobiłeś i dlaczego, oraz
co wymaga decyzji właściciela. Commituj lokalnie, po polsku, w trybie
rozkazującym („Nie pozwól, by…", „Pokaż…", „Przywróć…").
Zgłoszenia czytaj przez: gh issue view NUMER --repo woogitsu/kuking.pl

------------------------------------------------------------------------
TWOJE ZADANIE
------------------------------------------------------------------------
Dziś za "sprawdzone" uchodzi coś, czego nikt faktycznie nie sprawdził w
panelu dostawcy: audyt `WERYFIKACJA_713_492_2026_09_20.md` (pozycje A2 i
A3 długu #713) stwierdza wprost "BRAK DOWODU" jurysdykcji i publiczności
bucketów R2 (#120/#617/#619) oraz wyłączenia piksela śledzącego otwarć u
EmailLabs (#204). Wszystko, co jest w repozytorium — `LocationHint` w
kodzie, nagłówki dot. śledzenia linków w `TransportEmailLabs` — to odczyt
KODU, nie odczyt PANELU dostawcy. Audytor pisze to wprost: "LocationHint
nie dowodzi jurysdykcji UE" i "lokalny nagłówek dotyczący linków nie
dowodzi wyłączenia piksela".

Te dwie rzeczy wymagają zalogowania się do panelu Cloudflare (R2) i panelu
EmailLabs — masz do tego dostęp WŁAŚCICIEL, nie Ty. Twoim zadaniem NIE
jest "idź i sprawdź" — nie masz tych kont ani nie wolno Ci ich zakładać
ani logować się w cudzym imieniu. Twoim zadaniem jest napisać dla
właściciela DOKŁADNĄ, gotową do wykonania procedurę sprawdzenia, krok po
kroku, z podaniem dokładnie tego, co ma zobaczyć na ekranie, żeby uznać
pozycję za zamkniętą albo za wciąż otwartą.

CO MASZ ZROBIĆ (praca, którą wykonujesz Ty, lokalnie, w tej sesji):

1. Przeczytaj źródła:
   - `WERYFIKACJA_713_492_2026_09_20.md`, sekcje A2 i A3 (oraz odnośne
     issues #120, #617, #619, #204 przez `gh issue view NUMER --repo
     woogitsu/kuking.pl`, wyłącznie odczyt, bez komentowania).
   - Kod: `app/**/TransportEmailLabs*`, wszystko co dotyczy R2/S3 klienta
     (konfiguracja `filesystems.php`, zmienne `R2_*`), oraz istniejące
     testy tych obszarów.
2. Dla R2 (A2) napisz procedurę właściciela obejmującą OSOBNO trzy rzeczy,
   które audyt wymienia jako różne: jurysdykcję/region bucketa, ustawienie
   publiczny/prywatny każdego z trzech bucketów (#120/#617/#619), oraz
   politykę lifecycle/retencji i blokady (Object Lock) — audyt mówi wprost,
   że #617 to wybór STRATEGII retencji, nie tylko brak testu. Podaj:
   dokładny adres w panelu Cloudflare, dokładną nazwę pola/przełącznika,
   jaką wartość oznacza "dobrze" a jaką "trzeba poprawić", i jak zrobić
   zrzut ekranu/eksport ustawień jako dowód.
3. Dla EmailLabs (A3) napisz procedurę: gdzie w panelu EmailLabs jest
   przełącznik śledzenia otwarć (piksel), jak sprawdzić że jest wyłączony
   DLA KONKRETNEGO szablonu/domeny używanej przez Kuking (nie globalnie —
   jeśli panel ma ustawienia per-domena/per-szablon, procedura MUSI to
   rozróżnić), oraz jak właściciel ma wysłać sobie samemu jeden testowy
   list i sprawdzić w źródle HTML czy piksela nie ma.
4. KONTROLA DODATNIA (wymagana część zadania): oba te obszary to sprawdzenia
   panelu, nie skany kodu — ale jeśli Twoja procedura każe właścicielowi
   użyć JAKIEGOKOLWIEK narzędzia lub skryptu (np. do sprawdzenia HTML maila
   pod kątem piksela, albo do odczytania publicznych ustawień bucketa przez
   `aws s3api`/`rclone` z kluczem tylko-do-odczytu), to Ty masz wcześniej
   pokazać, że to narzędzie/skrypt NAPRAWDĘ wykrywa czerwień: przygotuj
   lokalnie plik HTML z celowo wstrzykniętym pikselem (`<img src=... 1x1>`)
   i pokaż, że Twoje sprawdzenie go znajduje, oraz plik bez piksela, gdzie
   sprawdzenie milczy. W tym repozytorium obowiązuje zasada: "skan, który
   nie znajduje żadnego pliku, PRZECHODZI" — nie oddawaj takiego skanu.
5. Jeśli czegoś nie da się przygotować bez dostępu do panelu (np. nie da
   się z kodu ustalić obecnego stanu przełącznika) — napisz to WPROST jako
   pełnoprawny wynik: "nie da się tego ustalić bez X" + dokładnie czego
   brakuje. To NIE jest porażka zadania.
6. Oznacz każdą informację przejętą z issues/audytu jako
   `[pomiar cudzy: źródło]`; Twoje własne czynności (odczyt kodu, budowa
   i test skryptu wykrywającego piksel) pisz jako własny pomiar.

ZAKAZY (dodatkowo do zasad floty):
- Nie loguj się do żadnego panelu dostawcy, nie zgaduj/nie testuj haseł.
- Nie wysyłaj żadnej wiadomości e-mail do prawdziwych adresów ani przez
  produkcyjny EmailLabs.
- Nie łącz się z produkcyjnym bucketem R2 ani żadnym kluczem produkcyjnym.
- Nie pobieraj żadnych danych produkcyjnych.

WYNIK: plik `docs/audits/procedura-r2-emaillabs-<data>.md` w Twoim
worktree (NIE w repozytorium kanonicznym) z dwiema gotowymi procedurami
oraz dowodem kontroli dodatniej opisanym w punkcie 4. Zacommituj lokalnie.
Nie pushuj, nie otwieraj PR.
