Pracujesz nad portalem Kuking.pl (Laravel, Blade, PostgreSQL).

Katalog roboczy:  C:\Users\matma\Documents\kuking-flota\gpt-logowanie-haslem-lokalne
Gałąź:            gpt/logowanie-haslem-lokalne  (już istnieje, odgałęziona od main = 4c811cc7)
Repozytorium kanoniczne: C:\Users\matma\Documents\Codex\kuking.pl
  — tam wykonujesz WYŁĄCZNIE polecenia gita, nigdy nie zapisujesz plików.

NAJPIERW przeczytaj w całości:
  C:\Users\matma\Documents\kuking-flota\_wspolne\ZASADY_FLOTY.md
potem AGENTS.md w korzeniu swojego katalogu. AGENTS.md jest JEDYNYM
źródłem prawdy projektu; CLAUDE.md to tylko wskaźnik.

TWARDE ZAKAZY
- Nie pushujesz i nie otwierasz PR-ów. Pchanie idzie szeregowo przez
  kolejkę; dwa równoległe pchnięcia zderzają się na bazie i dają fałszywe
  porażki (142 failures, deadlocki — zdarzyło się dwa razy jednego dnia).
- Nie uruchamiasz `git worktree prune`. Z Windows każdy worktree spod
  /home/ wygląda na „prunable" i jedno polecenie wypruwa wszystkie naraz.
- Nie używasz `git reset --hard`, `--no-verify` ani gołego `git stash`
  (stos jest WSPÓLNY dla wszystkich stanowisk — użyj tymczasowego commita).
- PostgreSQL wyłącznie 127.0.0.1:55439, nigdy współdzielony 5432.
- Nie pracujesz w C:\Users\matma\Documents\Codex ani w podkatalogach.
- Nie wysyłasz wiadomości do ludzi ani instytucji.

ZASADY PROJEKTU, KTÓRE OBOWIĄZUJĄ ZAWSZE
- Bugfix = test regresyjny. Test MUSISZ zobaczyć NA CZERWONO przed
  poprawką — inaczej nie wiesz, czy mierzy to, co obiecuje.
- Testy chodzą na PostgreSQL, nie na SQLite.
- Poprawne dane użytkownika nigdy nie znikają: po błędzie walidacji
  formularz odtwarza to, co człowiek wpisał, a nie to, co stoi w bazie.
- Błędy i komunikaty PO POLSKU, mówiące CO ZROBIĆ, nie co się stało.
- UUID ani nazwa w adresie to NIE autoryzacja — każde wejście przez Policy.
- Nie zostawiaj martwych przycisków (D-053); i odwrotnie — działający
  endpoint bez drogi dla człowieka to ta sama usterka od drugiej strony.
- UX 50+: tekst >= 18 px, przyciski >= 48 px, bez hover i bez swipe.
- `status` i `role` użytkownika NIGDY w `$fillable`.
- Zmiana schematu = migracja + test + docs/DATABASE.md + plan wycofania.
  Wszystkie cztery, nie trzy.
- Przed zgłoszeniem gotowości: `vendor/bin/pint`.

ZANIM ZACZNIESZ KODZIĆ — ZMIERZ ZACHOWANIE NA NIETKNIĘTYM DRZEWIE.
Dziś w tym projekcie dwa zgłoszenia okazały się już naprawione, a jedno
straszyło jako blokada startu. Zamknięcie zgłoszenia z pomiarem jest
pełnoprawnym wynikiem i oszczędza czyjś tydzień.

JEŚLI COŚ OKAŻE SIĘ PYTANIEM PRODUKTOWYM, A NIE USTERKĄ — nie domykaj
tego asercją. Asercja zabetonuje zachowanie, którego nikt nie wybrał.
Wypisz warianty z kosztem jako decyzję dla właściciela.

URUCHAMIANIE TESTÓW (Git Bash na Windows; przedrostek MSYS_NO_PATHCONV=1
jest OBOWIĄZKOWY — bez niego ścieżka /mnt/c/... zostaje zepsuta):
  MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/c/Users/matma/Documents/kuking-flota/_wspolne/przygotuj-runtime.sh gpt-logowanie-haslem-lokalne
  MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/c/Users/matma/Documents/kuking-flota/_wspolne/testuj.sh gpt-logowanie-haslem-lokalne --filter TwojTest
Skrypt sam zakłada .env i APP_KEY. Po KAŻDEJ zmianie w kodzie uruchom go
ponownie, zanim odpalisz testy.

PUŁAPKI, NA KTÓRE KTOŚ JUŻ SIĘ NADZIAŁ
- Porażka poza Twoim obszarem NIE jest „zastana", dopóki jej nie
  odtworzysz. Trzy stanowiska dziś tak odpisały i dwa razy była to
  prawdziwa usterka oprzyrządowania. Jedyny znany wyjątek:
  ProbaOdtworzeniaTest pada, bo wszystkie stanowiska trafiają w tę samą
  bazę kuking_zrodlo_proby_glowny — wolno go pominąć, ale wymień z nazwy.
- Skan, który nie znajduje żadnego pliku, PRZECHODZI. Każdy nowy strażnik
  potrzebuje kontroli dodatniej na wzór `test_skan_naprawde_czyta_modele`.
- Pod `set -o pipefail` konstrukcja `printf '%s' "$X" | grep -q "$WZ"` daje
  warunek FAŁSZYWY mimo trafienia (SIGPIPE). Pisz `grep -qE "$WZ" <<< "$X"`.
- `<env>` w phpunit.xml bez `force` nie nadpisze prawdziwej zmiennej
  środowiskowej, ale nadpisze `.env`.
- `.gitignore` zaczyna się od `*.log` — dowody w .log wymagają `git add -f`.

JAK MELDUJESZ
Na koniec podaj: SHA commitów, co zmierzyłeś SAM (a co przejąłeś, z
adnotacją „[pomiar cudzy: źródło]"), czego NIE zrobiłeś i dlaczego, oraz
co wymaga decyzji właściciela. Commituj lokalnie, po polsku, w trybie
rozkazującym („Nie pozwól, by…", „Pokaż…", „Przywróć…").
Zgłoszenia czytaj przez: gh issue view NUMER --repo woogitsu/kuking.pl

------------------------------------------------------------------------
TWOJE ZADANIE
------------------------------------------------------------------------
Dziś za "logowanie działa" uchodzi coś, czego nikt faktycznie nie
przeprowadził od początku do końca. Audyt `WERYFIKACJA_713_492_2026_09_20.md`,
pozycja C4 (#697), mówi to wprost: "poprawka wdrożona, pełne logowanie
nadal nieudowodnione". Issue #697 jest CLOSED, jest commit `85ef2c15`,
własny GET audytu potwierdził CSP/widget/200 na produkcji — ale cytowany
w audycie komentarz do #697 "wprost wyklucza logowanie na konto".
`PolitykaCspDopuszczaPowrotTurnstileTest` pilnuje wyłącznie nagłówka CSP,
nie tego, czy człowiek faktycznie może się zalogować hasłem przez
Turnstile do działającej sesji. To jest różnica między "strona się
renderuje" a "logowanie działa".

To DA SIĘ sprawdzić lokalnie, pod warunkiem że zrobisz to na środowisku
DEWELOPERSKIM z testowymi kluczami Turnstile (Cloudflare udostępnia
oficjalne testowe site key/secret key, które zawsze przepuszczają — to
nie jest obchodzenie zabezpieczenia, tylko standardowy tryb testowy
dostawcy), NIGDY przeciw produkcyjnemu Turnstile i NIGDY próbą jego
ominięcia. Jeśli w tym repozytorium nie ma skonfigurowanych testowych
kluczy Turnstile, napisz to wprost jako brak i zatrzymaj się na tym
etapie zamiast próbować czegokolwiek innego.

CO MASZ ZROBIĆ:

1. Przeczytaj: sekcję C4 audytu, issue #697 i wskazany w audycie komentarz
   (`gh issue view 697 --repo woogitsu/kuking.pl`, tylko odczyt), kod
   `PolitykaCspDopuszczaPowrotTurnstileTest`, kontroler logowania i
   middleware/regułę walidacji Turnstile.
2. Sprawdź w `.env.example`/konfiguracji, czy projekt przewiduje testowe
   klucze Turnstile do środowiska deweloperskiego (Cloudflare publikuje
   oficjalne testowe klucze pod https://developers.cloudflare.com/turnstile/troubleshooting/testing/
   — zawsze-przepuszczający i zawsze-blokujący). Jeśli takich kluczy nie
   ma skonfigurowanych w projekcie, NIE dodawaj ich samodzielnie do
   `.env` produkcyjnego ani nie próbuj używać prawdziwego sekretu — opisz
   to jako brakujący element procedury dla właściciela i zakończ na tym tę
   część zadania.
3. Jeśli klucze testowe są dostępne: uruchom lokalny runtime
   (`przygotuj-runtime.sh gpt-logowanie-haslem-lokalne`), utwórz TESTOWE
   konto z hasłem w lokalnej bazie (nie produkcyjnej), i przejdź CAŁĄ
   ścieżkę: formularz logowania -> Turnstile (testowy, zawsze
   przepuszczający) -> wysłanie hasła -> sprawdzenie, że sesja jest
   faktycznie ustawiona (np. dostęp do strony wymagającej zalogowania,
   obecność poprawnego cookie sesji) -> wylogowanie.
4. Osobno przetestuj wariant z BŁĘDNYM hasłem i wariant z testowym kluczem
   Turnstile "zawsze blokującym" — sprawdź, że komunikat błędu jest po
   polsku i mówi, co zrobić (zgodnie z zasadami UX projektu), a poprawnie
   wpisane dane (np. e-mail) nie znikają z formularza.
5. KONTROLA DODATNIA: pokaż, że Twój test rzeczywiście odróżnia "działa"
   od "nie działa" — np. tymczasowo zepsuj middleware sesji albo podaj
   złe dane i potwierdź, że wtedy scenariusz z punktu 3 faktycznie
   kończy się niepowodzeniem (a nie fałszywym zielonym wynikiem). Dopiero
   potem przywróć kod i pokaż przejście na nietkniętym drzewie.
6. Napisz wynik jako jasne TAK/NIE dla pytania "czy pełne logowanie hasłem
   przez Turnstile działa lokalnie" wraz z dokładnym opisem kroków i
   dowodami (np. treść cookie sesji bez wrażliwych wartości, kod HTTP,
   zrzut logu testu). Jeśli coś nie zadziała — opisz dokładnie gdzie i
   dlaczego, to pełnoprawny wynik.
7. Zaznacz wyraźnie: ten test NIE zastępuje C4 na produkcji — audyt mówi
   wprost, że nieudowodnione jest logowanie NA PRODUKCJI przez prawdziwy
   Turnstile. Napisz osobno, jaka byłaby minimalna, bezpieczna procedura
   dla właściciela do jednorazowego sprawdzenia tego na produkcji (bez
   przekazywania Ci jego hasła ani sesji), skoro Ty tego wykonać nie
   możesz i nie powinieneś.

ZAKAZY (dodatkowo do zasad floty):
- Nie próbuj ominąć ani zautomatyzować prawdziwego (produkcyjnego)
  Turnstile w jakikolwiek sposób.
- Nie testuj na produkcji, nie używaj prawdziwego konta ani prawdziwego
  hasła właściciela.
- Nie proś użytkownika o podanie hasła w tej sesji.

WYNIK: plik `docs/audits/logowanie-haslem-turnstile-lokalnie-<data>.md`
w Twoim worktree z wynikiem TAK/NIE, dowodami, kontrolą dodatnią oraz
osobną krótką procedurą dla właściciela do sprawdzenia na produkcji.
Zacommituj lokalnie po polsku. Nie pushuj, nie otwieraj PR.
