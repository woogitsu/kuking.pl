# Audyt przed kolejką — przebieg 3 (noc 20/21 września)

**21 września 2026, 07:54.** Audytor doradczy. **Nie poprawiono kodu, nie commitowano,
nie pchano, nie otwierano PR-ów, nie ruszano kolejki, nie komentowano w PR-ach.**
Podstawa: `AUDYT_PRZED_KOLEJKA.md`, punkty 1–9. Numeracja znalezisk ciągnie się
z przebiegu 2 (`AUDYT-2026-09-20-2042.md`), gdzie skończyła się na Z17.

Zakres: 53 gałęzie pchnięte między 20.09 21:17 a 21.09 07:15. Praca odczytem —
nie uruchomiono ani jednej baterii testów (`load average` 3,5, ale zlecenie i tak
tego zabrania). Wszystko poniżej pochodzi z `git diff` wobec `merge-base` z
`origin/main`, lektury raportów w `docs/` i lektury plików dowodowych w drzewie.

---

## CZĘŚĆ 0 — korekta zakresu, którą trzeba przeczytać przed resztą

### Z18 — `pchniete.txt` TYM RAZEM NIE zawyżał, ale zaniża o cztery gałęzie

Poprzedni audyt (Z12) ustalił, że `pchniete.txt` miesza „pchnięte” z „pominięte”.
**Dziś tej wady nie widać** — `docs/flota/kolejka11.sh:63-70` (gałąź
`flota/stan-sesji-2`) dopisuje do listy dopiero PO porównaniu SHA lokalnego
ze zdalnym. To jest poprawka poprzedniego znaleziska i działa.

Rozliczenie stanu na 07:54:

| | liczba |
|---|---|
| gałęzi na GitHubie | **114** (zlecenie mówiło 111 — kolejka dorzuciła trzy w trakcie) |
| wpisów w `pchniete.txt` | 52 |
| w `pchniete.txt`, brak na GitHubie | **1** — `codex/audyt-ux50plus` |
| pchniętych tej nocy wg dat, brak w `pchniete.txt` | **4** |

- `codex/audyt-ux50plus` **nie jest zgubą**: PR **#918 MERGED**, gałąź skasowana
  po scaleniu. Lista mówi prawdę.
- Brakujące na liście, a pchnięte: **`flota/zdjecia-formularze`,
  `naprawa/kontrola-dodatnia-do-przodu`, `naprawa/proba-odtworzenia-w-ci`,
  `robota/bazy-stanowisk`**. Trzy z nich to gałęzie naprawcze. Kto planuje
  kolejność PR-ów z `pchniete.txt`, **pominie je**.

### Z19 — tylko 4 z 53 gałęzi mają PR; 49 czeka

`flota/dsa-odwolania` → **#914 MERGED** (scalona squashem — jej treść jest już
w `main`, wbrew temu, że gałąź nie jest przodkiem `main`; zostały 6 commitów
rozjazdu). `flota/gotowanie` → **#913 OPEN**, `flota/zdjecia-formularze` →
**#923 OPEN**, `robota/bazy-stanowisk` → **#920 OPEN**. Pozostałe **49 bez PR-a**.

### Z20 — znana pułapka „gałąź sprzed 20.09 ma stary `ci.yml`” NIE dotyczy dziś nikogo

Sprawdzone dla wszystkich 53: bazy to `4c811cc7` (20.09), `534e0a51` (20.09)
albo `e65f297f` (21.09). Żadna nie jest sprzed 20.09, więc czterech czerwieni
`PortMarkiMaWlasnaBramkeCiTest` z tego powodu nie będzie. **To wynik negatywny
i jest dobrą wiadomością** — nie trzeba tego sprawdzać przy każdym PR-ze.

### Z21 — metodyczne ograniczenie, które unieważnia punkt 1 karty dla 40 gałęzi

Czterdzieści gałęzi `gpt-*`, `gemini-*` i `bramka-startowa` ma **jeden zbiorczy
commit** „Odzyskaj prace stanowiska … po awarii repozytorium”, którego treść
mówi wprost: *„historii posrednich commitow, ani wynikow testow — nic tu nie bylo
uruchamiane”*. **Kolejność „test przed poprawką czy po” jest dla nich
nierozstrzygalna z historii.** Jedynym dowodem czerwieni jest raport w `docs/`.
Dlatego poniżej „brak raportu” traktuję jako pełnoprawne znalezisko z punktu 1 —
przy tej historii nie ma niczego innego do obejrzenia.

---

## CZĘŚĆ 1 — znaleziska przekrojowe (najdroższe, bo dotyczą wielu gałęzi naraz)

### Z22 — WAGA WYSOKA — sześć gałęzi wnosi strażnika JS, którego NIC nie uruchamia

Zlecenie kazało szukać wzoru „strażnik bez zmiany, której pilnuje”. **Tego wzoru
nie ma** — wszystkie sześć zespołów audytowych zgodnie stwierdziło, że każdy nowy
test PHPUnit pokrywa realną zmianę w `app/`/`resources/`. Sześć razy wyszedł
natomiast wzór **odwrotny i groźniejszy: strażnik jest, pilnuje właściwej rzeczy
— i nigdy nie pobiegnie.**

W tym repozytorium **jedyną drogą, którą CI uruchamia `node --test`, jest
`npm run build`** (`.github/workflows/ci.yml:798, 955, 1051, 1160, 1335`; poza tym
tylko zahardkodowany `node --test scripts/port-grupy.test.mjs`). Lista plików
w `package.json` → `"build"` jest zamknięta i **żadna gałąź poza `flota/gotowanie`
jej nie rusza**:

| gałąź | nowy plik | w `build`? |
|---|---|---|
| `gpt-ustawienia-profilu` | `scripts/kopiowanie-adresu.test.mjs` | **NIE** |
| `gpt-ustawienia-profilu` | `scripts/wyglad-nawigacja.test.mjs` | **NIE** |
| `gpt-rozbicie-uslug` | `scripts/railway-roles.test.mjs` | **NIE** |
| `gpt-skladniki` | `scripts/tagi-potwierdzenie.test.mjs` | **NIE** |
| `flota/zdjecia-formularze` | `scripts/podglad-object-url.test.mjs` | **NIE** |
| `gpt-zalegle` | `scripts/offline-ponowienie.test.mjs` | **NIE** |
| `gpt-obciazenie` | `korpus-605.test.mjs`, `probnik-605.test.mjs` | **NIE** (wpięte w `przyrzad-605.test.mjs`, który sam też nie jest w `build`) |
| `flota/gotowanie` | `minutnik-krok.test.mjs`, `wake-lock-gotowania.test.mjs` | **TAK** |

Skutek: regresja w `resources/js/tagi-w-opisie.js`, w podglądzie `object URL`,
w ponowieniu offline i w nawigacji ustawień **wróci bez jednej czerwieni**.
Dwie gałęzie posuwają się dalej i **twierdzą w raporcie, że są w CI**:

- `[pkt 2+8] docs/SKLADNIKI_PORCJE_TAGI_878_741_750_754.md` (gpt-skladniki) —
  „Test DOM w Chromium jest w CI”, „nowy krok znajduje się dokładnie raz,
  w zadaniu `assets`”. Gałąź nie rusza ani `package.json`, ani `.github/`.
- `[pkt 2+8] docs/design/USTAWIENIA_PROFILU_801_804.md:109` (gpt-ustawienia-profilu) —
  „Testy JS: `npm run test:ustawienia-profilu`; podłączone do zadania assetów CI”.
  Takiego skryptu nie ma w `package.json` na tej gałęzi.

**To najtańsza naprawa w całym audycie:** jedna linia w `package.json` na gałąź.
Ale uwaga — patrz Z23: ta linia jest polem minowym.

### Z23 — WAGA WYSOKA — linia `build` w `package.json` to dokładnie pułapka z punktu 7 karty

`flota/gotowanie` **konfliktuje z `origin/main` na `package.json`** (sprawdzone
`git merge-tree`). Obie strony dopisują do tej samej linii `"build"`:

```
main:            … tagi-w-opisie.test.mjs resources/js/licznik-znakow.test.mjs && vite build
flota/gotowanie: … tagi-w-opisie.test.mjs resources/js/minutnik-krok.test.mjs resources/js/wake-lock-gotowania.test.mjs && vite build
```

Rozwiązanie „biorę swoją wersję” po stronie gałęzi **wypisuje
`licznik-znakow.test.mjs` z bramki budowania** i nic nie zapali się na czerwono —
`licznik-znakow.js` ma wtedy zero pokrycia. Ten sam plik wnosi zresztą
`gpt-n1-powiadomienia` (`resources/js/app.js`: `+import './licznik-znakow.js'`).
**Scalać ręcznie, sumując listy.** To samo dotyczy każdej gałęzi z Z22, gdy
zacznie dopisywać swój plik do tej linii — po dwóch takich gałęziach konflikt
jest pewny.

### Z24 — WAGA WYSOKA — cztery gałęzie podbijają wersję na `Alfa 0.68`, żadna nie pisze w `CHANGELOG.md`

`config/kuking.php:2761` — `'etykieta' => 'Alfa 0.67'` → `'Alfa 0.68'` w:
**`gpt-zdjecia-publikacja`, `gpt-ugotowalem-dostep`, `gpt-zdjecia-limity`,
`gpt-zalegle`**. Zmiana jest identyczna co do znaku, więc git scali pierwszą
czysto, a przy pozostałych trzech zgłosi konflikt jednej linii, który „rozwiąże
się” wzięciem swojej wersji — **bez żadnej czerwieni**. Po czterech scaleniach
numer `0.68` opisuje cztery różne wydania, czyli nie opisuje żadnego.

Gorzej: `config/kuking.php:2758-2760` zawiera regułę w tym samym pliku, trzy
linie wyżej: *„KAŻDE PODBICIE CYFRY MA WPIS W `CHANGELOG.md` — jedno pilnuje
drugiego. Wersja bez wpisu jest numerem bez treści”*. **Żadna z czterech gałęzi
nie dotyka `CHANGELOG.md`** (najwyższy wpis to nadal `## Alfa 0.67`).
`git grep CHANGELOG origin/main -- tests` → **pusto**: reguły nie pilnuje nic
poza komentarzem. `[pkt 8 — obietnica bez pokrycia, pkt 2 — reguła bez strażnika]`

**Decyzja właściciela:** kto dostaje `0.68`, a kto `0.69`+; albo czy podbicia
cofnąć i zrobić jedno na koniec serii scaleń.

### Z25 — WAGA WYSOKA — dwie sprzeczne decyzje produktowe scalą się bez konfliktu i bez czerwieni

`flota/gotowanie`, commit `77b6d71a` (#764), **dodaje** w
`resources/views/pages/recipes/cooking.blade.php` dopisek „— do smaku”:

```blade
@if($ingredient->no_amount && ! str_contains(mb_strtolower($ingredient->ingredient_text), 'do smaku'))
    <span class="meta"> — do smaku</span>
```

`gpt-skladniki` (#878) **usuwa dokładnie ten sam blok** z
`resources/views/pages/recipes/show.blade.php` — **na jawne polecenie
właściciela**, z zapisem w `docs/DECISIONS.md` (D-033) i `docs/DATABASE.md`.

Pliki są rozłączne → git nie zgłosi konfliktu, oba zestawy testów przejdą,
a produkt dostanie „sól do smaku — do smaku” w trybie gotowania i czysty tekst
na stronie przepisu. `[pkt 4 + pkt 7]` **Rozstrzygnąć PRZED scaleniem
którejkolwiek z dwóch.**

### Z26 — WAGA ŚREDNIA — `scripts/check.sh`: kolizja dwóch gałęzi, w której „biorę swoją” niszczy cudzą sondę

`gpt-harmonogram` wstawia `elif ! bash tests/skrypty/cache-assetow.sh` **przed**
wywołaniem `kontrola-ujemna`; `gpt-zalegle` wstawia `elif ! bash
tests/skrypty/check-postgres.sh` **zaraz po**. Hunki stykają się kontekstem, więc
git zgłosi konflikt — ale wzięcie którejkolwiek wersji zostawia w drzewie
osierocony skrypt sondy, którego już nikt nie wywołuje, **a pełny zestaw testów
jest zielony, bo kompletności łańcucha `elif` w `check.sh` nie pilnuje nic**.
`[pkt 7]` **Scalać ręcznie, zachowując OBA `elif`.**

### Z27 — WAGA ŚREDNIA — `gpt-zalegle` wpisuje port klastra floty na sztywno do wspólnej bramki

`scripts/check.sh` (gałąź `gpt-zalegle`) zamienia sondę PostgreSQL na twarde:

```sh
if [ "$DB_HOST" != 127.0.0.1 ] || [ "$DB_PORT" != 55439 ]; then
    zle "Ustaw DB_HOST=127.0.0.1 i DB_PORT=55439 dla lokalnego odbioru."; exit 1
```

`scripts/check.sh` jest **wspólną, reklamowaną w trzech workflow lokalną bramką**
(`deploy.yml:5`, `preview.yml:5`, `railway-iac.yml:5`: „Kontrolę robimy lokalnie,
za darmo: ./scripts/check.sh”). Po scaleniu każdy, kto nie ma akurat wyeksportowanych
tych czterech zmiennych i nie siedzi na klastrze 55439, dostanie `exit 1` **zamiast
kontroli** — łącznie z pozostałymi stanowiskami floty i z każdym, kto wejdzie do
repozytorium później. Usunięto też automatyczny start klastra. `[pkt 7 + pkt 8]`

### Z28 — WAGA NISKA — pułapka SIGPIPE w skrypcie samej kolejki pchania

`docs/flota/kolejka11.sh:12` ma `set -uo pipefail`, a `:37`:

```sh
psql -d postgres -tc "SELECT 1 FROM pg_database WHERE datname='kuking_flota_push'" | grep -q 1 || createdb kuking_flota_push
```

To dokładnie konstrukcja, którą **ten sam plik zakazuje w komentarzu na linii ~95**
(„Nigdy `printf '%s' "$x" | grep -q "$y"`”). Wyjście jest jednolinijkowe, więc
SIGPIPE trafia się rzadko — ale gdy trafi, warunek „baza istnieje” odpowie
fałszywie i poleci `createdb` na istniejącej bazie. Ma być here-string.
`[pkt 9]` Awaria jest głośna (błąd `createdb`), nie cicha — stąd waga niska.

### Z29 — pola dowodowe `przywrocenie: "nie wykonane"` we wszystkich JSON-ach kontroli

Wspólny przyrząd `scripts/kontrola-ujemna.sh` zapisuje JSON **przed** trapem
przywracającym plik, więc **najważniejsze pole dowodu jest zawsze nieprawdziwe**.
Opisało to niezależnie co najmniej sześć raportów, więc **nie liczę tego jako
znalezisko żadnej gałęzi** — ale przyrząd warto poprawić, bo dziś każdy czytelnik
musi wiedzieć o tej wadzie, żeby nie odrzucić dobrego dowodu.
`gpt-kontakt-panel` naprawił w tym samym pliku pułapkę SIGPIPE (pkt 9) i dołożył
test regresyjny — warto poprawić i to pole przy okazji.

---

## CZĘŚĆ 2 — gałąź po gałęzi

Format: **Zakres** / **Znaleziska** (`[pkt N] plik:linia — skutek`) albo jawne
„Nic nie znalazłem” + co sprawdzono / **Werdykt**.

### flota/dsa-odwolania — PR #914 JUŻ SCALONY
Zakres: cztery poprawki DSA (ponowione zgłoszenie, zaległe potwierdzenie
w transakcji, ekran po terminie, ekran zgłaszającego) + osłabienie miernika panelu.
Znaleziska:
- `[pkt 2] scripts/panel-validation-snapshot.php:117-155` — zmiana wyłącza
  `ostatnio_widziany_at` i `pwa_prompt_state` ze zrzutu `users`, czyli **osłabia
  miernik wdrożenia**, a komentarz deklaruje, że kontrola dodatnia „jest w tym
  samym teście”. W teście jej nie ma:
  `tests/Feature/Sledztwo914PomiarPanoluOdwolanTest.php:270-298` sprawdza tylko
  `assertSame([], $this->roznice(...))`.
- `[pkt 2/3] tests/Feature/Sledztwo914PomiarPanoluOdwolanTest.php:255-268` —
  docblock odsyła po dowód czerwieni do `scripts/sledztwo-914-kontrola-dodatnia.sh`
  i do „raportu zadania”. **Sprawdzone: ani tego skryptu, ani żadnego raportu tej
  gałęzi nie ma w drzewie** (`git ls-tree -r origin/flota/dsa-odwolania` → jedyne
  trafienie to sam plik testu). Nazwa testu obiecuje drugą połowę
  („…ale prawdziwy zapis nadal by wykryto”), której ciało nie wykonuje.
Werdykt: **PO FAKCIE** — scalona. Znalezisko zostaje jako dług: miernik wdrożenia
jest dziś w `main` słabszy niż wczoraj i nikt tego nie udowodnił.

### gpt/ocr-import
Zakres: projekt (bez kodu) OCR zeszytów i importu przepisu z URL.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: jedyny plik
`docs/product/PROJEKT_OCR_IMPORT_28.md`, oznaczanie cudzych pomiarów
(`[pomiar cudzy: …]`), §11 z własnymi liczbami (4393 passed / 83 692 asercje,
Pint 1155 plików), jawne wyliczenie tego, czego nie wykonano. Brak kodu → pkt 2, 5,
6 nie mają zastosowania.
Werdykt: **GOTOWA NA PR.**

### gpt/tokeny-zaproszen
Zakres: `shouldSend` dla `UstawienieNowegoHasla` i `ZaproszenieDoZalozeniaKonta` —
kolejka nie wysyła listu z tokenem, który już wygasł.
Znaleziska:
- `[pkt 4] app/Notifications/UstawienieHaslaZamiastLinku.php:89-135` — bliźniacza
  klasa dziedziczy po tym samym `ResetPassword`, jest `ShouldQueue`, dostaje **ten
  sam token brokera** (`app/Domain/Security/WyslijOdzyskanieKonta.php:210`) — i
  **została nietknięta**: brak `shouldSend`, a `waznosc($minut)` dalej czyta
  konfigurację w chwili wykonania zadania. Skutek identyczny z naprawianym: martwy
  link w skrzynce i zdanie o ważności liczone od startu workera. Raport wymienia
  jako niepoprawione tylko logowanie linkiem — tej klasy nie wspomina.
  (Potwierdzone: gałąź zmienia tylko `UstawienieNowegoHasla.php` i
  `ZaproszenieDoZalozeniaKonta.php`.)
Sprawdzono bez zastrzeżeń: raport podaje czerwień z liczbami (16 porażek, 52
asercje) i przyczyną; uzupełnienia czterech istniejących testów są konieczne,
nie osłabiające.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — reguła w dwóch z trzech miejsc.

### gpt/dziennik-wyjatkow
Zakres: cztery granice moderacji logują klasę wyjątku zamiast `getMessage()`.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: `app/Moderacja/ExceptionContext.php`
i cztery miejsca wywołania, `ModeracjaBezTresciWyjatkowTest` (asercja
`assertCount(1, $this->records)` — brak ostrzeżenia też oblewa), cztery
zacommitowane JSON-y kontroli ujemnych z `md5_przed`/`md5_po_mutacji` i
`oczekiwana_przyczyna`, aktualizacja `docs/legal/SYGNALY_AUTOMATU.md` §8.6.
Raport podaje czerwień (4 FAIL, 1 PASS, 26 asercji) z przyczyną każdej.
Werdykt: **GOTOWA NA PR** — strażnik pilnuje dokładnie tego, co gałąź wnosi.

### gpt/zawieszone-konto
Zakres: decyzja właściciela #926 — zawieszone konto zachowuje nazwane czynności
prywatne; formularze pytają politykę przed wyświetleniem.
Znaleziska:
- `[pkt 1] docs/product/ZAWIESZENIE_926.md` i `…_ODTWORZENIE.md` — 15 kontroli
  ujemnych opisanych jako „PASS → FAIL z właściwego powodu → PASS”, z logami
  w `gpt-zawieszone-konto-PLIKI/output/naprawa-926/`. **Żaden dowód ani nazwa
  oblanej asercji nie trafiły do repozytorium.** Przy historii z Z21 nie da się
  z gałęzi rozstrzygnąć, co było czerwone.
- `[pkt 2, niski] app/Policies/UserPolicy.php:47-68` — memoizacja blokad
  w `request()->attributes` obowiązuje tylko dla GET. Dziś bez skutku (bramka
  `follow` wołana wyłącznie z HTTP), ale wywołanie z workera dałoby pamięć
  nieodświeżalną.
Sprawdzono bez zastrzeżeń: lista wyjątków `EnsureAccountIsActive` to osiem
konkretnych nazw tras (nie wzorzec); `cooking.reset` ma trasę, przycisk
z potwierdzeniem i wiersz w macierzy pięciu ról; test pilnuje, że po błędzie
walidacji wpisany opis wraca do formularza.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — dołożyć dowody albo wypisać w raporcie,
co i jak padło. Kod i testy same w sobie mocne.

### gpt-dlug-weryfikacyjny
Zakres: audyt 19 pozycji długu #713 i luk #492 — bez zmian w aplikacji.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: konsekwentne rozdzielenie „własny
pomiar” / „odczyt kodu” / `[pomiar cudzy: źródło]` w każdym wierszu tabel A1–D8d;
`docs/audits/evidence/dlug-713-492/README.md` z poleceniami każdego dowodu
(z jawnym `MSYS_NO_PATHCONV=1`); zgodność `testy.txt` z deklaracją
(4393 passed / 83 692 asercje, 508,30 s); niepusty `paginatory.txt` (38 trafień)
z zastrzeżeniem o zakresie skanu. Sekcja „decyzje i rozjazdy dowodów” sama wytyka
cudze „zmierzono” bez pomiaru — to autor zgłasza, nie audyt.
Werdykt: **GOTOWA NA PR.**

### gpt-tagi
Zakres: siedem usterek obserwowania tagów (#853–#859, #861) + pomiar do #858.
Znaleziska:
- `[pkt 3, drobne] docs/research/OBSERWOWANIE_TAGOW_2026-09-20.md:3` — nagłówek
  deklaruje „Stan początkowy: `534e0a51`, gałąź `gpt/tagi`”, a gałąź nazywa się
  `gpt-tagi` i siedzi na `4c811cc7`. Pomiary zrobiono wobec innej bazy niż ta,
  która idzie do PR-a. Commit różnicy nie dotyka tagów, więc skutku dla wyniku
  nie widzę — ale deklaracja SHA jest nieaktualna.
Sprawdzono bez zastrzeżeń: czerwień z liczbami (14 porażek, 4 przejścia), siedem
kontroli ujemnych po jednej na zgłoszenie, każda z porównaniem MD5/mtime
i czyszczeniem skompilowanych widoków; ograniczenie `UpdateTagFollows::save()`
opisane przez autora (kolumna `timestampTz` bez precyzji — potwierdzone
`database/migrations/2026_09_07_100000_create_tags_tables.php:216`).
Werdykt: **GOTOWA NA PR** po sprostowaniu SHA w nagłówku raportu.

### gpt-2fa-ustawienia
Zakres: cztery usterki ustawień 2FA (#811, #875, #876, #877).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: czerwień osobno dla każdego
zgłoszenia; pięć kontroli ujemnych PHPUnit + jedna przeglądarkowa z zacommitowanymi
`ujemna-*.json`; `scripts/fixtures/ustawienia-2fa-kontrole.sh` używa `grep -qF`
na pliku (bez pułapki z pkt 9), ma `set -Eeuo pipefail` i `view:clear` przed
każdym etapem; zgodność wsteczna starych kodów `XXXX-XXXX` potwierdzona w kodzie
(`TwoFactorAuthenticator.php:202-231`) i pokryta testem; zmiany w
`error-summary.blade.php`/`field.blade.php` zachowują domyślny `errorBag`.
Przejście z `assertSessionHasErrors` na `assertSessionHasErrorsIn` to zaostrzenie.
Uwaga bez skutku: raport, jak `gpt-tagi`, deklaruje bazę `534e0a51`.
Werdykt: **GOTOWA NA PR.** *(Konfliktuje z `main` na `field.blade.php` — patrz
tabela konfliktów.)*

### gpt-dr-baza
Zakres: lokalna próba odtworzenia bazy na 5,5 mln wierszy (#594/#193).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: `scripts/dr594-kontrole.sh` ma
prawdziwą parę dodatnią/ujemną (zgodność → `DELETE 1` → wymagany kod 63 →
przywrócenie → ponowna zgodność), `trap cleanup EXIT`, bramki na nazwę bazy/host/
port/rolę, `grep -q` na pliku a nie w potoku; `scripts/dr594-pomiar.py:76-78` ma
jawne `if len(tables) < 40: raise`, czyli **zabezpieczenie przed pustą listą
wejściową wprost z punktu 2**; §6 uczciwie zgłasza, że surowe `pg_dump --schema-only`
nie były identyczne, i wyjaśnia normalizację; produkcyjne RPO/RTO jawnie
niezmierzone.
Werdykt: **GOTOWA NA PR** — najlepiej udowodniony pakiet pomiarowy tej nocy.

### gpt-redis-ha
Zakres: materiał decyzyjny #603/#604 (Redis vs PostgreSQL HA); zero zmian w `app/`.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: raport podaje nazwaną czerwień
(864 porażki z własnego błędu `APP_ENV`, potem 2 porażki
`DokumentyMdNieMajaMartwychOdnosnikowTest` z cytatem asercji) i jawnie mówi, że
pełnego zestawu po poprawkach dokumentacyjnych nie powtarzano; `probe.php check`
ma kontrolę dodatnią i ujemną (`guard-positive.txt`, `guard-negative.txt`, kod
odmowy 2); cudze liczby oznaczone `[pomiar cudzy: …]`; `verify.sh` bez potoków
do `grep`; gałąź sama deklaruje „kod aplikacji niezmieniony… brak podstaw do
zamknięcia #603/#604”.
Werdykt: **GOTOWA NA PR** jako pakiet dokumentacyjno-pomiarowy, nie wdrożenie.

### gpt-openai-granice
Zakres: granica wymiarów zdjęcia do OpenAI (#912), ponowna analiza po edycji
komentarza (#909), atomowe usunięcie komentarza (#911).
Znaleziska:
- `[pkt 4] app/Jobs/PrzeanalizujTresc.php + app/Http/Controllers/PostController.php`
  — reguła „edycja tekstu idzie ponownie do oceny” wdrożona **wyłącznie dla
  komentarza** (`CommentController::update` woła `PrzeanalizujTresc::dlaKomentarza()`).
  Edycja wpisu/pytania (`PostController::update` → `EditPost`) nadal nie zleca
  ponownej analizy. Tekst podmieniony po publikacji na spam **we wpisie** nie
  trafia do moderacji. Raport nie oznacza tej ścieżki jako świadomej granicy.
- `[pkt 7] app/Http/Controllers/CommentController.php` — plik przepisuje też
  `gpt-ugotowalem-dostep` (inaczej: `recoverExpiredEdit` + flash sesji). Naiwne
  scalenie zgubi jedną z dwóch poprawek.
Nie zgłaszam (autor opisał sam): brak audytu produkcyjnych metadanych zdjęć,
ograniczenia testu przeplotu, kolizja z `gpt-moderacja-ai`.
Sprawdzono bez zastrzeżeń: raport podaje CO padło i z jakim komunikatem dla ~10
czerwieni; sześć kontroli PASS→FAIL→PASS z `wyniki.json` i potwierdzeniem MD5+mtime.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### gpt-zdjecia-publikacja — ZOBACZ TEŻ Z30 NIŻEJ
Zakres: ponowienie publikacji nie wgrywa zdjęć drugi raz (#873), zdjęcia
„Ugotowałem” przeżywają błąd walidacji (#872), wspólne `RecoveredFormPhotos` (#871).
Znaleziska:
- `[pkt 4] app/Http/Controllers/PostController.php:491 (zebranZdjecia)` — raport
  twierdzi: „Własność, usunięcie oraz **wszystkie istniejące odwołania** do mediów
  są sprawdzane przed odtworzeniem. Dotyczy wpisu, pytania i…”. W rzeczywistości
  `RecoveredFormPhotos` (pełna lista `KasujZdjecie::ODWOLANIA` + `status != deleted`)
  użyto w widoku i w `CookedEventController::store`, ale ścieżka, która **naprawdę
  przypina zdjęcia do wpisu/pytania**, dalej filtruje tylko `owner_id` +
  `whereDoesntHave('posts')`. Podstawione `media_ids[]` z własnym awatarem,
  `hero_media_id` przepisu, zdjęciem kroku albo mediami oznaczonymi jako skasowane
  przejdą do nowego wpisu. Obietnica raportu bez pokrycia w kodzie.
- `[pkt 7 + 8] config/kuking.php:2761` — `Alfa 0.68` bez wpisu w `CHANGELOG.md`
  (patrz Z24).
Sprawdzono bez zastrzeżeń: nazwane czerwienie przed poprawką (dodatkowe pliki po
ponowieniu, HTTP 500 przy nie-UUID, kotwica `f-photos-0` bez celu); pięć mutacji
PASS→FAIL→PASS z potwierdzeniem MD5/mtime; autor sam zamienił `printf | grep -q`
na here-string we własnym oprzyrządowaniu.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### Z30 — WAGA WYSOKA — `gpt-zdjecia-limity` zawiera CAŁĄ `gpt-zdjecia-publikacja`

Sprawdzone plik po pliku: **zbiór plików poza `docs/` unikalnych dla
`gpt-zdjecia-publikacja` jest PUSTY.** Wszystkie 12 wspólnych plików są identyczne
co do treści na obu gałęziach, z wyjątkiem `cooked/create.blade.php` i
`posts/create.blade.php`, gdzie wersja `gpt-zdjecia-limity` to wersja
`gpt-zdjecia-publikacja` **plus** atrybuty limitu. `gpt-zdjecia-limity` wnosi
ponadto `app/Support/LimityZdjec.php`, `app/Models/Profile.php`,
`resources/css/ekran-dodawania.css`, `resources/js/app.js`,
`settings/avatar.blade.php`, `scripts/zdjecia-limity.mjs`,
`tests/Feature/LimityIPodgladZdjecTest.php`.

**Jeden PR na `gpt-zdjecia-limity` dostarcza obie gałęzie. To ~65 minut
oszczędności i jeden konflikt mniej na `config/kuking.php`.** Wymaga potwierdzenia
przez koordynatora, że raporty obu zgłoszeń mają iść razem.

### gpt-kontakt-formularz
Zakres: czyszczenie `page_path` (#836), potwierdzenia odporne na odświeżenie (#842),
adres gościa jako część tożsamości ponowienia (#838), instrukcja zmiany adresu (#837).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: raport wymienia liczbę i przedmiot
każdej czerwieni (5 dla #836, 7 dla #842/#838/#837, 3 dla zgłoszenia nielegalnej
treści) i przyznaje, że wcześniejszy pełny przebieg miał 3 porażki własnych zmian;
siedem kontroli ujemnych; pomiar #841 ma mianownik (100 wiadomości, 4 GET listy,
88/93 GET szczegółów) i nie jest przenoszony na produkcję; osiem istniejących
testów przeszło z odczytu flash na odczyt ekranu — asercje **mocniejsze**;
`status` poza `$fillable`; klucz `potwierdzenie` w URL nie daje dostępu bez własnej
sesji (`FormConfirmation::active()` wiąże wpis z `user_id`); `tests/Measurements/`
poza suitą `Feature` i opisane jako pomiar, nie kontrakt.
Werdykt: **GOTOWA NA PR** — najlepiej udokumentowana gałąź produktowa tej nocy.

### gpt-ugotowalem-dostep
Zakres: „Ugotowałem” pod `RecipePolicy::cook` (#902), odzyskanie własnej poprawki
komentarza po 15 minutach (#910), reset odhaczeń kroków (#903).
Znaleziska:
- `[pkt 4] resources/views/components/post-card.blade.php:420` — **potwierdzone
  odczytem**: trzecie i najczęściej oglądane wejście do „Ugotowałem” (karta wpisu
  w feedzie) nadal stoi pod `@auth`, bez `@can('cook', …)`; gałąź w ogóle nie rusza
  tego pliku (zmienia tylko `recipes/show.blade.php:317` i
  `recipes/cooking.blade.php:160`), a raport mówi o „obu zaproszeniach”. Skutek:
  konto zawieszone widzi w feedzie przycisk w kolorze marki prowadzący do 403 —
  **martwy przycisk, ten sam objaw, który #902 miało usunąć**. Sekcja „szerszy
  wzorzec zawieszenia” wylicza komentarz, obserwowanie, zapisywanie i odhaczanie,
  ale nie ten przycisk, więc to nie jest opisane ograniczenie.
- `[pkt 8] config/kuking.php:2761` — `Alfa 0.68` bez `CHANGELOG.md` (Z24).
- `[pkt 7] app/Http/Controllers/CommentController.php` — kolizja z
  `gpt-openai-granice` (obie przepisują `update()`).
Sprawdzono bez zastrzeżeń: raport podaje liczbę i treść czerwieni (2/4/2 porażki,
404 na proponowanej trasie) i przyznaje, że pełnego zestawu po ostatniej poprawce
macierzy autoryzacji nie powtórzono; cztery kontrole ujemne z oczekiwaną przyczyną.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### gpt-wersje-przepisu
Zakres: treść, migawka i audyt w jednej transakcji z numerowaniem pod blokadą (#895),
pełniejsza migawka (#896), „Uwaga do składnika” (#898), reguła protokołu (#900).
Znaleziska:
- `[pkt 7] app/Http/Controllers/RecipeController.php` i
  `resources/views/pages/recipes/szczegoly.blade.php` — oba ruszane równolegle
  przez `gpt-kreator-przepisu`; wspólna linia to pętla `@foreach($oldSteps …)`,
  w którą obie gałęzie wstawiają nowe kontrolki.
Nie zgłaszam (autor opisał sam i przekazał imiennie): niedomknięcie #900
w `recipe-wizard.blade.php`. **Odnotowuję jako fakt koordynacyjny:**
`gpt-kreator-przepisu` tej reguły nie wnosi, więc po scaleniu obu kreator dalej
zapisze `ftp://`, a raport #900 twierdzi inaczej.
Sprawdzono bez zastrzeżeń: czerwienie nazwane co do przyczyny, z PID-ami backendów
i SQLSTATE 23505 na `recipe_versions_recipe_id_version_number_unique`; siedem
kontroli ujemnych, w tym opisana pułapka skompilowanego Blade po przywróceniu
mtime; test wyścigu ma `#[Group('dwa-polaczenia')]`, więc nie wchodzi do
domyślnego przebiegu.
Werdykt: **GOTOWA NA PR** — pod warunkiem ustalenia kolejności wobec
`gpt-kreator-przepisu`.

### gpt-kreator-przepisu
Zakres: brak zastępczego przepisu po usunięciu szkicu (#893), błędy wędrujące za
przestawionym krokiem (#894), stabilne klucze wierszy (#897), plakietka
autozapisu (#892), lista `/dodaj/szkice` (#901).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: raport podaje dokładnie co padło
(dwa rekordy zamiast jednego, tekst gubiony pod kluczami `3` i `1000000`,
`STARE_POTWIERDZENIE` w Chromium, 404 listy szkiców); pięć kontroli ujemnych
mutujących realne pliki aplikacji, plus niezależne porównanie MD5 trzech plików
po serii; każdy nowy test ma odpowiadającą mu zmianę w `app/`/`resources/`/`routes/`;
`BladDlaCzlowieka` łapany we wszystkich ścieżkach wołających `persist()`, więc nie
ma białego ekranu, a komunikat mówi CO ZROBIĆ; bez JS druga plakietka schowana
przez `x-cloak`; #899 zgłoszono jako wariant do decyzji, bez asercji betonującej
dzisiejsze zachowanie.
Werdykt: **GOTOWA NA PR** (kolizja plików z `gpt-wersje-przepisu`).

### gpt-tagi-miejsce
Zakres: przeniesienie ręcznie przypiętych tagów pod opis w czterech formularzach
(#647) + pomiary do #369.
Znaleziska:
- `[pkt 7] resources/views/pages/posts/create.blade.php` i
  `resources/views/pages/questions/create.blade.php` — te same pliki przebudowuje
  `gpt-zdjecia-publikacja` (mapa kotwic `x-error-summary :targets`, `aria-invalid`
  na `#f-photos`, blok zachowanych zdjęć), w blokach sąsiadujących w tym samym
  `<div data-tagi-opis>`.
Sprawdzono: `647-czerwien.txt` zawiera **pełny ślad czerwieni z komunikatem**
(„Lista tagów ma być przy opisie, przed kolejnym polem formularza”,
`Failed asserting that 0 is identical to 1`, `TagiPrzyOpisieTest.php:53`) — to
najlepszy dowód czerwieni w całym audycie; instrukcja „Wpisz # i nazwę” nie
zginęła (żyje w `components/tagi-formularz.blade.php:34`); tabela #369 ma mianownik
(2/3/5/10/42 osoby) i nie twierdzi, że wyznaczyła bezpieczny próg.
Werdykt: **GOTOWA NA PR** z zastrzeżeniem kolejności wobec `gpt-zdjecia-publikacja`.

### gpt-dr-zdjecia
Zakres: lokalna próba odtworzenia zdjęć na własnym MinIO (#617) i pomiar okna GPS
przy surowym PUT (#602); zero zmian w `app/`.
Znaleziska: **Nic nie znalazłem.** Podejrzenie „strażnik bez zmiany” się nie
potwierdza: gałąź nie udaje, że wnosi zmianę — nagłówek testu brzmi „Pomiar
wariantu DR, NIE decyzja o wdrożeniu”. Sprawdzono: czerwień zacytowana dosłownie
(`BRAK_ODMOWY: pisarz kasuje chronioną wersję`, 27 asercji) wraz z opisem
nieudanego fixture’u, którego nie zaliczono; trzy kontrole ujemne, w tym mutacja
realnego `app/Domain/Media/Actions/StoreUploadedImage.php` — skan dowodzi, że
czyta produkcyjny kod; RTO/RPO jawnie **niezmierzone**, z zakazem liniowego
przeliczania 0,520 s na 111 GB; `tests/Dr/` nie jest w żadnym `<testsuite>`
w `phpunit.xml`, więc nie udaje strażnika CI; poświadczenia MinIO maskowane.
Werdykt: **GOTOWA NA PR** jako pakiet pomiarowy.

### gpt-pytania-poradzcie
Zakres: wejście „Czeka na odpowiedź (N)” na `/pytania`, akcja „Otwórz i odpowiedz”,
naprawa obejścia flagi działu we Wspomnieniach (#881).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: licznik idzie przez ten sam
`QuestionList` (bez drugiej definicji widoczności); `Wspomnienia::dlaOsoby`
+ `enabledKinds()`; dwa nowe testy asertują na prawdziwym HTTP, nie na kodzie;
trzy kontrole ujemne w `docs/qa/pytania-poradzcie/kontrola-*.json` z MD5 przed/po,
„FAIL (kod 1)” i potwierdzoną przyczyną. Raport mówi, które próby były czerwone
i że test nazwy był od początku zielony.
Werdykt: **GOTOWA NA PR.**

### gpt-zdjecia-limity
Zakres: wspólny limit zdjęć na obu formularzach, podgląd i usuwanie nowych plików,
zachowanie zdjęć po błędzie, komunikat odrzuconego awatara. **Nadzbiór
`gpt-zdjecia-publikacja` — patrz Z30.**
Znaleziska:
- `[pkt 3] docs/research/zdjecia-limity-dowody/kontrole.txt:1-11` — **potwierdzone
  odczytem**: raport linkuje ten plik jako „wyniki kontroli”, a plik zawiera
  wyłącznie `full:` / `pint:` / `phpstan:` / `build:` / `targeted:` i pod każdym
  `System.Object[]` (zepsuta serializacja PowerShell). Twierdzenia o 4466 testach,
  PHPStanie i czterech kontrolach ujemnych **nie mają w repozytorium żadnego
  czytelnego dowodu**.
- `[pkt 7 + 8] config/kuking.php:2761` — `Alfa 0.68` bez `CHANGELOG.md` (Z24).
- `[pkt 7, semantyczny, bez konfliktu tekstowego] app/Models/Profile.php:133` —
  gałąź zawęża `zdjecieSieJeszczePrzygotowuje()` do `pending`/`processing`, a
  `gpt-ustawienia-profilu` opiera na tej metodzie nowy opis
  (`settings/profile.blade.php:41`) i przypadek testowy `'ready bez wariantu'`
  (`OpisAwataraWProfiluTest.php:26`). Po scaleniu obu `ready` bez wariantu trafi
  w gałąź „Nie masz jeszcze zdjęcia” i **ten przypadek padnie**. `git merge-tree`
  konfliktu nie pokazuje.
Nie zgłaszam: brak usuwania w kreatorze Livewire i brak DataTransfer — autor
opisał jako świadome ograniczenie.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### gpt-ustawienia-profilu
Zakres: #803 zgodność formularza usuwania awatara, #802 opis skrótu zdjęcia,
#804 wstrzymanie nawigacji przy niepotwierdzonym zapisie, #801 instrukcja kopiowania.
Znaleziska:
- `[pkt 2] scripts/wyglad-nawigacja.test.mjs` i `scripts/kopiowanie-adresu.test.mjs`
  — oba nowe strażniki **nie są przez nic uruchamiane** (patrz Z22).
- `[pkt 8] docs/design/USTAWIENIA_PROFILU_801_804.md:109` — „Testy JS:
  `npm run test:ustawienia-profilu`; podłączone do zadania assetów CI”. Takiego
  skryptu nie ma. Obietnica bez pokrycia w zdaniu, które ma dowodzić wykonania.
Sprawdzono i nie zgłaszam: zmiany w `CzteryDrogiZdjeciaPodBlokadaTest` to wyłącznie
komentarz (asercje nietknięte).
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — jedna linia w `package.json` (ale patrz Z23).

### gpt-panel-moderacji-marka
Zakres: wyłącznie dokumentacja — ponowny lokalny odbiór #581 (0 linii w `app/`;
17 835 wstawień to raport i JSON-y dowodowe).
Znaleziska: **Nic nie znalazłem.** Podejrzenie „strażnik bez zmiany” nie potwierdza
się w drugą stronę: gałąź nie wnosi ani strażnika, ani asercji —
`docs/design/PANEL_ODBIOR_FLOTY_581_2026_09_20.md:76` mówi wprost „Nie powstała
poprawka błędu, nowy strażnik ani nowa asercja produktowa”. Cudze wyniki oznaczone
`[pomiar cudzy: …]`; adaptacja pięciu plików oprzyrządowania udokumentowana sumami;
granice („nie zastępuje odbioru produkcyjnego”, „nie uruchamiano mutacji”) podane
przez autora.
Werdykt: **GOTOWA NA PR** — nie zamyka #581; jedyny koszt to ~18 tys. linii JSON.

### gpt-moderacja-ai
Zakres: #827 dostęp automatu przez Policy, #828 klasa wyjątku w dzienniku,
#826 walidacja odpowiedzi modelu, #829/#830 ocena etapami.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: `AutomaticAnalysisAccess` (kopia
modelu, nigdy zapis, `null` jako gość — nie podszywanie się pod autora);
`mergeOpen()` pod `lockForUpdate()` z pominięciem `resolved`/`rejected`;
`PrzeanalizujZdjecieWpisu` (`ShouldBeUnique`, ponowne sprawdzenie dostępu po HTTP);
`Post::media()` ma `orderBy('post_media.position')`, więc wybór przy zleceniu
i w zadaniu jest ten sam; klucze `moderation.model.*` istnieją już w
`config/kuking.php:2715,2720` — funkcja nie jest martwa;
`docs/legal/MODERACJA_AI_826_830_KONTROLE.txt` zawiera pełne siedem cykli
PASS → mutacja z widocznym diffem → FAIL z oczekiwanego wzorca → PASS.
Werdykt: **GOTOWA NA PR.**

### gpt-pytania-widoki
Zakres: #834 aktualny tytuł pytania w powiadomieniu, #860 rozdzielenie w Pomocy,
#847 pomiar retencji (bez zmiany zachowania).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: `QuestionNotificationContext`
(dwa odczyty zbiorcze, `widoczneDla`, filtr `body_removed_at`, flaga działu, brak
kopii tytułu w JSON); test liczby zapytań dla 2 i 20 pytań; test utraty dostępu
i wyłączonej flagi; escapowanie w Blade. Zmiana w
`PrzedawnioneWiadomosciDoOperatora.php` to korekta nieaktualnego komentarza —
autor pisze „Bez zmiany zachowania” i przedstawia cztery warianty do decyzji
właściciela, żadnego nie wdrażając. Dwa próbniki w `docs/qa/` są poza katalogiem
testów i autor tego nie ukrywa.
Werdykt: **GOTOWA NA PR.**

### gpt-onboarding
Zakres: #851 przenoszenie wyboru osób, #852 komunikat po częściowo nieudanym
zapisie, #862 `follow=null` (było HTTP 500), #849 nazwy zastrzeżone, #850
odzyskanie pól po wygaśnięciu domknięcia OAuth.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: token wyboru związany z kontem
i sesją (30 min), `UserPolicy::follow` przy odtwarzaniu, limit 50/20;
`ExternalRegistrationDraft` trzyma tylko niesekretne pola, jest związany
z identyfikatorem dostawcy, nie dziedziczy się na inne konto, oświadczenia nie
wracają zaznaczone; `scripts/onboarding-kontrole.sh` — sześć kontroli dodatnich
z konkretną mutacją i oczekiwanym wzorcem, plus bramka odmawiająca pracy poza
własną bazą, bez pułapki z pkt 9; zmiany w istniejących testach logowania
wyłącznie addytywne; „Dalej” z `formmethod="POST"` działa bez JS.
Werdykt: **GOTOWA NA PR.**

### gpt-pwa-push
Zakres: ponowny pomiar PWA #278 (bez zmian kodu) + fundament limitu Web Push #35.
Znaleziska: **Nic nie znalazłem.** **Komplet z punktu 6 jest pełny:** migracja
`2026_09_20_220000_create_push_daily_reservations` + test
`tests/Feature/PushDeliveryLimitsTest.php` (w tym rollback pustej i wypełnionej
tabeli) + wpis w `docs/DATABASE.md` + plan wycofania w migracji i w dokumencie.
`down()` odmawia na niepustej tabeli, żeby cykl down/up nie odnowił zużytych
limitów. Kod jest świadomie niepodłączony do żadnego kanału i jest to napisane
w trzech miejscach, więc to nie jest obietnica bez pokrycia.
`scripts/push-budget-race.php` odmawia pracy poza `kuking_flota_*` na 55439.
Werdykt: **GOTOWA NA PR.**

### gpt-eksport
Zakres: #819–#825 — wspólna transakcja prośby i zadania, osobne zadanie listu
z `notified_at`, `false` z `writeStream` jako porażka, pełny UUID w nazwach plików.
Znaleziska:
- `[pkt 1] docs/infra/EKSPORT_NIEZAWODNOSC.md (cały plik)` — jedyny raport tej
  gałęzi **nie zawiera ani jednego zdania o wykonanych pomiarach**: co było
  czerwone, z jakim komunikatem, ile testów/asercji, żadnej kontroli ujemnej,
  żadnego pliku dowodowego. Gałąź wnosi pięć nowych testów i modyfikuje cztery
  istniejące (m.in. `KomunikatWyjatkuNieWchodziSurowyDoDziennikaTest.php:129`,
  gdzie asercja `'550 5.1.1'` zmienia się na klasę wyjątku). Przy historii z Z21
  **nie da się odróżnić testów, które padły, od dopisanych do gotowego kodu.**
- `[pkt 8 — NIE DO ROZSTRZYGNIĘCIA Z ODCZYTU] app/Domain/Users/Exports/ExportQueue.php:19-24`
  — bramka sprawdza `config('queue.connections.database.driver')`, czyli pole,
  które **zawsze** ma wartość `database`, a nie faktyczne `queue.default`;
  dispatch jest bezwarunkowo `->onConnection('database')`. Przy
  `QUEUE_CONNECTION=sync` (tak ma `phpunit.xml`) zamówienie nie poleci
  synchronicznie, tylko wyląduje wierszem w `jobs`, którego nikt nie odbierze.
  Na Railway `QUEUE_CONNECTION=database`, więc na produkcji skutku nie widać.
  **Żeby rozstrzygnąć, trzeba uruchomić:** zamówienie eksportu przy
  `QUEUE_CONNECTION=sync` i sprawdzić status rekordu.
Nie zgłaszam: brak wspólnej transakcji storage↔PostgreSQL, brak gwarancji dokładnie
jednego listu, nienaprawianie zaległych `queued` — autor opisał jako granice.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — uzupełnić raport o rzeczywiste czerwienie
(reszta floty ten standard trzyma).

### gpt-rozbicie-uslug
Zakres: `->onOneServer()` na 19 zadaniach harmonogramu + test manifestu Railway
+ dokumentacja #595/#600.
Znaleziska:
- `[pkt 5] scripts/railway-roles.test.mjs:9-23,32-46` — **test odwołuje się do
  stałych, których w repozytorium NIE MA i których gałąź NIE DODAJE.** Sprawdzone
  bezpośrednio: gałąź nie rusza `.railway/` w ogóle, a
  `.railway/railway.ts` na tej gałęzi ma z wymaganych tylko
  `PRODUCTION_SPLIT_SERVICES`; brak `STAGING_SPLIT_SERVICES`,
  `PRODUCTION_WEB_REPLICAS`, `PRODUCTION_MEDIA_WORKER` i zmiennej
  `KUKING_POLACZENIA_BUDZET`. Test jest **czerwony od pierwszego przypadku**.
  Praca nad manifestem przepadła w commicie odzyskania.
- `[pkt 2] scripts/railway-roles.test.mjs` — i nikt tego nie zobaczy, bo strażnik
  nie jest nigdzie uruchamiany (Z22).
- `[pkt 3] docs/infra/ROZDZIELENIE_ROL_595_600_POMIARY.md:24-28` — „Testy
  rzeczywistego manifestu Railway w powyższym buildzie: 7 testów” i „npm run build:
  27 testów skryptów, PASS” opisują drzewo, którego w gałęzi nie ma.
Nie jest znaleziskiem: `->onOneServer()` stoi na wszystkich 19 zadaniach,
a `HarmonogramJednegoSerweraTest` ma realną kontrolę dodatnią.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — najcięższa usterka tej nocy.

### gpt-tablica
Zakres: panel tablicy dnia — zachowanie wyboru i notatek po błędzie, wyszukiwanie
osób spoza 40, kolejność wyróżnień, pytania na tablicy.
Znaleziska:
- `[pkt 3] docs/research/TABLICA_DNIA_863_868.md:4` — „Baza pracy: 534e0a51”,
  a rzeczywista baza to `4c811cc7`. Liczby pełnych przebiegów (4400/4401) zmierzono
  na innym drzewie niż to, które idzie do scalenia.
Poza tym nic. Sprawdzono: raport podaje co padło (dwie czerwienie pełnego przebiegu,
w tym podmiana `OdstepPodPodpisemPolaTest`), 9 kontroli ujemnych z przyczynami;
nowe testy chronią realne zmiany (`DailyBoard::fromCuratedPicks`,
`DailyPick::scopeForDate` z `orderBy('id')`, `DailyBoardCandidates`); walidacja
`wpisy`/`osoby` przechodzi przez zapytanie kandydatów, więc ręczny UUID
niedostępnego konta jest odrzucany.
Werdykt: **GOTOWA NA PR** po wyprostowaniu zdania o bazie.

### gemini-profil
Zakres: zakładka „Ugotowane” (stabilna paginacja, N+1), bezpieczny powrót po
usunięciu wykonania, opcja „Nie podaję”, alty zdjęć, badge 0 minut.
Znaleziska:
- `[pkt 1] brak jakiegokolwiek pliku w `docs/`** — gałąź **nie ma raportu**. Sześć
  nowych testów i pięć zmian w `app/`/`resources/` bez jednego zdania o tym, co
  padło, z jakim komunikatem i na czym mierzono N+1. Przy historii z Z21 nie da
  się rozstrzygnąć, czy którykolwiek test był widziany na czerwono.
- `[pkt 4] resources/views/pages/cooked/celebrate.blade.php:36` — ta sama
  fotografia wykonania renderowana przez `<x-photo>` **bez zastępczego `alt`**.
  Gałąź naprawia to w `components/cooked-card.blade.php` („Zdjęcie 1 z 3
  wykonania”), a ekran gratulacji zaraz po publikacji zostaje z `alt=""`.
- `[pkt 2, NIE DO ROZSTRZYGNIĘCIA Z ODCZYTU] app/Http/Controllers/CookedEventController.php:167,172`
  — dopisane gałęzie `=== ''` / `empty()` są prawdopodobnie martwe, bo
  `ConvertEmptyStringsToNull` zamienia `value=""` na `null` wcześniej.
  `WycofanieOcenyWykonaniaTest::test_wybranie_nie_podaje_zapisuje_null` przeszedłby
  też bez nich. **Żeby rozstrzygnąć, trzeba uruchomić** ten test z cofniętą zmianą
  kontrolera.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### gemini-794
Zakres: nagłówek i cytat celu zgłoszenia (#794), „Wróć” z autoryzowanego celu
zamiast Referera (#795), błędy przy polach na ekranie prywatności (#792).
Znaleziska:
- `[pkt 7] resources/views/pages/settings/privacy.blade.php` — **kolizja
  z `gpt-wspomnienia-prywatnosc`**. Obie gałęzie przerabiają TEN SAM formularz na
  DWA wykluczające się sposoby: tu `session()->hasOldInput() ? old(...) : $z bazy`,
  tam ukryte `original_digest`/`original_memories` + `value="0"` i kontrola
  konfliktu. „Biorę swoją wersję” nie zapali czerwieni po stronie tej gałęzi,
  a wywróci drugą — i odwrotnie.
- `[pkt 5] tests/Feature/OdrzuconyFormularzPrywatnosciTest.php:113-121` —
  `test_poprawne_zapisanie_obu_pol_dziala_jak_dawniej` wysyła PUT bez `original_*`
  i oczekuje `assertSessionHasNoErrors()`. Po scaleniu `gpt-wspomnienia-prywatnosc`
  ten sam request kończy się błędem walidacji → **test czerwony**. Analogicznie
  `test_poprawny_pierwszy_get_pokazuje_stan_z_bazy` (regex
  `name="memories_enabled"[^>]*checked`) trafi na dodane pole ukryte `value="0"`.
- `[pkt 1] brak pliku w `docs/`** — gałąź **nie ma raportu**.
Nie jest znaleziskiem: cytat treści nie łamie autoryzacji —
`ReportController::create()` woła `ReportContent::authorize()` przed zbudowaniem
`CelZgloszenia`, a `CelZgloszenia` ma bramkę `body_removed_at`/`trashed()`/`STATUS_REMOVED`.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### naprawa/ci-hybryda-runnerow
Zakres: podział `ci.yml` na dwie pule runnerów (9 × `CI_RUNS_ON`, 4 przeglądarkowe),
D-225 i przepisanie strażnika dokumentów.
Znaleziska:
- `[pkt 3] docs/DECISIONS.md, D-225, „Liczby, od których to się zaczęło”** — dwa
  zdania wzajemnie sprzeczne: „`CI_RUNS_ON` stała na `ubuntu-latest`, więc
  WSZYSTKIE joby jadły płatne minuty” i cztery wiersze niżej „Własne runnery
  działają: zielony przebieg `main` o 17:11 miał na nich dwanaście z trzynastu
  jobów”. Jeśli prawdziwe jest drugie, premisa oszczędności opisuje stan, który
  już był. **NIE DO ROZSTRZYGNIĘCIA Z ODCZYTU** — trzeba zobaczyć wartość
  `CI_RUNS_ON` w Settings → Variables i konkretny przebieg.
- `[pkt 3] tamże` — „811 z 3 000 minut w pięć godzin”, „~65 minut na przebieg”,
  „ponad 500 minut na ponowienia” to liczby z rozliczenia GitHuba, czyli spoza
  repozytorium, a **nie są oznaczone `[pomiar cudzy: źródło]`** — wymóg, który
  reszta dzisiejszych gałęzi respektuje.
Nie jest znaleziskiem: podział jest realny (13 deklaracji `runs-on:`, 9+4);
`DokumentyCiMowiaPrawdeORunnerzeTest` **nie złagodniał pozornie** — dokłada trzy
kontrole zapalające się przy przeniesieniu joba między pulami, przy
zahardkodowanej etykiecie i przy zadeklarowanej, lecz nieużywanej puli; kolejność
wdrożenia (zmienna nieustawiona = `ubuntu-latest`) jest bezpieczna.
**Żadna inna z 53 gałęzi nie rusza `.github/` — kolizji w workflow nie ma.**
Werdykt: **GOTOWA NA PR** po oznaczeniu źródła liczb i usunięciu sprzeczności.

### gpt-wyszukiwanie-granice
Zakres: twarda granica 120 znaków frazy z komunikatem zamiast cichego obcinania
(#885) i wiodące `@` traktowane jak zapis nazwy (#886).
Znaleziska: **Nic nie znalazłem.** Sprawdzono: czerwień przed zmianą z komunikatem
(„Skopiowana nazwa nie odnalazła profilu”, 2 testy / 9 asercji), trzy kontrole
ujemne; `normalize()` przestało ciąć `mb_substr(…,0,120)`; oba wywołania domeny
(`SearchController`, `OnboardingController`) osłonięte `$searchErrors->isEmpty()`,
a `git grep` nie pokazuje trzeciego wywołania `SearchQuery::recipes()/people()`;
sygnał `search_performed` nie zapisuje się dla odrzuconej frazy;
`Admin\UzytkownicyController::normalizuj()` nie ma własnego cichego limitu, więc
reguła **nie jest połowiczna**; pełny tekst zostaje w polu.
Werdykt: **GOTOWA NA PR.** *(Konfliktuje z `main` na `SearchController.php`.)*

### gpt-wspomnienia-prywatnosc
Zakres: ochrona przed nadpisaniem nowszej decyzji o zgodzie (#880),
`enabledKinds()` (#881), komunikaty opisujące czynność (#882, #879).
Znaleziska:
- `[pkt 7] resources/views/pages/settings/privacy.blade.php` +
  `app/Http/Controllers/Settings/PrivacySettingsController.php:22-31` — kolizja
  z `gemini-794` (opis wyżej). Dodatkowo ta gałąź **zmienia kontrakt endpointu**:
  `original_digest`/`original_memories` są `required`, przez co trzeba było
  poprawić sześć istniejących testów. Każdy POST/PUT bez tych pól (zakładka otwarta
  przed wdrożeniem) jest odrzucany — to jest zamierzone, ale kolejność scalania
  rozstrzyga, która gałąź wyjdzie czerwona.
- `[pkt 8, NIE DO ROZSTRZYGNIĘCIA Z ODCZYTU] privacy.blade.php:8-9` —
  `old('original_*', session()->hasOldInput() ? '' : …)`: jeśli człowiek trafi na
  ekran prywatności w żądaniu, w którym w sesji leżą stare dane INNEGO formularza,
  ukryte pola wyjdą puste i pierwszy zapis zostanie odrzucony komunikatem
  o konflikcie, którego nie było. Droga wyjścia istnieje i jest testowana.
  **Żeby rozstrzygnąć, trzeba odtworzyć** przekierowanie z błędem lądujące na
  `/ustawienia/prywatnosc`.
Nie jest znaleziskiem: przerobienie `KomunikatWyjatkuNieWchodziSurowyDoDziennikaTest`
opisane w raporcie razem z przyczyną czerwieni; raport podaje czerwienie
(4 porażki + kontrola dodatnia), cztery kontrole ujemne i pomiar dwóch połączeń
z `pg_blocking_pids`.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — nie z powodu własnej jakości (wysoka),
tylko kolizji z `gemini-794`.

### gpt-tag-tygodnia
Zakres: pakiet redakcyjny #18 — kalendarz polskiej kuchni w JSON, instrukcja,
raport weryfikacji, test spójności danych.
Znaleziska: **Nic nie znalazłem.** „Zero zmian w `app/`” **nie jest tu** wzorcem
strażnika bez zmiany: `KalendarzKuchniDaneTest` pilnuje pliku danych, który ta
sama gałąź wnosi (`docs/product/dane/kalendarz-polskiej-kuchni.json`); test ma
kontrolę dodatnią wobec pustego skanu (`assertGreaterThanOrEqual(12, $count)`
z komunikatem „test musi przeczytać propozycje, nie pusty plik”) i drugą wobec
bazy (`assertContains('pierogi', $names)` po realnym `TagSeeder`). Raport podaje
dwie kontrole ujemne z MD5, rozliczoną jedyną czerwień pełnego przebiegu i granice
odbioru (brak UI → brak audytu 320 px/200%).
Werdykt: **GOTOWA NA PR.**

### bramka-startowa
Zakres: domyślne `secure` dla ciasteczka sesji na produkcji + trzy dokumenty
RODO/DSA + przepisane wiersze listy gotowości.
Znaleziska: **Nic nie znalazłem.** Sprawdzono najważniejsze: **test NAPRAWDĘ
zapala się po cofnięciu zmiany** — cofnięcie wraca do `env('SESSION_SECURE_COOKIE')`,
czyli `null`, a `test_brak_zmiennej_na_produkcji_daje_ciasteczko_secure` robi
`assertTrue(null)`. Test ma własną kontrolę dodatnią (poza produkcją domyślnik NIE
może wymuszać `secure`, inaczej test produkcyjny przestaje cokolwiek mierzyć)
i trzeci przypadek na jawne `SESSION_SECURE_COOKIE=false`; `podstaw()` podmienia
zmienną we wszystkich trzech źródłach `env()` i przywraca w `tearDown`;
`require config/session.php` omija policzoną konfigurację. Pomiar produkcji
oznaczony `[pomiar cudzy: sesja prowadząca floty, 20.09.2026]`. Wiersze
`COMPLIANCE.md` nie zamieniają dokumentu na podpis (przy Meta wprost napisano,
że umowa powierzenia to niewłaściwy instrument).
Werdykt: **GOTOWA NA PR.**

### gpt-testy-50plus
Zakres: wyłącznie dokumentacja badania z ludźmi #15 — nowy protokół R1 (8 zadań)
zastępuje stary zestaw 10 zadań.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: brak zmian w `app/`, `resources/`,
`database/`, `tests/` (pkt 1, 2, 6 nie dotyczą); wszystkie odsyłacze względne
wskazują istniejące pliki; jedyna ścieżka w protokole (`/home`) istnieje
w `routes/web.php:580`; nieaktualny `SCENARIUSZE_UZUPELNIAJACE_15.md` dostał
na górze jawną adnotację, że jego zapisy nie obowiązują w R1.
Werdykt: **GOTOWA NA PR.**

### gpt-skladniki
Zakres: #878 (widok przepisu bez dopisku „— do smaku”), #741 (koniec obietnicy
skalowania porcji), #754 (trwałe potwierdzenie wyboru tagu).
Znaleziska:
- `[pkt 2 + 8] scripts/tagi-potwierdzenie.test.mjs` — jedyny strażnik #754 nie jest
  uruchamiany przez nic, a raport twierdzi, że „Test DOM w Chromium jest w CI”
  (patrz Z22).
- `[pkt 4 + 7] resources/views/pages/recipes/show.blade.php` — **usunięcie „— do
  smaku” zderza się z `flota/gotowanie`, która ten sam blok dodaje w trybie
  gotowania** (patrz Z25).
- Znany przypadek `1.255 → 1.26`: **potwierdzam jako nienaprawiony**, ale autor
  sam go opisuje jako otwarty — `:step="0.5"` stoi identycznie w kreatorze
  (`recipe-wizard.blade.php:1123`) i w formularzu jednostronicowym
  (`szczegoly.blade.php:168`), a wybór między setnymi a połówkami czeka na decyzję
  właściciela. **Nie liczę jako znalezisko** — to jawnie opisane ograniczenie.
Sprawdzono bez zastrzeżeń: raport podaje, co było czerwone i z jakim objawem
(„pieprz — do smaku”, „olej do smażenia — do smaku — na patelnię”) plus cztery
kontrole ujemne PASS→FAIL→PASS.
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

### naprawa/proba-odtworzenia-w-ci
Zakres: jedna linia — sonda gotowości Postgresa ma pytać o `${BAZA_HOST}:${BAZA_PORT}`.
Znaleziska:
- `[pkt 7] tests/skrypty/proba-odtworzenia.sh:147` — **ta zmiana jest już
  w `origin/main`**, wniesiona przez #929 („Doinstaluj klienta PostgreSQL 18
  i skieruj sonde gotowosci na port CI”), co do znaku. **Potwierdzone:**
  `git diff origin/main origin/naprawa/proba-odtworzenia-w-ci` nie wymienia tego
  pliku w ogóle. Gałąź stoi na starej bazie `4c811cc7` i po przebazowaniu **nie
  wnosi nic**.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — nie ma czego scalać. **PR-a nie
otwierać**: zamknąć gałąź albo sprawdzić, co jeszcze miała wnieść.
**To najtańsze 65 minut do odzyskania w całym zestawie.**

### naprawa/kontrola-dodatnia-do-przodu
Zakres: zdejmuje blokadę `CI != "true"` ze `scripts/kontrole-negatywne-alfa08.py`,
wprowadza `StraznikTekstuMaKontroleDodatniaTest` i zamkniętą listę zastanych.
Znaleziska: **Nic nie znalazłem w samej gałęzi.** Sprawdzono dokładnie to, o co
zlecenie pytało: własna kontrola dodatnia strażnika **jest i jest realna** — dwa
nowe wpisy w `checks` mutują stałą `STRAZNIK_TEKSTU_TEST` i znacznik
`@bez-kontroli-dodatniej`; `test_plik_bez_czytania_zrodel_nie_jest_straznikiem_tekstu`
jest kontrolą z drugiej strony. Obie kontrole realnie zapaliły — komentarze
opisują dwie konkretne porażki i ich naprawę. `tests/straznicy-tekstu-zastane.txt`
**nie jest otwartą furtką**: lista jest zamknięta na `4c811cc7`,
`test_lista_zastanych_wskazuje_istniejace_pliki` wymusza jej kurczenie się,
a dopisanie nowego pliku jest widoczne w diffie.
- `[pkt 7 — skutek dla kolejki, nie usterka tej gałęzi]` Po scaleniu tej gałęzi
  **dwa nowe testy z innych gałęzi stają się czerwone**, bo klasyfikator uzna je
  za strażników tekstu bez pokrycia: `tests/Feature/SondaCacheAssetowTest.php`
  (`gpt-harmonogram`) i `tests/Feature/PowiekszenieMaStanBleduIPonowienieTest.php`
  (`flota/zdjecia-formularze`). **Git nie zgłosi konfliktu — pliki są rozłączne —
  więc scalanka będzie zielona, a przebieg testów czerwony.** Każdy z tych dwóch
  potrzebuje wpisu w `checks` albo znacznika `@bez-kontroli-dodatniej` z powodem.
Werdykt: **GOTOWA NA PR** — pod warunkiem ustalenia kolejności (ta gałąź **po**
`gpt-harmonogram` i `flota/zdjecia-formularze` wymusza dopisanie dwóch pokryć).

### gpt-ai-piloty
Zakres: badanie trzech pilotów AI bez ani jednego żądania do dostawcy — przyrządy
CLI, korpusy syntetyczne, testy kontraktowe na atrapach HTTP.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: raport oznacza pomiary jako własne,
podaje mianowniki (31/60 i 40/60 z 60 pytań obsługiwalnych, 100 pytań wykonanych,
105 przepisów), p95 i medianę opatruje zastrzeżeniem „brak rozgrzewki, nie
interpretować jako trwałego przyspieszenia”, a brak klucza API opisuje wprost
zamiast udawać wynik; surowe dane w `docs/research/ai-pilots/evidence/*.json`;
cztery kontrole dodatnie w `scripts/ai-pilots/controls.sh` mutują konkretne warunki
w `Pilot.php`, `Transport.php`, `Budget.php`; nic nie trafia do bazy; treść błędu
dostawcy nie wycieka do wyniku; zmiana `phpstan.neon` uzasadniona pomiarem, nie
jest wyciszeniem. Wzmianka bez skutku: `controls.sh` ma na sztywno ścieżki
`/home/mateusz/flota/…`, więc uruchomi się tylko na tej maszynie.
Werdykt: **GOTOWA NA PR.**

### robota/bazy-stanowisk — PR #920 OPEN
Zakres: nazwa testowej bazy liczona z katalogu repozytorium, gdy `.git` nie istnieje.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: commit `2fec705b` podaje ślad
czerwieni („sprawdzony na czerwono przed poprawką: 3 oblane, na zielono po niej:
10 przeszło”), a `test_dwa_stanowiska_bez_pliku_git_dostaja_dwie_rozne_bazy`
pilnuje dokładnie tej reguły; dołożone testy granic (stabilność nazwy, bezpieczne
znaki, limit 63 bajtów identyfikatora Postgresa); kolizja na
`tests/skrypty/proba-odtworzenia.sh` z `naprawa/proba-odtworzenia-w-ci` **nie
istnieje**, bo tamta poprawka jest już w `main`, a ta gałąź zmienia w tym pliku
wyłącznie komentarze; `scripts/cleanup-test-dbs.sh` używa
`grep -qxF "$sufiks" <<< "$zywe_worktree"` (bez pułapki SIGPIPE).
Werdykt: **GOTOWA NA PR** (PR już otwarty).

### flota/zdjecia-formularze — PR #923 OPEN
Zakres: #742 zwalnianie object URL podglądu, #743 stan błędu i ponowienie
w powiększeniu, #744 opis zastępczy, #747 podsumowanie błędów kreatora, #745
tablicowy `old input`.
Znaleziska:
- `[pkt 2 + 8] scripts/podglad-object-url.test.mjs` — jedyny strażnik #742 nie jest
  podpięty do niczego (patrz Z22). Kontrola ujemna opisana w commicie `f639f599`
  („cofnięcie drugiego nasłuchu error psuje 5 z 6 testów”) **nikomu nie zapali po
  scaleniu**. Pozostałe cztery poprawki mają testy PHPUnit, więc chodzą normalnie.
- `[pkt 7] resources/js/app.js` — gałąź przepisuje tę samą logikę zwalniania
  `object URL`, co `gpt-zdjecia-limity` (dwie niezależne poprawki tego samego
  miejsca). Konflikt tekstowy pewny, a po JS nie chodzi żaden test PHPUnit.
Sprawdzono bez zastrzeżeń: `WierszFormularza::aktywnyWiersz()` nie gubi poprawnych
danych; komunikat lightboxa po polsku i mówi co zrobić; „Spróbuj ponownie” ma
`type="button"` w `<form method="dialog">`, więc nie jest martwym przyciskiem.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** (PR otwarty — poprawić przed scaleniem).

### flota/gotowanie — PR #913 OPEN
Zakres: minutnik na zegarze monotonicznym w osobnym module, odtwarzanie po
przeładowaniu, odzyskiwanie Wake Locka, grupy składników, naprawa
`PublishRecipe::syncSteps`.
Znaleziska:
- `[pkt 7] resources/views/pages/recipes/cooking.blade.php` vs
  `gpt-skladniki:resources/views/pages/recipes/show.blade.php` — **sprzeczność
  produktowa scalająca się bez jednej czerwieni** (patrz Z25). Do rozstrzygnięcia
  przed scaleniem którejkolwiek z dwóch.
- `[pkt 7] package.json` — konflikt z `main` na linii `build`; „biorę swoją”
  wypisuje `licznik-znakow.test.mjs` z bramki (patrz Z23).
- `[pkt 5 — SPRAWDZONE I NAPRAWIONE]` Znany przypadek zamykam: przeniesienie
  arytmetyki do `resources/js/minutnik-krok.js` faktycznie zepsuło
  `scripts/minutnik-regresja.mjs`, ale ostatni commit `b3787218` naprawia to
  porządnie (wycinek leci jako moduł ES z realnie serwowanego originu, asercje
  z `isDisabled()` na `isHidden()`). Skrypt jest w CI (`ci.yml:1177`).
Sprawdzono bez zastrzeżeń: nowe moduły JS **mają** testy podpięte do `build` —
jedyna gałąź tej nocy, która to zrobiła; `syncSteps` przesuwa kroki poza zakres
przed przestawieniem pozycji, co jest wymuszone `unique(recipe_id, position)`.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — nie z powodu kodu tej gałęzi, tylko
kolizji #764 z decyzją właściciela w #878.

### gpt-harmonogram
Zakres: #835 każde zadanie harmonogramu melduje kod wyjścia, #809 sonda cache
zastępuje odczyt nagłówka manifestu realnym pobraniem hashowanych CSS/JS.
Znaleziska: **Nic nie znalazłem** poza kolizją. Sprawdzono dokładnie to, o co
zlecenie pytało — **sonda potrafi zameldować porażkę**: `tests/skrypty/cache-assetow.sh`
uruchamia prawdziwy blok wycięty ze `scripts/sprawdz-wdrozenie.sh` (z atrapą `curl`,
bez sieci) w **dwunastu** scenariuszach — 404, 500, timeout, brak JS przy dostępnym
manifeście, HTML zamiast JS, pusty plik, `no-cache`, `private`, obce źródło,
wstrzyknięcie w ścieżkę, pozorne odnośniki — i dla każdego wymaga jawnego `BLAD:`
**oraz** niezerowego kodu, jawnie łapiąc „fałszywy sukces”. Sytuacja „nie znalazłem
żadnego pliku” kończy się błędem (`css == 0 || js == 0`). Drugi test pilnuje zmiany,
która w tej gałęzi **naprawdę jest** (`docker/Caddyfile`).
`HarmonogramSprawdzaKodWyjsciaTest` przepuszcza każde zdarzenie przez prawdziwy
`CallbackEvent` dla kodów 0/1/2/137. Skrypty używają `grep -qE "$WZ" <<< "$X"`.
- `[pkt 7] scripts/check.sh` — kolizja z `gpt-zalegle`; „biorę swoją” zostawia
  osierocony skrypt sondy i **nic nie zapali** (patrz Z26).
Werdykt: **GOTOWA NA PR** — z ręcznym rozwiązaniem konfliktu `check.sh`.

### gpt-kontakt-panel
Zakres: panel wiadomości do operatora — idempotencja wysyłki (#839), niepewność
poczty (#840), notatka a `handled_at` (#843), `handled_by` po usunięciu operatora
(#844), dwa szkice (#845), wersjonowanie karty (#846); 3 migracje, 6 testów.
Znaleziska:
- `[pkt 8] resources/views/pages/admin/wiadomosc.blade.php:66,174` — dwa formularze
  scalono w jeden, a **pierwszym** przyciskiem submit jest „Wyślij odpowiedź”
  (`formaction=admin.contact.reply`). Jest to więc **domyślny przycisk formularza**:
  implicit submission (Enter przy zaznaczaniu radia „Stan wiadomości”) wyśle
  **nieodwracalny list** zamiast zapisać stan. Skasowany w tym samym commicie
  komentarz D-058 mówił dokładnie o tym ryzyku. Raport opisuje testy przeglądarkowe
  klikania, ale nie ścieżki klawiaturowej — a UX 50+ zakłada obsługę klawiaturą.
  **Żeby rozstrzygnąć, trzeba uruchomić:** focus na radiu stanu → Enter → sprawdzić,
  czy poszedł `POST /reply`.
- `[pkt 8] app/Http/Controllers/Admin/WiadomosciController.php:82` — `show()` (GET)
  wykonuje zapisy (`finishAudit`). Naprawa-przy-odczycie na GET.
Nie znalazłem naruszenia pkt 6: trzy migracje mają komplet migracja + test +
`docs/DATABASE.md` + jawny plan wycofania (dwa `down()` świadomie **odmawiają**
z wyjaśnieniem — to jest poprawne). `status` i `role` poza `$fillable`.
**Poprawka `scripts/kontrola-ujemna.sh` (`printf | grep -q` → here-string) to
dokładnie punkt 9 i ma własny test regresyjny w `tests/skrypty/kontrola-ujemna.sh:162`
— najlepsza robota higieniczna tej nocy.**
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — domyślny przycisk formularza wykonuje
operację nieodwracalną.

### gpt-konto-poczta
Zakres: odbiorca ostrzeżenia utrwalany przy zleceniu (#888), strażnik nieaktualnego
linku logowania (#889), zachowanie adresu po odmowie budżetu (#890), konflikt nazwy
profilu → błąd pola zamiast 500 (#887).
Znaleziska:
- `[pkt 3] docs/design/KONTO_POCZTA_887_890.md:31,63,69,75` — raport **pięć razy**
  powołuje się na dowody `output/konto-poczta/pomiar-887.json`, `odbior-887.json`,
  JSON-y kontroli ujemnych i powtarzalny `kontrole.sh`. **Żadnego z tych plików
  nie ma w repozytorium**, a w przeciwieństwie do `gpt-monitoring` **nie ma tu
  żadnej uwagi, że dowody przepadły**. Czytelnik dostaje „16 asercji”,
  „302/500 → 302/302”, „pięć fizycznych kontroli ujemnych” bez czegokolwiek
  do sprawdzenia.
Nie znalazłem naruszeń w kodzie: `LoginLinkToken::znajdzPoTokenie()/jestWazny()`
istnieją w `main`; rozpoznawanie indeksu `profiles_username_(lower_)?unique`
bierze **pierwszą linię** diagnostyki, więc `DETAIL` z treścią pola go nie podrobi,
a oba indeksy naprawdę istnieją; każdej zmianie odpowiada nowy test.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — dołożyć dowody albo dopisać uczciwą
uwagę. **Kod sam w sobie wygląda na gotowy.**

### gpt-monitoring
Zakres: `AlarmMemory` — awaria cache (tej samej bazy, o której alarmujemy) nie może
zablokować dzwonka (#599); przyrządy pomiaru połączeń #598.
Znaleziska:
- `[pkt 2] scripts/monitoring/verify-local.sh:3` — `cd /home/mateusz/flota/gpt-monitoring-run`
  pod `set -euo pipefail`. Skrypt kontroli **poza tym jednym stanowiskiem nie da
  się uruchomić** (padnie w drugiej linii). To nie „skan przechodzący na zero
  plików”, ale strażnik, którego nikt inny nie odtworzy.
Nie znalazłem naruszenia pkt 2 w rdzeniu: `AlarmPrzyAwariiCacheTest` ma kontrolę
przyrządu (`assertTrue($failed, 'Próba nie wywołała awarii cache')` na prawdziwym
PostgreSQL), `verify-local.sh` wywołuje `scripts/kontrola-ujemna.sh` z mutacją
`CACHE_BLOKUJE_ALARM`, a `measure.py:73-74` ma kontrolę dodatnią przyrządu
(`assert peak == 6` po uruchomieniu sześciu procesów). Brak dowodów
`output/monitoring/*.json` **autor sam opisał** w commicie `effab631` i w sekcji
„Uwaga o dowodach surowych” — nie liczę tego (porównaj `gpt-konto-poczta`, gdzie
takiej uwagi nie ma).
Werdykt: **GOTOWA NA PR** — jedyna zmiana runtime to opakowanie `Cache::`
w try/catch, zakres wąski, kontrola ujemna opisana z nazwą mutacji.

### gpt-n1-powiadomienia
Zakres: żywy wycinek komentarza zamiast zamrożonej kopii (#758/D-223) + migracja
czyszcząca stare `data.excerpt`, odmowa zamiast cichego komentarza głównego (#761),
adresy powiadomień zbiorczo (#759).
Znaleziska:
- `[pkt 2] tests/Feature/MigracjaCzysciZamrozoneWycinkiTest.php:37-43` —
  **potwierdzone odczytem: test nie uruchamia migracji.** `uruchomCzyszczenie()`
  to ręcznie przepisana kopia zapytania z
  `2026_09_20_120000_usun_zamrozone_wycinki_komentarzy.php`. Jeśli migracja jest
  zła (albo ktoś ją zmieni), test pozostanie zielony — mierzy sam siebie. Nagłówek
  testu twierdzi wprost nieprawdę: *„Zapytanie jest przepisane z migracji dosłownie,
  więc rozjazd między nimi jest widoczny jako czerwień”* — rozjazd jest
  **niewidoczny**, co dwadzieścia linii niżej autor sam przyznaje. Naprawa:
  wywołać instancję migracji (`require` pliku → `->up()`).
- `[pkt 7] app/Models/Notification.php` i
  `tests/Feature/PowiadomieniaWidocznoscTest.php` — **konflikt z `origin/main`**
  (#924 `ee80a8b2` dołożyło tam test #757). Zmiany są semantycznie uzupełniające
  (main dodaje `urlDoKomentarza()`, gałąź `zyweWycinkiKomentarzy()`), ale
  **wzięcie swojej wersji pliku testu skasuje test #757 z `main`** i nic się nie
  zaświeci. Scalać ręcznie, sumując.
Nie znalazłem naruszenia pkt 6 — migracja danych ma komplet: migracja + test +
`docs/DATABASE.md` + plan wycofania. Nie znalazłem pkt 4: `excerpt` zniknął
konsekwentnie w zapisie (`PublishComment`), na ekranie (`notifications.blade.php`),
w eksporcie RODO (`CollectUserExportData`) i nigdzie nie ma planu zapasowego
„weź stary z `data`”. `parentRequested` dołożono we wszystkich trzech kontrolerach.
Werdykt: **USTERKA DO NAPRAWY PRZED PR** — jeden strażnik migracji nie pilnuje
migracji.

### gpt-obciazenie
Zakres: jawny korpus fotografii dla generatora obciążenia 605 + odporność próbnika
na wymianę PID; 96 plików dowodów z rampy 5→120 rps.
Znaleziska: **Nic nie znalazłem.** Sprawdzono: `scripts/probnik-605.test.mjs:22-28`
wykonuje **rzeczywisty blok** skryptu powłoki i wymaga `[1,1]` dla wzrostu CPU tego
samego PID-u oraz `[null,null]` dla pięciu scenariuszy resetu licznika;
`scripts/korpus-605.test.mjs:41-52` wymusza trzy stany (rotacja wszystkich plików,
odrzucenie liczone jako porażka, zmienione bajty zatrzymują pomiar przed napływem);
oba wpięte w `przyrzad-605.test.mjs:678`. `RAPORT.md` podaje mianowniki
(`2595 / 3600`, `609 przy 3000 w locie`, `457,1 s`), cudze wyniki oznaczone
`[pomiar cudzy: …]`, a ograniczenia (wspólny L3, brak R2/TLS/Cloudflare,
`memory.peak` od startu kontenera) wymienione osobno. `kontrola-ujemna-redirect.json`
z polem `przywrocenie: nie wykonane` jest **jawnie zdyskwalifikowany** jako dowód —
to uczciwe.
Zastrzeżenie przekrojowe: te trzy pliki `.test.mjs` też nie są w `npm run build`
(Z22), ale są przyrządem pomiarowym uruchamianym ręcznie, nie strażnikiem produktu.
Werdykt: **GOTOWA NA PR** — zmiany dotykają wyłącznie `scripts/` i dokumentacji.

### gpt-odbior-wdrozen
Zakres: odbiór lokalny #568 (>200 trafień wyszukiwarki) i #601 (fotografie
12/24/48 MP przez kolejkę bazodanową) — dokumentacja + sonda
`scripts/odbior-media-601.php`; zero zmian w `app/`.
Znaleziska: **Nic nie znalazłem** — i to jest ważny wynik, bo dziś sonda wdrożenia
już raz meldowała sukces bez pomiaru. **Tutaj jest odwrotnie:**
`docs/infra/evidence/odbior-wdrozen-2026-09-20/pierwsza-kontrola-sondy.txt`
**dokumentuje porażkę samej sondy** — mutacja weszła, test dalej przechodził, bo
skrypt po bootstrapie Laravela zwracał kod 0 mimo wyjątku. Dopiero po dodaniu
`set_exception_handler(… exit(1))` (`scripts/odbior-media-601.php:33`) trzy kontrole
ujemne dają pełne PASS → FAIL z oczekiwanego powodu → PASS. Sonda ma też wewnętrzne
kontrole dodatnie (`check(LicznikDekodowanGd::$liczba === 3, 'Kontrola dodatnia
licznika nie wykryła trzech dekodowań baseline')`). Tabela czasów podpisana „Jedna
próbka na fotografię, współdzielony host”, kolumna nazwana **„Szczyt RSS procesu
przed baseline”** (nie „RSS joba”), dane wejściowe w repozytorium z weryfikacją SHA-256.
Werdykt: **GOTOWA NA PR** — wzorcowe postępowanie z sondą, którą złapano na kłamstwie.

### flota/stan-sesji-2
Zakres: 21 commitów dokumentacji floty (dziennik nocny, inwentarz, kolejność
scalania, skrzynka, 72 prompty) + zawężenie strażnika tras o katalog `docs/flota/`.
Znaleziska:
- `[pkt 2] docs/flota/triaz-bramki.sh:6` — narzędzie triażu czyta
  `/home/mateusz/flota/kolejka9.log`, a kolejka pchania z tej samej gałęzi
  (`docs/flota/kolejka11.sh:32`) pisze do `kolejka11.log`. Przy nieistniejącym
  pliku `grep` nic nie wypisuje, pętla `while` nie wykonuje ani jednego obiegu,
  a skrypt kończy się kodem 0 — **„triaż nie znalazł żadnej porażki” wygląda tak
  samo jak „bramka jest zielona”.** Wzorzec „skan na zero plików PRZECHODZI”,
  tylko w narzędziu operatorskim. Naprawa: poprawić ścieżkę albo dodać
  `[ -f "$L" ] || { echo "brak logu $L"; exit 1; }`.
- `[pkt 9, niski] docs/flota/kolejka11.sh:37` — SIGPIPE pod `pipefail` (patrz Z28).
Nie znalazłem naruszenia pkt 2 w `DokumentyMdNieMajaMartwychOdnosnikowTest`:
kontrole dodatnie są (`assertGreaterThan(100, $sprawdzone)`,
`assertGreaterThan(80, …)`, `assertGreaterThan(50, count($realneTrasy))`, plus
osobny test parsera z listą fałszywych tras, które **mają** zostać odrzucone).
Dodanie `'docs/flota/'` zawęża wyłącznie test tras — odnośniki markdown nadal
sprawdzane. `odswiez-spis.sh` ma wypisane obie pułapki powłoki i ich unika.
Commit `46162dd5` „Popraw zmyślone godziny w dzienniku nocnym” to samodzielne
wykrycie i naprawa fabrykacji — na plus.
Werdykt: **GOTOWA NA PR** poza `triaz-bramki.sh`.

### gpt-zalegle
Zakres: wydruk przepisu na A4 (#765), ekran offline naprawdę ponawia (#749), lista
relacji po usunięciu ostatniej karty (#748), odrzucone zdjęcie przepisu (#752),
sonda PostgreSQL w `check.sh` (#732).
Znaleziska:
- `[pkt 8] config/kuking.php:2761` — `Alfa 0.68` bez wpisu w `CHANGELOG.md` (Z24).
- `[pkt 7 + 8] scripts/check.sh:45-70` — twardy port `55439` we wspólnej bramce
  (Z27).
- `[pkt 7] scripts/check.sh` vs `gpt-harmonogram` — „biorę swoją” osierocia cudzą
  sondę bez czerwieni (Z26).
- `[pkt 2] scripts/offline-ponowienie.test.mjs` — strażnik nieuruchamiany (Z22).
Nie znalazłem naruszenia pkt 5 — gałąź **sama** wykryła i naprawiła wzorzec
„strażnik bez zmiany”: commit `06e8b7f8` mówi wprost, że `SamodzielneEkranyMarkiTest`
zaostrzono na pusty `href`, a `public/offline.html` nie zmieniono, więc test padał
**na własnej gałęzi**; poprawka dokłada brakującą połowę. `tests/skrypty/check-postgres.sh`
jest porządną kontrolą dodatnią sondy i unika pułapki `grep -c` (`|| true`).
Werdykt: **USTERKA DO NAPRAWY PRZED PR.**

---

## CZĘŚĆ 3 — tabela konfliktów (do kolejności scalania)

Sprawdzone `git merge-tree` wobec `origin/main` (stan `cd966aae`).

**Konflikt z `main` już dziś:** `gpt/zawieszone-konto` (`profile/show`,
`profile/connections`), `gpt-2fa-ustawienia` (`field.blade.php`),
`gpt-ustawienia-profilu` (`confirm-button.blade.php`), `gpt-wyszukiwanie-granice`
(`SearchController.php`), `gpt-wspomnienia-prywatnosc` (`SocialController.php`),
`flota/zdjecia-formularze` (`field.blade.php`), `flota/gotowanie` (`package.json`,
`app.js`), `gpt-n1-powiadomienia` (`Notification.php`, `PowiadomieniaWidocznoscTest.php`).
**Pozostałe 45 scalają się z `main` czysto.**

**Konflikty między gałęziami — „biorę swoją” przechodzi bez czerwieni:**

| plik | gałęzie | co ginie po cichu |
|---|---|---|
| `config/kuking.php` | zdjecia-publikacja × ugotowalem-dostep × zdjecia-limity × zalegle | numer wydania przestaje identyfikować (Z24) |
| `package.json` `build` | gotowanie × `main` | `licznik-znakow.test.mjs` wypada z CI (Z23) |
| `scripts/check.sh` | harmonogram × zalegle | jedna z dwóch sond osierocona (Z26) |
| `resources/js/app.js` | zdjecia-limity × zdjecia-formularze × ustawienia-profilu × gotowanie × n1-powiadomienia | dwie niezależne poprawki tego samego `object URL`; po JS nie chodzi żaden test PHPUnit |
| `CommentController.php` | openai-granice × ugotowalem-dostep | jedna z dwóch poprawek `update()` |
| `RecipeController.php`, `recipes/szczegoly.blade.php` | wersje-przepisu × kreator-przepisu × skladniki × n1-powiadomienia | kontrolki w tej samej pętli `@foreach` |
| `settings/privacy.blade.php` | gemini-794 × wspomnienia-prywatnosc | **dwie wykluczające się koncepcje #792** |
| `posts/create.blade.php`, `questions/create.blade.php` | zdjecia-publikacja × tagi-miejsce | sąsiadujące bloki w `<div data-tagi-opis>` |
| `error-summary.blade.php` | 2fa-ustawienia × zdjecia-publikacja × zdjecia-limity × zdjecia-formularze | mapy kotwic |
| `PowiadomieniaWidocznoscTest.php` | n1-powiadomienia × `main` (#924) | **test #757 z `main`** |

**Konflikty SEMANTYCZNE — git nic nie zgłosi, czerwień przyjdzie dopiero
po scaleniu:**

1. `flota/gotowanie` (#764 dodaje „— do smaku” w trybie gotowania) ×
   `gpt-skladniki` (#878 usuwa na polecenie właściciela) — **Z25**.
2. `gpt-zdjecia-limity` (`Profile::zdjecieSieJeszczePrzygotowuje()` zawężone) ×
   `gpt-ustawienia-profilu` (`OpisAwataraWProfiluTest` liczy na starą semantykę) —
   przypadek `'ready bez wariantu'` padnie.
3. `gemini-794` (`OdrzuconyFormularzPrywatnosciTest` wysyła PUT bez `original_*`) ×
   `gpt-wspomnienia-prywatnosc` (te pola `required`) — dwa testy czerwone.
4. `naprawa/kontrola-dodatnia-do-przodu` × `gpt-harmonogram` i
   `flota/zdjecia-formularze` — dwa nowe testy zostaną uznane za strażników tekstu
   bez pokrycia.

---

## CZĘŚĆ 4 — RANKING: na co wydać minuty

Minut GitHub Actions nie znamy — API rozliczeń odmawia naszemu tokenowi. **Tego
NIE zmierzyłem i nie zgaduję.** Ranking układa się po ryzyku i po tym, ile PR
zwraca za 65 minut.

### NAJPIERW NIE WYDAWAĆ MINUT — jedna gałąź do zamknięcia bez PR-a

**`naprawa/proba-odtworzenia-w-ci`** — jej jedyna zmiana jest już w `main` (#929),
co do znaku. PR byłby pusty. **Zaoszczędzone: 65 minut.**

### DRUGA OSZCZĘDNOŚĆ — jeden PR zamiast dwóch

**`gpt-zdjecia-limity` jest ścisłym nadzbiorem `gpt-zdjecia-publikacja`** (Z30).
Jeden PR na `gpt-zdjecia-limity` (po naprawie jego własnych usterek) dostarcza
obie gałęzie. **Zaoszczędzone: 65 minut i jeden konflikt na `config/kuking.php`.**

### GOTOWE NA PR OD RAZU (18 gałęzi, malejąco wg pewności)

Kolejność w pierwszej piątce dobrana tak, żeby najwcześniej wchodziły gałęzie
bez konfliktów i bez zmian w aplikacji — czyli najmniejsze ryzyko ponowienia.

1. **`gpt-dr-baza`** — najlepiej udowodniony pakiet; zero `app/`; brak konfliktów.
2. **`gpt-odbior-wdrozen`** — sonda złapana na kłamstwie i naprawiona, dowód w drzewie.
3. **`gpt-kontakt-formularz`** — najlepiej udokumentowana gałąź produktowa nocy.
4. **`gpt-dr-zdjecia`**, 5. **`gpt-redis-ha`**, 6. **`gpt-dlug-weryfikacyjny`**,
   7. **`gpt/ocr-import`**, 8. **`gpt-testy-50plus`**, 9. **`gpt-panel-moderacji-marka`**
   — wszystkie bez zmian w `app/`, bez konfliktów. **Do rozważenia: scalić je
   w JEDEN PR dokumentacyjny.** Osiem PR-ów × 65 min = 520 minut za materiał,
   który nie dotyka kodu wykonawczego.
10. **`gpt-obciazenie`** — jw., przyrząd i dowody.
11. **`gpt/dziennik-wyjatkow`** — wąska zmiana, czerwień i kontrole w drzewie.
12. **`gpt-moderacja-ai`** — siedem pełnych cykli kontroli w drzewie.
13. **`gpt-pwa-push`** — jedyna zmiana schematu tej nocy z **pełnym kompletem**.
14. **`gpt-onboarding`**, 15. **`gpt-pytania-poradzcie`**, 16. **`gpt-pytania-widoki`**,
    17. **`gpt-ai-piloty`**, 18. **`gpt-tag-tygodnia`**, 19. **`bramka-startowa`**,
    20. **`gpt-kreator-przepisu`**, 21. **`gpt-monitoring`**, 22. **`gpt-tablica`**
    (po sprostowaniu SHA), 23. **`gpt-tagi`** (jw.), 24. **`gpt-2fa-ustawienia`**,
    25. **`robota/bazy-stanowisk`** (PR już otwarty), 26. **`flota/stan-sesji-2`**.

**Wymagają wyłącznie ustalenia KOLEJNOŚCI, nie naprawy:**
`gpt-wersje-przepisu` (po/przed `gpt-kreator-przepisu`), `gpt-tagi-miejsce`
(wobec `gpt-zdjecia-publikacja`), `gpt-harmonogram` (ręczny `check.sh`),
`naprawa/kontrola-dodatnia-do-przodu` (**po** harmonogramie i zdjęciach-formularzach),
`gpt-wyszukiwanie-granice` (konflikt z `main` do rozwiązania),
`naprawa/ci-hybryda-runnerow` (po poprawieniu dwóch zdań w D-225).

### USTERKA DO NAPRAWY PRZED WYDANIEM MINUT (14 gałęzi)

Uszeregowane wg tego, ile kosztowałoby przeoczenie.

| # | gałąź | co naprawić | koszt naprawy |
|---|---|---|---|
| 1 | **`gpt-rozbicie-uslug`** | brak `.railway/railway.ts` — **test nie ma prawa przejść**, a nikt go nie uruchamia | odzyskać manifest albo usunąć test |
| 2 | **`gpt-zalegle`** | port `55439` na sztywno we wspólnym `check.sh` psuje bramkę wszystkim; `CHANGELOG.md` | dwie zmiany, obie małe |
| 3 | **`flota/gotowanie`** × **`gpt-skladniki`** | sprzeczna decyzja „— do smaku” (Z25) | **decyzja właściciela**, potem jedna z dwóch gałęzi |
| 4 | **`gemini-794`** × **`gpt-wspomnienia-prywatnosc`** | dwie wykluczające się koncepcje #792 | **decyzja właściciela** |
| 5 | **`gpt-ugotowalem-dostep`** | `post-card.blade.php:420` bez `@can('cook')` — martwy przycisk w feedzie | jedna linia |
| 6 | **`gpt-zdjecia-publikacja`** | `zebranZdjecia()` nie używa `RecoveredFormPhotos` — obietnica raportu bez pokrycia | jedna ścieżka albo jedno zdanie w raporcie |
| 7 | **`gpt-n1-powiadomienia`** | test migracji nie uruchamia migracji | `require` migracji zamiast kopii zapytania |
| 8 | **`gpt-kontakt-panel`** | domyślny przycisk formularza wysyła nieodwracalny list | przenieść „Zapisz” na pierwszą pozycję |
| 9 | **`gpt/tokeny-zaproszen`** | `UstawienieHaslaZamiastLinku` pominięty przy tej samej regule | jedna metoda |
| 10 | **`gpt-zdjecia-limity`** | `kontrole.txt` = `System.Object[]`; `CHANGELOG.md` | podmienić plik dowodu |
| 11 | **`gpt-ustawienia-profilu`** | dwa strażniki JS poza CI + raport twierdzi, że są w CI | jedna linia `package.json` |
| 12 | **`flota/zdjecia-formularze`** | strażnik JS poza CI | jw. |
| 13 | **`gpt-skladniki`** | strażnik JS poza CI + raport twierdzi inaczej | jw. |
| 14 | **`gpt-openai-granice`** | reguła #909 tylko dla komentarza, nie dla wpisu | dispatch w `EditPost` albo zdanie w raporcie |
| 15 | **`gpt-konto-poczta`** | pięć odwołań do dowodów, których nie ma | dołożyć albo dopisać uwagę |
| 16 | **`gpt/zawieszone-konto`** | dowód czerwieni poza repozytorium | jw. |
| 17 | **`gpt-eksport`** | raport bez jednego zdania o pomiarze | uzupełnić |
| 18 | **`gemini-profil`** | **brak raportu w ogóle** + `celebrate.blade.php:36` bez `alt` | raport + jedna linia |

---

## Czego NIE zrobiłem i dlaczego

- **Nie uruchomiłem żadnego testu.** Zlecenie zabrania, a maszyna dzieli rdzenie.
  Wszystkie werdykty pochodzą z odczytu. Pięć rzeczy oznaczyłem wprost jako
  **nie do rozstrzygnięcia z odczytu**, z podaniem, co trzeba uruchomić:
  `gpt-eksport` (`ExportQueue` przy `QUEUE_CONNECTION=sync`), `gemini-profil`
  (martwe gałęzie `=== ''`), `gpt-wspomnienia-prywatnosc` (puste `original_*`
  po cudzym `old input`), `gpt-kontakt-panel` (Enter na radiu stanu),
  `naprawa/ci-hybryda-runnerow` (wartość `CI_RUNS_ON`).
- **Nie sprawdziłem minut GitHub Actions** — API rozliczeń odmawia naszemu tokenowi.
  Liczba „~65 minut na PR” pochodzi ze zlecenia, nie z mojego pomiaru.
  **[pomiar cudzy: zlecenie koordynatora, 21.09.2026]**
- **Nie oglądałem 61 gałęzi starszych niż ta noc** — poza zakresem zlecenia.
- **Nie liczyłem `1.255 → 1.26` jako znaleziska** — autor `gpt-skladniki` opisał to
  jako otwartą decyzję właściciela, a karta zabrania powtarzania świadomych granic.
- **Nie liczyłem pola `przywrocenie: "nie wykonane"`** jako znaleziska żadnej
  gałęzi — to wada wspólnego przyrządu, opisana niezależnie w sześciu raportach.

## Co wymaga decyzji właściciela lub koordynatora — nie domykam tego sam

1. **„— do smaku” w trybie gotowania** (#764) kontra decyzja #878/D-033. Dwie
   gałęzie realizują przeciwne polecenia.
2. **Który numer wydania** dostaje która z czterech gałęzi, i czy `CHANGELOG.md`
   ma być pilnowany testem, skoro reguła stoi tylko w komentarzu.
3. **Która koncepcja #792** zostaje: `old input` (`gemini-794`) czy kontrola
   konfliktu z `original_*` (`gpt-wspomnienia-prywatnosc`).
4. **Czy `gpt-zdjecia-publikacja` zamknąć na rzecz `gpt-zdjecia-limity`** (Z30).
5. **Czy dziewięć gałęzi bez zmian w `app/` złączyć w jeden PR** — 520 minut
   różnicy.
6. **Kolejność scalania `naprawa/kontrola-dodatnia-do-przodu`** — wchodząc jako
   ostatnia, zażąda pokrycia od dwóch cudzych, świeżo scalonych testów.
