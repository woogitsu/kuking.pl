# Odbiór korekty raportów feedu i budżetu — 20.09.2026

Stanowisko: `gpt-pomiar-feedu-i-budzetu`, gałąź `gpt/pomiar-feedu-i-budzetu`.
Baza: `4c811cc7bff365fb8f86d87eabac93b7738a45cd`, równa `origin/main`
po wykonanym `git fetch origin --prune`. Początkowo czyste drzewo.
Stanowisko działało; nie wykonywano procedury naprawy ani odtwarzania plików.

## A. Status dostarczenia feedu

Przed (raport w commicie `d906c778`, nadal na bazie powyżej):
> końcowe review, pełny hook, PR i CI pozostają do wykonania.
> Issues #585 i #609 pozostają otwarte.

Po:
> pakiet scalono 16 września w PR #628 jako `e951554cfd3f86147b95fb60606912c1650493f8`; CI PR i main zakończyło się sukcesem.
> Issues #585 i #609 zamknięto 16 września.

Własny odczyt historii: commit scalający
`e951554cfd3f86147b95fb60606912c1650493f8`, rodzice `620b9aec` / `b23bc976`,
16.09.2026 17:46:50 +02:00, tytuł „Zmierz rzeczywisty feed i porównaj
warianty SQL (#628)”, treść „Zmierz rzeczywisty feed i porównaj warianty
SQL — #585”. Jest przodkiem HEAD.

Własny odczyt GitHub przez `gh`: PR MERGED o 15:46:51 UTC; 12 kontroli
pass, trzy ręczne zadania preview skipping. Run main `35117632571`:
`success`, `headSha=e951554cfd3f86147b95fb60606912c1650493f8`.
#585 zamknięte 16:00:44 UTC, #609 zamknięte 16:00:48 UTC, 16.09.2026.
#609 jest **zgłoszeniem z decyzją**, nie drugim PR-em do scalenia.

Własny odczyt kodu: nadal `pluck('users.id')->all()` i `whereIn`.
Od #628 doszły m.in. `enabledKinds`, status tagów i `withVisibleCommentCount`;
nie utożsamiamy dzisiejszych zapytań w całości z historycznym benchmarkiem.
Wniosek IN/EXISTS/JOIN i historyczne czasy pozostawiono bez zmian.
Hipoteza JIT trafiła decyzją #609 do #605; poprawiono też to odesłanie.

[pomiar cudzy: opis PR #628] Pełny hook zakończył się kodem 0.
[pomiar cudzy: POMIAR_FEEDU_585.md i evidence/feed585] Czasy i porównania
wariantów z 16.09. Nie uruchomiono nowego benchmarku wydajności.

## B. Obliczony budżet a pomiar

Przed (§4):
> ponad trzykrotność zmierzonego szczytu (16).

Po (§4):
> ponad trzykrotność obliczonego budżetu szczytowego (16).

Przed (§5):
> Liczby w §3 poza „16" są **policzone** i tak są nazwane.

Po (§5):
> Budżety w §3, w tym **16**, oraz wolne miejsca i procenty są **obliczone**;

Własny rachunek: `2 × (4R + Q + S) + 1 + 3` dla `R=Q=S=1` daje 16;
warianty dodatkowej repliki i workera dają 24, 18, 26. Drobna korekta
w tym samym wierszu: „dwukrotny zapas” → „około 1,92-krotność budżetu
(50 / 26)”.

Historia: `3656a1dd0f9918fcb7cd05231fa3883b19dd2d5a` od początku opisuje
konfigurację 16 jako policzoną. `0c96de71dfe62e6f931e4f01b17cd4b8fdda411d`
dodaje sprzeczny raport, lecz jego opis jawnie zachowuje progi 50/125.
Klasa `StanPolaczenBazy` pobiera budżet z konfiguracji, a bieżące zajęcie
z `pg_stat_activity`. Komenda zapisuje osobno oba pola; sam wpis 16 w polu
`budzet_szczytowy` nie jest dowodem produkcyjnego szczytu 16.

[pomiar cudzy: evidence/polaczenia598/pomiar-lokalny-2026-09-19.txt]
Odczyty lokalne po poprawieniu próbnika wynosiły 0, 1, 5 i 6. Dokument §5
wprost wykluczał pomiar szczytu wdrożenia. Nie znaleziono w przejrzanych
artefaktach dowodu produkcyjnego szczytu 16. Nie pobierano nowych logów
Railway, więc nie twierdzimy, że taki pomiar nigdy później nie powstał.

Obie poprawki są redakcyjne: historia zawiera świadomy wybór IN oraz
jawny rozdział obliczeń i pomiarów. Nie odwracamy decyzji i nie dopisujemy
uzasadnienia wstecz. #598 jest nadal OPEN i nadal wymaga produkcyjnego
pomiaru szczytu; ten odbiór go nie zastępuje.

## Własne uruchomienia

WSL, PostgreSQL `127.0.0.1:55439`, użytkownik `kuking`, izolowana baza
`kuking_flota_gpt-pomiar-feedu-i-budzetu`. Runtime odświeżany skryptem floty.

1. Nietknięte drzewo: `PomiarFeeduGraniceTest`, `BudzetPolaczenBazyTest`,
   `PomiarCzujekTrafiaDoDziennikaTest`: **33 passed / 182 assertions**.
2. Nowy test `RaportyPomiarowRozdzielajaStanIDowodTest` przed korektą:
   **2 failed / 8 assertions**. Przyczyny: brak SHA scalenia w raporcie
   feedu oraz obecne „zmierzonego szczytu (16)” w raporcie budżetu.
3. Po korekcie, te same trzy klasy plus nowy test:
   **35 passed / 196 assertions**. Każdy odczyt raportu sprawdza istnienie
   i niepustą zawartość; brak pliku nie daje pozornej zieleni.
4. `vendor/bin/pint`: **PASS, 1156 plików** w runtime; format nowego testu bez zmian. `git diff --check` bez błędów.
5. Lokalne logi komendy w końcowym przebiegu zawierały bieżące zajęcie
   7–10 oraz budżet 16. To odczyty testowe, nie szczyt produkcyjny.

Pierwsze wywołanie z alternatywą `|` w filtrze zostało błędnie zinterpretowane
przez powłokę WSL. Zastąpiono je jawnymi ścieżkami trzech testów; tylko
zakończone sukcesem wywołanie jest wynikiem bazowym opisanym powyżej.

## Zakres i ograniczenia

Bez zmian w kodzie aplikacji, schemacie, konfiguracji i decyzjach produktu.
Nie wykonano pełnego pakietu testów, hooka ani nowego CI: zmiana dotyczy
dwóch raportów i ich regresji. Nie wykonano produkcyjnego pomiaru ani
ponownego benchmarku SQL. Nie pushowano, nie otwierano PR ani nie zmieniano
zgłoszeń. Nie ma nowej decyzji właściciela potrzebnej do tych korekt;
otwarty pomiar produkcyjny pozostaje zakresem #598. Wycofanie: odwrócenie
commita tej korekty, bez operacji na bazie.
