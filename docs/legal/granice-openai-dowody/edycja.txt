
── Kontrola ujemna: edycja ──
  plik:     app/Http/Controllers/CommentController.php
  MD5 przed: 71777c65fcbc0da74d2e7e5c8bfe24ff
  oczekuję:  job was not dispatched

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 71777c65fcbc0da74d2e7e5c8bfe24ff → d4a07a083c0d9eba6bca6c4c431f3011.
  różnica:
             $comment->fill(['body' => trim($data['body'])]);
             if ($comment->isDirty('body')) {
                 $comment->save();
    -            PrzeanalizujTresc::dlaKomentarza($comment)->afterCommit();
    +
             }

             return back()->with('status', 'Komentarz poprawiony.');
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „job was not dispatched” wystąpił w wyjściu.
    The expected [App\Jobs\PrzeanalizujTresc] job was not dispatched.
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — edycja
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/gpt-openai-granice/docs/legal/granice-openai-dowody/edycja.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
