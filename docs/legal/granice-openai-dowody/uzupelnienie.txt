
── Kontrola ujemna: uzupelnienie ──
  plik:     app/Jobs/PrzeanalizujTresc.php
  MD5 przed: 6b672408c3328bf1bfce4abf5d7543f2
  oczekuję:  numer telefonu

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 6b672408c3328bf1bfce4abf5d7543f2 → 5c097e6fde775fd6ab77cca5b68d1900.
  różnica:

                 $sygnaly = array_merge($wykrywacz->dla($tresc), $model->dla($tresc));

    -            $oznaczenie = $oznacz->handle($tresc, $sygnaly, uzupelnij: $tresc instanceof Comment);
    +            $oznaczenie = $oznacz->handle($tresc, $sygnaly, uzupelnij: false);

                 if ($oznaczenie !== null) {
                     $alarm->handle($oznaczenie, $sygnaly);
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „numer telefonu” wystąpił w wyjściu.
    Failed asserting that 'Wcześniejszy sygnał.' [UTF-8](length: 22) contains "numer telefonu" [ASCII](length: 14).
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — uzupelnienie
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/gpt-openai-granice/docs/legal/granice-openai-dowody/uzupelnienie.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
