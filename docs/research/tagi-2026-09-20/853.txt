
── Kontrola ujemna: 853 ──
  plik:     app/Domain/Tags/Actions/UpdateTagFollows.php
  MD5 przed: 31c2a9652d49a64771a41ee4e7172297
  oczekuję:  Session is missing expected key

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 31c2a9652d49a64771a41ee4e7172297 → 72979d9c2a963183d49aa0b04551971b.
  różnica:
             }
             // Blokada wierszy chroni też przed zmianą statusu poza scalaniem.
             $active = Tag::query()->whereIn('id', $ids)->orderBy('id')->lockForUpdate()->get();
    -        if ($active->count() !== count($ids) || $active->contains(fn (Tag $tag): bool => $tag->status !== Tag::STATUS_ACTIVE)) {
    +        if (false) {
                 throw ValidationException::withMessages([
                     'tags' => 'Jeden z wybranych tagów nie jest już dostępny. Sprawdź pozostałe zaznaczenia i zapisz ponownie.',
                 ]);
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „Session is missing expected key” wystąpił w wyjściu.
    Session is missing expected key [errors].
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — 853
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/_tagi-dowody/853.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
