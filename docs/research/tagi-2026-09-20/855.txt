
── Kontrola ujemna: 855 ──
  plik:     app/Http/Controllers/TagFollowController.php
  MD5 przed: b6cc733916a197c566ac1026a65cc288
  oczekuję:  matches PCRE pattern

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 b6cc733916a197c566ac1026a65cc288 → 13e6aee139088d95b154abc66c6d6c85.
  różnica:
                 $token = $forms->encode($user, $tags->pluck('id')->all(),
                     $followed->mapWithKeys(fn (Tag $tag): array => [$tag->getKey() => (string) $tag->pivot->created_at])->all());
             }
    -        $selected = session()->hasOldInput() ? (array) old('tags', []) : $followed->pluck('id')->all();
    +        $selected = $followed->pluck('id')->all();

             return view('pages.settings.tags', [
                 'tags' => $tags->sortBy('name')->values(),
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „matches PCRE pattern” wystąpił w wyjściu.
    ' matches PCRE pattern "/<input[^>]*value="01a0bf88-ee11-737c-b5ab-f7fb8aa114d5"[^>]*checked/".
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — 855
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/_tagi-dowody/855.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
