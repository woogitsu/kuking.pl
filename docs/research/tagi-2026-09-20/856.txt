
── Kontrola ujemna: 856 ──
  plik:     app/Domain/Tags/Actions/UpdateTagFollows.php
  MD5 przed: 31c2a9652d49a64771a41ee4e7172297
  oczekuję:  Pozostał częściowy zapis

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 31c2a9652d49a64771a41ee4e7172297 → cc3a4d7b64e63045fa121550aca76e34.
  różnica:
         /** Różnica względem otwarcia formularza, nigdy względem całego obecnego zbioru. */
         public function save(User $user, array $selected, array $scope): void
         {
    -        DB::transaction(function () use ($user, $selected, $scope): void {
    +        call_user_func(function () use ($user, $selected, $scope): void {
                 TagMutationLock::forPost();
                 User::query()->whereKey($user->getKey())->lockForUpdate()->firstOrFail();
                 $initial = array_keys($scope['followed']);
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „Pozostał częściowy zapis” wystąpił w wyjściu.
    Pozostał częściowy zapis zamiast początkowego A.
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — 856
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/_tagi-dowody/856.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
