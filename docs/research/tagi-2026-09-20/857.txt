
── Kontrola ujemna: 857 ──
  plik:     app/Domain/Tags/Actions/UpdateTagFollows.php
  MD5 przed: 31c2a9652d49a64771a41ee4e7172297
  oczekuję:  Ponowne obserwowanie przepisało datę

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 31c2a9652d49a64771a41ee4e7172297 → 42ad417363504290052fece72a682b30.
  różnica:

         private function insert(User $user, array $ids): void
         {
    -        $ids = array_values(array_unique($ids));
    +        $user->followedTags()->syncWithoutDetaching(array_fill_keys($ids, ['created_at' => now()])); $ids = array_values(array_unique($ids));
             if ($ids === []) {
                 return;
             }
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „Ponowne obserwowanie przepisało datę” wystąpił w wyjściu.
    Ponowne obserwowanie przepisało datę.
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — 857
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/_tagi-dowody/857.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
