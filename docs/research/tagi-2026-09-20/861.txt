
── Kontrola ujemna: 861 ──
  plik:     app/Http/Requests/TagSelection.php
  MD5 przed: 1f7d8a3982aad542635ab6ca0732af07
  oczekuję:  Odrzucone wejście uruchomiło weryfikację istnienia

── 1/4 — czy test w ogóle przechodzi na nietkniętym źródle ──
✓ Test przechodzi na nietkniętym źródle.
── 2/4 — mutacja i dowód, że weszła ──
✓ Mutacja weszła: 1 podmian, MD5 1f7d8a3982aad542635ab6ca0732af07 → 3ba150d8cc0f489ed6d39475ceb68879.
  różnica:
                 }
             }
             $tags = array_values(array_unique(array_map('strtolower', $tags)));
    -        Validator::make(['tags' => $tags], ['tags' => ['array', 'max:'.$limit]], [
    +        Validator::make(['tags' => $tags], ['tags' => ['array', 'nullable']], [
                 'tags.max' => 'Wybierz najwyżej :max tagów z listy i zapisz ponownie.',
             ])->validate();
             $format = Validator::make(['tags' => $tags], ['tags.*' => ['bail', 'required', 'string', 'uuid']]);
── 3/4 — czy test to wykrywa, i czy z właściwego powodu ──
✓ Test oblał Z OCZEKIWANEGO POWODU — wzorzec „Odrzucone wejście uruchomiło weryfikację istnienia” wystąpił w wyjściu.
    Odrzucone wejście uruchomiło weryfikację istnienia.
── 4/4 — czy po przywróceniu źródła test znów przechodzi ──
✓ Test znów przechodzi. Pełny przebieg: PASS → FAIL → PASS.

✓ KONTROLA UJEMNA POTWIERDZONA — 861
  zapis: /mnt/c/Users/matma/Documents/kuking-flota/_tagi-dowody/861.json
✓ Źródło przywrócone: MD5 i mtime PORÓWNANE ze stanem sprzed przebiegu.
